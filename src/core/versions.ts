/** Upstream behavior baseline for the native runtime. */
export const TURBO_BASELINE_VERSION = "8.0.23" as const

/** Canonical server helper baseline used by the standalone Rails demo. */
export const TURBO_RAILS_BASELINE_VERSION = "2.0.23" as const

/** Oldest turbo-rails release supported by the public gem matrix. */
export const TURBO_RAILS_MINIMUM_VERSION = "2.0.10" as const

/** Rails and Action Cable baseline used by the standalone Rails demo. */
export const RAILS_BASELINE_VERSION = "8.1.3" as const

/** Expo Turbo wire protocol version. */
export const EXPO_TURBO_PROTOCOL_VERSION = "0.1" as const

/** Package runtime version. Kept explicit until release metadata generation exists. */
export const EXPO_TURBO_RUNTIME_VERSION = "0.1.0" as const

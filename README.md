# expo-turbo

Host-neutral XML protocol runtime foundation for Expo React Native and Rails, targeting Turbo 8.0.23 semantics.

> [!IMPORTANT]
> This repository implements an early XML tree, typed registry, React renderer with opt-in document/Frame lifecycle, and Rails XML foundation. It does not yet implement or claim complete Turbo compatibility, and no package has been published.

## Repository layout

- `src/` — public TypeScript package source.
- `rails/` — public `expo_turbo-rails` gem and non-route-owning Rails Engine.
- `example/expo/` — standalone native consumer and compatibility gallery.
- `example/rails/` — standalone Rails and Action Cable host.
- `protocol/` — shared protocol manifest and conformance fixtures.
- `docs/` — public architecture, compatibility, and release guidance.

The repository root is the publishable TypeScript package. It is intentionally not a package-manager workspace; both example applications keep independent dependency state.

## Development

The TypeScript package requires Bun 1.3.14 or newer:

```sh
bun install
bun run check
```

The Ruby scaffold requires Ruby 3.2 or newer:

```sh
cd rails
bundle install
bundle exec ruby "$(bundle show rake)/exe/rake"
bundle exec ruby "$(bundle show rake)/exe/rake" build
```

Install each example independently, then run both checks from the repository root:

```sh
cd example/expo && bun install --frozen-lockfile
cd ../rails && bundle install
cd ../..
bun run examples:check
```

See [CONTRIBUTING.md](./CONTRIBUTING.md) before proposing changes.

## Support checklist

A checked item is implemented and covered by the public test suite in the current source snapshot. It does not imply complete Turbo compatibility; the package remains pre-release until the full conformance and physical-device gates pass.

### At a glance

- [x] **Package surface:** ESM TypeScript package with explicit `core`, `adapters`, `react`, `registry`, and `testing` boundaries.
- [x] **XML and document tree:** strict namespace-aware document and Turbo Stream-fragment parsing, limits, deterministic XML/tree diagnostics, stable IDs, and structural selectors.
- [x] **Native rendering:** typed component registries, schema/attribute admission, component actions, scoped state, and a React renderer for registered XML components.
- [x] **Visits, links, and Frames:** same-origin document/Frame loading, bounded history/cache/preload behavior, link activation, Frame targeting/lifecycle, and selected native focus/scroll equivalents.
- [x] **Forms:** registered string-valued native controls, constraint-validation hooks, URL-encoded and bounded `text/plain` planning, submission lifecycle, and authoritative XML/Stream/redirect/empty response handling. The Expo example exposes an opt-in native-only Frame-form panel against the standalone Rails host. A controlled renderer test and separate desktop provider/root smoke cover initial Frame loading, server-owned `422` XML, an adapter-followed canonical `303` GET, and replacement-input reset; neither is release-build or physical-device evidence. Multipart and File/Blob uploads are not supported.
- [x] **Turbo Streams:** built-in and registered custom actions, target/selector dispatch, refresh handling, and identity-safe cleanup.
- [x] **Action Cable transport foundation:** strict `actioncable-v1-json` codec, a pure same-origin endpoint resolver, and an injected one-socket WebSocket adapter. The adapter preserves opaque identifiers, waits for `welcome` and per-identifier confirmation, routes exact string Stream deliveries, and honors only a valid server `disconnect` with `reconnect: true`: it closes the old socket, marks active sources reconnecting, creates one replacement, resubscribes after its `welcome`, and reports `connected(true)` only after each previously connected record is confirmed again. The registry can opt into `reconnectRefresh`, scheduling one scroll-preserving canonical current-document reconciliation after the first active re-confirmation in that reconnect cycle; paired with `DocumentReconnectReconciler` and `DocumentRefreshController`, one request that arrives during an active document visit waits for the next idle document and is dropped unless its captured URL still owns it. This is separate from ordinary Stream `refresh` and never reconciles a Frame. Socket close/error, malformed input, unsupported protocol, `reconnect: false`, and command failure remain terminal. A desktop CI smoke now GETs the Rails XML document, retains its signed public Stream source through the public registry and adapter, completes a real `/cable` WebSocket handshake, POSTs one Redis-backed XML Stream broadcast, then emits the exact unsubscribe command and closes the socket. The package adapter owns no credentials, heartbeat, reconnect timing/retry policy, device lifecycle, browser/device origin policy, Redis connection configuration, or physical-device proof.
- [x] **Rails foundation:** a route-free Engine, Expo Turbo XML rendering, Frame/Stream helpers, template-capability validation, and tests against `turbo-rails` 2.0.10 and 2.0.23.
- [x] **Cross-runtime contract:** source-only shared XML fixtures exercise the TypeScript package and Rails gem against the same protocol manifest.
- [x] **Examples and bundle checks:** independently installed Expo and Rails examples; Expo checks export web, iOS, and Android bundles. Native exports catch bundle drift, not release-build or physical-device behavior.
- [ ] **Still required for a production release:** Action Cable credentials/authentication, full reconnect/lifecycle policy, protected Channels, frame-aware reconciliation, complete conformance coverage, physical-device/accessibility evidence, and npm/RubyGems publication.

The manual **Release candidate** workflow runs only from merged `main`, builds and clean-installs the exact npm tarball and Ruby gem, records a paired checksum manifest, and verifies its GitHub OIDC repository, `main` ref, and `release` environment claims. It deliberately cannot publish packages, create tags, or create releases. That keeps candidate verification separate from the remaining compatibility gates. Before any registry trusted publisher is enabled, protect the `release` environment with reviewer and `main` deployment rules. The first unscoped npm publication also remains a deliberate human 2FA bootstrap: npm trusted publishing can be configured only after the package exists.

### Detailed implemented surface

- [x] Public-source ESM package with explicit `core`, `adapters`, `react`, `registry`, and `testing` exports.
- [x] Host-neutral fetch, form-confirmation, form-announcement, navigation, Cable subscription-interface, strict Action Cable v1 JSON codec, pure `resolveActionCableEndpoint` admission, injected single-lifetime `ActionCableV1WebSocketAdapter`, lifecycle, visibility, focus, bounded same-document `DocumentAnchorScrollAdapter` requests, one-shot `DocumentRefreshScrollAdapter` reset, bounded root-history `DocumentHistoryScrollAdapter` restoration, style, storage, observability, clock, request-ID, and restoration-ID adapter contracts. `resolveActionCableEndpoint` maps one credential-free HTTP(S) origin plus an explicit same-origin path to a `ws:`/`wss:` URL and rejects userinfo, queries, fragments, origins with paths, and authority-changing or normalizing mount paths; it does not select hosts, carry tickets, or create sockets. The Cable adapter uses that host-supplied `ws:`/`wss:` endpoint and exact `actioncable-v1-json` protocol, sends subscriptions only after `welcome`, reports `connected` only after an exact confirmation, shares one server subscription per opaque identifier, and closes when its final local subscription leaves. A valid server `disconnect` with `reconnect: true` is the sole rotation path: live identifiers resubscribe only after the replacement `welcome` and re-confirmation. An optional registry `reconnectRefresh` requester receives one frozen `{ baseUrl, scroll: "preserve" }` request after the first active `connected(true)` in a reconnect cycle; initial connections, terminal transitions, stale transports, and ordinary Stream `refresh` do not invoke it. Socket close/error, malformed input, an unsupported negotiated protocol, `reconnect: false`, or command failure is terminal and reported as a redacted `SubscriptionError`; the adapter owns no credentials, retry timing, authentication, heartbeat monitoring, URL derivation, or native lifecycle/network binding.
- [x] Bounded post-active-document Cable reconciliation through `DocumentReconnectReconciler`: a host may wrap a `DocumentRefreshController` before injecting it as `CableStreamSourceRegistry.reconnectRefresh`. If a server-directed reconnect re-confirms while a document visit is active, it retains only the latest frozen recovery request and hands it off once the visit sequence reaches idle. The underlying refresh controller still coalesces/debounces and admits only an active document whose URL canonically matches the captured URL, so a changed document, stale request, disposed reconciler, or a visit that remains active starts no extra GET. Handoff failures are redacted. This does not retry sockets, replay messages, reconcile Frame sources, or change ordinary Stream `refresh` semantics.
- [x] Typed protocol errors, pinned version baselines, and an injected development inspector.
- [x] Expo example CI bundles web, iOS, and Android exports; the native exports produce Hermes bytecode and catch Metro/runtime dependency drift, but do not substitute for release-build, simulator, or physical-device evidence.
- [x] Strict, namespace-aware XML documents and ordered multi-root Turbo Stream fragment parsing.
- [x] Pre-parse byte/depth/DOCTYPE/processing-instruction guards plus node, attribute, text, and Stream-action limits.
- [x] Shared text normalization with inherited `xml:space`, explicit `default` reset, preserved CDATA, collapsed XML whitespace, and deterministic fixture/diagnostic serialization.
- [x] Addressable mixed-content tree with deterministic keys, unique ID lookup, parent links, Frame/source indexes, comments, and CDATA.
- [x] Case-sensitive XML selector queries for lists, tags, IDs, classes, attributes, combinators, and structural pseudo-selectors without stale-result caching.
- [x] Typed component/module registry composition, Zod prop admission, explicit attribute codecs, child policies, aliases/deprecations, duplicate ownership errors, and deterministic capabilities.
- [x] Typed component-action definitions/modules with Zod params, injected state, provider-backed React invocation, serialized execution, and ordered `onSuccess`/`onError`/`onEnd` lifecycle.
- [x] Document-scoped external state store with stable per-key snapshots, fine-grained subscriptions, no-op suppression, component-action sharing, React bindings, and Strict Mode replay-safe provider-unmount disposal.
- [x] Core identity-bound Frame/form state-scope registry with child-update preservation, replacement/removal cleanup, explicit disposal, and stable-ID reuse isolation.
- [x] React Frame/form state inheritance with automatic Frame boundaries, explicit form boundaries, nearest-scope bindings/actions, update preservation, and replacement cleanup.
- [x] Host-neutral, identity-bound registry for the caller-admitted string-entry subset: enabled named values, checked checkables, and prefiltered ordered multi-values owned by the nearest form or an explicit exact-ID `form` association. Explicit ownership overrides ancestry, entries follow whole-document XML order, repeated/empty entries are preserved, the activated submitter follows Turbo 8.0.23's append-last ordering, and exact-node cleanup follows control or form removal/replacement.
- [x] Bounded explicit native multi-name string-entry snapshots: one exact registered control with no outer `name` may contribute at most `MAX_FORM_CONTROL_ENTRIES_PER_CONTROL` (256) ordered `{ name, value }` pairs at that control's whole-document position. Entry names and values are strings and may be empty; pair order and duplicate names/values are preserved, and the hidden-control `_charset_` rewrite does not apply. Exact form ownership/external association, direct and fieldset disabledness, datalist exclusion, optional validity, immutable updates, and exact-node cleanup remain shared with other controls. The host supplies the snapshot; this does not discover XML/DOM controls, infer `dirname`, accept File/Blob values, or enable multipart/upload transport.
- [x] Explicit native select snapshots: a disabled or unnamed select emits nothing; otherwise selected options emit in host-supplied option order only when neither directly disabled nor inside a disabled `optgroup`, preserving repeated and empty values. An explicit value, including `""`, wins; otherwise a host-supplied raw text snapshot is stripped and collapsed using HTML ASCII whitespace. Hosts own current selectedness, order, and group state; the package performs no XML/DOM option discovery or browser default selection.
- [x] Capability-visible native fieldset semantics for explicitly registered controls: component definitions declare `fieldset` or `legend` roles, core evaluates every live disabled-fieldset ancestor independently, only the first direct semantic legend exempts its descendants, external `form` ownership does not change physical ancestry, inherited-disabled controls are omitted, inherited-disabled submitters are rejected, and React projects the effective disabled/accessibility state. Automatic XML/DOM control discovery remains unsupported.
- [x] Capability-visible native datalist successful-entry semantics for explicitly registered controls: component definitions declare a `datalist` role, physical datalist ancestry omits ordinary entries from every descriptor family regardless of external `form` ownership, an explicitly selected submitter still follows Turbo 8.0.23's manual append-last behavior, and React leaves the control's disabled/accessibility state unchanged. Automatic XML/DOM discovery, suggestion behavior, and child hiding remain unsupported and host-owned.
- [x] Explicit native hidden-control entries: enabled named hidden descriptors emit their caller value or `""` when absent, while only an ASCII-case-insensitive `_charset_` hidden name replaces that value with `UTF-8`. Authored name casing and XML order are preserved; duplicates emit independently; disabled, unnamed, empty-name, fieldset-barred, and datalist-barred controls stay unsuccessful; ordinary value descriptors and the manually appended submitter are not rewritten; automatic XML/DOM hidden-input discovery remains unsupported.
- [x] Explicit value/hidden direction companions: a nonempty host-admitted companion name plus resolved `ltr`/`rtl` is emitted immediately after its owning value, including empty primary values and hidden `_charset_` replacement, without browser control discovery or direction inference.
- [x] Turbo 8.0.23's manual activated-submitter append: only a nonempty submitter name plus raw value or `""` is appended last. Image-button `.x`/`.y` coordinates and submitter `dirname` sidecars from the browser's optional-submitter entry-list algorithm are deliberately absent because pinned Turbo constructs `FormData(form)` without that submitter.
- [x] App-installed string-only native form-associated component equivalents through registered components and `useExpoTurboFormControl`, including bounded multi-name `entries` snapshots. Browser custom elements and `ElementInternals` lifecycle/state callbacks are N/A on React Native; File/Blob values and upload transport remain pending.
- [x] Explicit React form ownership and live native control registration with `formOwner: true` component declarations, current-value updates, registration-bound submitter selection, nearest-form isolation, exact-ID external controls/bindings/submitters, target insertion/removal/same-ID replacement rebinding, Strict Mode replay safety, and stale-owner proposal rejection. Association changes form transport ownership only; it does not move React/state-scope ancestry.
- [x] Turbo 8.0.23-compatible native form interception modes: document form controls default to `on`, support global `off` and form-ancestor `optin`, classify the form and an activated internal or external submitter against their independent live `data-turbo` ancestry, and expose the synchronous decision through core and React. Explicit planning/submission APIs remain intentional host-owned bypasses.
- [x] Pure immutable form request planning from raw form/submitter metadata and at most `MAX_FORM_REQUEST_ENTRIES` (1,024) successful string entries: credential-free same-origin HTTP(S) action resolution; submitter/form action, method, and enctype precedence; GET Stream opt-in and automatic unsafe Stream headers; GET query replacement; URL-encoded unsafe bodies; Rails `_method` normalization; submitter-entry consistency; and shared protocol headers. Explicit action fragments and multipart bodies fail closed.
- [x] Staged one-read form-request input admission: every top-level planner field is read once at its admission stage, and form, submitter, protocol, and entry scalar values are immediately cloned and frozen before any later field can mutate them. Same-origin/credential URL admission happens before entries are read. Throwing property reads, revoked Proxies, entry-array traps, and entry-field failures become package-owned `RequestError`s without retaining host causes or values; the exact `AbortSignal` reference remains intentionally live. Active request-plan, proposal, submit, and retry entry points likewise stage and redact their outer option and protocol reads before composing or dispatching a request.
- [x] Bounded HTML/native-equivalent `text/plain` request serialization for string-only raw-body endpoints: normalized scalar names and values remain unescaped in exact `name=value\r\n` order with duplicates, empty names and values, and literal delimiters. Each emitted pair ends in CRLF, an empty list emits an empty body, and the final UTF-8 representation is limited to `MAX_FORM_TEXT_PLAIN_BODY_BYTES` (1 MiB) during admission. GET remains URL-encoded and bodyless; direct unsafe methods keep `_method` as ordinary data, while Rails-normalized POST works only when the host intentionally consumes the raw body. Rails body-based method overrides stay rejected because Rack does not parse `text/plain`, and the standard explicitly makes the format ambiguous.
- [x] Immutable active-form request planning in core and React from current form/submitter attributes, registration-bound submitter identity, live XML-ordered entries, and caller-supplied protocol metadata/signal; it performs no fetch, destination targeting, pending UI, validation, or response handling.
- [x] Atomic active-form submission proposals in core and React with Turbo 8.0.23 submitter/form/Frame target precedence, distinct inside/outside-Frame blank and missing-target behavior, top-level named-Frame capture, internally derived `Turbo-Frame` metadata, browser-target rejection, and hidden exact form/submitter/destination/tree-generation identity; proposals perform no fetch or request-ownership change.
- [x] Caller-scoped form request execution lanes that admit only planner-issued requests, create the owning abort signal before planning, forward the exact request once, supersede only within that lane, settle cancellation even when an adapter ignores the signal, suppress late fetch/body results, enforce credential-free same-origin final URLs and negotiated MIME, and return frozen unapplied `xml`, `stream`, `empty`, or `canceled` candidates with `2xx`/`4xx`/`5xx` classification. Candidate creation does not resolve a destination or mutate the active tree.
- [x] Destination-aware form request ownership through `FormSubmissionController`: exact proposals are re-admitted before ownership, document and exact-Frame lanes are shared with GET loaders across instances, pre-admission failures cannot cancel an incumbent, shared-destination supersession remains owner-safe, an explicit same-form supersede can change destinations safely, distinct Frames remain concurrent, and the exact request is fetched once.
- [x] Canonical ownership-safe form response disposition through `FormSubmissionController`: the exact lease is retained and rechecked before staged mutations, and reentrant supersession stops and reports undispatched actions; negotiated Streams dispatch for `2xx`/`4xx`/`5xx`; successful matching-Frame, origin-Frame error, or document Expo XML, including authoritative server-rendered `422`, commits without refetch; ordinary `204`/blank-`201` are no-ops while adapter-followed empty Frame redirects update `src`; successful documents adopt the validated final URL, adapter-followed Frame XML updates `src`, and top-level `4xx`/`5xx` preserve the active document URL; unsafe nonredirect document `200`, invalid responses, or stale candidates cannot begin mutation.
- [x] Optional Turbo 8.0.23 form-response snapshot invalidation against a host-shared `DocumentSnapshotCache`: unsafe successful document XML clears before strict parse/application, while authoritative document `4xx`/`5xx` clears after replacement commits, including committed finalization failure; any unsafe successful or failed Frame response clears after exact response admission and before MIME/body/Frame parsing, including unsafe `204`/blank-`201`, wrong MIME, malformed XML, and missing Frames. Safe successful XML, document empty responses, direct Stream responses, denial, cancellation, supersession, and network failure preserve the cache. Hosts must inject the same cache instance used by document visits and Frame history.
- [x] Optional Turbo 8.0.23 form history/action coordination through aligned document history and exact mounted Frame controllers: the activated submitter's first-present `data-turbo-action` masks the form value, and only exact lowercase `advance`, `replace`, or `restore` is recognized. Successful document XML proposes from the validated final response URL; default/`advance` pushes except when that final URL equals the active entry, while `replace` retains the current index. Safe GET application optionally captures the outgoing snapshot when a shared cache is configured, then commits history → tree; unsafe success clears that cache before history → tree. For an exact active Frame destination, explicit or inherited `advance`/`replace` uses that Frame controller's existing `FrameHistoryCoordinator` and stable restoration scope without issuing a Frame GET: the response path optionally stages the latest outgoing whole-document snapshot, admits the final `src` before buffering except for a potentially empty `201`, then commits optional snapshot storage → history → Frame children/logical document URL only for successful matching XML. Direct Frame visits and form promotions therefore share one restoration identifier. Authoritative `4xx`/`5xx`, direct Streams, empty responses, denial, cancellation, supersession, transport/MIME/parse/missing-Frame failure, history drift, snapshot failure, and host-write failure publish no form history entry; Frame `restore` remains whole-document-only and fails closed.
- [x] Session-shared exact-form submission activity in core and React: observable busy state tied to the immutable planner-issued request; atomic rapid-duplicate prevention by default with an explicit same-form `supersede` override; pending/effective-disabled state plus nonempty raw `data-turbo-submits-with` presentation for only the activated submitter; and deterministic cleanup on success, failure, abort, form removal, or final mounted React form-scope/provider unmount before document, Frame, or Stream response application. Strict Mode replay and multiple providers share exact-form ownership; headless callers cancel explicitly. The standalone Expo example exercises this path.
- [x] Host-injected async native form confirmation through `FormSubmissionController`: the immutable proposal captures submitter-first `data-turbo-confirm` by attribute presence, including empty and whitespace values; confirmation receives a cancellation signal and must return a literal boolean; visible busy/submitter state, destination ownership, and fetch begin only after acceptance. Denial, duplicate prevention, explicit cancellation, replacement/removal, final form-scope/provider unmount, adapter failure, and stale or superseded approval cannot send or revive a request. The standalone Expo example supplies a native Alert adapter plus a settling browser-confirm fallback.
- [x] Turbo 8.0.23-compatible logical form submission lifecycle through an optional shared `FormSubmissionLifecycle`: `submit-start` follows resumed `before-fetch-request`, native history/navigation admission, and exact busy/submitter presentation immediately before physical fetch; `submit-end` clears presentation at response admission before body buffering, or at pre-response failure/abort, and precedes XML parsing or document/Frame/Stream application. Both are frozen synchronous notifications with stable listener snapshots and isolated redacted faults. Start exposes one frozen identity-bound handle with safe node/destination/request metadata, live `waiting`/`stopping`/`stopped` state, and exact `stop()` cancellation; end mirrors Turbo's top-level handle-only, response (`success` plus frozen status/redirect metadata), or redacted-error shape. Every emitted start receives one end across response, failure, abort, removal, or supersession, while validation failure, duplicate prevention, confirmation denial, and pre-start request/history cancellation emit neither. Registered forms, retries, and generated-form links share the controller lifecycle; the standalone Expo runtime provides one app-owned instance and a rendered-form smoke test.
- [x] Host-owned synchronous native constraint validation before interactive submit/retry: registered value, checkable, same-name multiple, select, and multi-name entry-list controls may provide a frozen valid or invalid snapshot with host-owned message copy; every eligible invalid control is returned in whole-document order, including unnamed and externally associated controls, while disabled/fieldset-barred, datalist-barred, hidden, and submitter controls are skipped. Form `novalidate` and the exact activated submitter's `formnovalidate` use boolean-presence semantics. An invalid attempt resolves as `status: "invalid"`, focuses only the first invalid logical node through an injected `FocusAdapter`, and starts no confirmation, busy/pending, terminal, ownership, or fetch work. Focus is an exact synchronous host boundary: thrown failures, returned values, Promises, and thenables fail redacted. Pure entry collection, request planning, and proposal creation remain deliberately validation-free, server `422` XML remains authoritative, and browser `invalid` events/default validation UI are N/A. The standalone Expo example uses an app-owned Zod rule plus identity-safe `TextInput` handles and user-driven focus tracking; physical-device and screen-reader evidence remains pending.
- [x] Exact-form terminal submission reporting and host-rendered recovery in core and React: a session-shared frozen snapshot distinguishes applied, empty, canceled, successful-but-unapplied, uncommitted failure, and committed-with-error outcomes only after busy/submitter cleanup; unapplied truth records visit prevention, lifecycle supersession, or awaited outside-root/excluded-extension navigation without pretending a completed mutation was canceled. Exact-attempt settlement prevents stale or superseded work from overwriting newer truth. A provider-registered form boundary can dismiss results or explicitly retry only a safely retryable failure with a fresh request ID; retry reuses the exact live submitter and replans from current registered values, while `retryFailure` refuses unsafe, unapplied, or already-committed outcomes.
- [x] Host-injected exact-form terminal live announcements in React: each new non-`none` redacted terminal revision is offered to the adapter at most once per exact form only after busy and activated-submitter cleanup; initial or cleared state, pre-start denial, stale or superseded settlement, Strict Mode replay, multiple providers, removal, and same-key replacement do not duplicate or leak an announcement. Hosts own localized copy and platform delivery; the standalone Expo example maps the contract to React Native `AccessibilityInfo` and persistent polite/assertive web live regions.
- [x] Bounded typed state-reference decoding with exact `{"$state":"key"}` values, `{{state:key}}` scalar interpolation, recursive arrays/objects, nearest-scope action admission, and typed failures.
- [x] Bounded semantic style adapters with schema-owned `style-tokens`, frozen capability manifests, token/group/component limits, deterministic precedence, explicit React resolution, and an example-owned native corpus.
- [x] Deterministic descendant-first subtree disposal hooks with identity-safe replacement handling, explicit unregister, typed cleanup errors, and once-only registered-component cleanup on logical or React unmount.
- [x] Typed logical event bus with serial dispatch, cancellable notifications, nested pause/resume, stable listener snapshots, unsubscribe, and post-failure queue recovery.
- [x] Native `before-cache` equivalent on the optional `DocumentVisitLifecycle` for ordinary cacheable outgoing-document captures owned by network visits, cached previews, explicit cached restores, cached or network host traversal, and successful safe-GET document forms. The frozen notification-only event runs outside the destination commit guard so synchronous listener mutations are included, then exact ownership, history, and tree checks run again before guarded snapshot/history commitment and replacement-tree application. Listener snapshots are stable and observer failures are isolated and redacted. Initially `no-cache` documents, refresh, preload/manual capture, unsafe/failed/empty/Stream form responses, and promoted-Frame snapshots intentionally emit nothing; browser next-task cloning and renderer-dependent Frame timing are not claimed.
- [x] Native `frame-missing` equivalent through an optional shared `FrameLifecycle` for eligible direct, redirected, bounded-recurse, and Frame-form XML responses that omit the expected application Frame. Failed cross-target forms transfer from the destination to the origin Frame before dispatch. The frozen cancellable event exposes only `{ frameId, response: { redirected, status, url }, visit }`; unprevented handling retains the typed `FrameMissingError`, while prevention produces explicit non-applied `prevented` truth. The package's missing-response path performs no tree mutation before the optional visitor. `visit()` records one `advance`/`replace` intent; an optional trusted response visitor receives the already-buffered XML only after the exact Frame/form lease is released, so host-controlled promotion needs no response refetch and ordinary listeners never receive body or headers. Matching Frames, primary `204`, negotiated Turbo Stream responses, MIME/parse failures, loop/depth errors, and work already stale, canceled, or superseded before dispatch emit nothing; listener-side supersession cancels continuation and visitor execution after the event already emitted. Browser DOM bubbling, literal `Content missing` markup, raw `Response` exposure, and browser physical-paint timing are not claimed.
- [x] Native `before-frame-render` through the optional shared `FrameLifecycle`: after a direct/recurse Frame GET or Frame-targeted form has matched and preflighted its exact response Frame, the frozen logical event exposes `{ frameId, newFrame, renderMethod: "replace" | "morph", url, render }` before the prepared Frame mutation, embedded Streams, autofocus, or renderer acknowledgement. `newFrame` is the parsed, stream-extracted response Frame; only a bounded direct `FrameController.reload()` with exact `refresh="morph"` reports `"morph"`, while direct/recurse navigation and Frame-targeted forms report `"replace"`. Stable synchronous listeners may replace `render` only with a wrapper that receives a controlled context, calls `renderDefault()` exactly once, and returns `undefined`; they cannot select a different render mode. Empty, Stream-only, missing, promoted, malformed, wrong-MIME, canceled, and superseded responses emit nothing. This deliberately does not claim browser DOM elements, cancellation/pause/resume, arbitrary native-tree replacement, broader Frame morph, or physical-paint timing.
- [x] Renderer-acknowledged native `frame-render` and `frame-load` notifications through an optional shared `FrameLifecycle`: every controller-owned matching Frame GET prepares an exact mounted-Frame ticket before mutation, whether or not public lifecycle observers exist. After replacement children and embedded Streams settle, React acknowledges only the live Frame at the sealed revision and applies Frame autofocus; the controller then publishes terminal GET state (`busy: false`, `complete: true`) before it dispatches frozen `frame-render` `{ frameId, renderMethod: "replace" | "morph", url }`, rechecks exact ownership, and dispatches frozen `frame-load` `{ frameId, url }`. Only the bounded direct reload path can report `"morph"`; applied Frame-target form XML remains `"replace"` and does not make Frame-controller `busy`/`complete` own the form submission. A listener-started reload, Frame replacement, disposal, or supersession suppresses stale notifications. Headless, empty, Stream-only, missing, promoted, canceled, superseded, unavailable, and same-ID-replaced Frames emit neither post-render event and do not wait; a failed acknowledgement releases the visual wait without either event. Broader Frame morph, browser DOM attributes, and browser physical-paint/repaint timing are not claimed.
- [x] Turbo 8.0.23-compatible automatic Frame response promotion for an exact sole XML root with `data-turbo-visit-control="reload"`. Eligible direct and bounded-recurse Frame GETs plus Frame-targeted form responses promote the exact buffered `2xx`/`4xx`/`5xx` response through the shared trusted response visitor only after the exact Frame/form lease is released. The marker wins before matching-Frame extraction or embedded Stream application, always requests an owning-document `advance` with reason `visit-control-reload`, and bypasses Frame child mutation, autofocus, Frame history, and `frame-missing`. Failed cross-target forms transfer promotion to their origin Frame; primary direct and form responses retain Turbo-compatible canonical `src` publication, while intermediary recurse responses do not retarget it. Missing or failed visitors fail closed with redacted errors, and stale or superseded ownership cannot promote.
- [x] Optional typed request lifecycle for document loads/preloads, primary and recurse Frame GETs, and form execution/submission: request-local `before-fetch-request` can pause, cancel, or mutate a controlled URL/method/header/body draft, with every mutation re-admitted through transport-specific same-origin, method, body, negotiation, cache-key, and protected-header rules before adapter fetch. `before-fetch-response` can prevent body reading/application with an explicit terminal result. Non-abort transport rejection emits redacted, cancellable `fetch-request-error`; prevention suppresses default visit/Frame/form/refresh/traversal/automatic-preload error delegation while the request still rejects and cleanup still runs. Aborts and late settlements remain cancellation-only, listener failures are redacted, and one paused request never blocks another. An explicitly retried adapter-invoked GET must remain GET after live replanning and lifecycle mutation; failures before adapter invocation retain the existing safe-retry behavior.
- [x] Turbo 8.0.23-compatible semantic-link `click` admission through the exact `DocumentVisitLifecycle` owned by the injected document controller. Every exact-live, enabled, supported, root-visitable plain non-anchor link emits one frozen cancellable `{ nodeKey, url }` event before top-level or Frame ownership; its canonical destination is captured before listener dispatch, so listener-side document URL changes cannot re-resolve a relative link. Prevention returns frozen native no-work evidence and allocates no request/history/navigation owner. Generated-form links, ordinary forms, opt-outs, delegated outside-root/cross-origin/extension URLs, `mailto:`/`tel:`, disabled links, unsupported target/download metadata, and non-anchor stale activations emit no click. The bounded same-document anchor path is the documented exception: it emits `click` under its narrower admission/revalidation contract without root-policy or visit ownership. React Native has no DOM `MouseEvent`, bubbling path, or automatic browser fallback after prevention.
- [x] Optional synchronous owning-document lifecycle for `DocumentVisitController`, successful document responses from `FormSubmissionController`, and promoted Frames through `FrameHistoryCoordinator`: cancellable `before-visit` runs after canonical proposal admission but before ordinary document history/tree application. Notification-only `visit` runs once after exact ownership and `started` publication for network visits, cached previews, explicit cached restores, current-document refresh, and cached or network host traversal. Host traversal deliberately skips `before-visit`; preview revalidation does not repeat the original event. A successful document form response proposes the final response URL after request and form-activity cleanup, using an explicit action or Turbo's default same-location-redirect `replace`: prevention or listener supersession returns successful `unapplied` truth, while outside-root/excluded-extension final URLs await one required host navigation and emit no `visit`; only root-visitable URLs emit `visit` and proceed to strict parse/history/tree application. Because Turbo proposes a Visit before rendering, a later strict parse failure retains that pair. A successful root-visitable document redirect emits Turbo's second final-URL `visit` with `replace` only after final history/tree commitment, without another `before-visit`, fetch, or controller start; redirected HTTP-error documents, empty, discarded, and stale outcomes emit no follow-up. Exact `advance`/`replace` Frame GET, lazy-appearance, and successful Frame-form promotions instead emit one final-response-URL pair only after history, Frame children, logical document URL, embedded Streams, controller completion or form cleanup, and autofocus intent are logically committed. Late prevention never rolls back that truth; listener supersession suppresses only the stale `visit`; outside-root/excluded-extension final URLs await the coordinator's host navigation and emit no `visit`; matching direct-GET `4xx`/`5xx` Frames may promote, while failed Frame forms do not. With request lifecycle installed, a controller-owned document GET orders `before-visit` → `before-fetch-request` listeners → `visit` → pause/cancellation settlement → physical fetch; a document form response instead finishes its request lifecycle and cleanup before `before-visit` → `visit`. Listener snapshots are stable, async returns fail redacted, notification faults are observer-only with redacted asynchronous surfacing when unhandled, and reentrant newer proposals remain authoritative. Promoted-Frame timing remains logical post-commit; it does not emit the ordinary mounted-Frame `frame-render`/`frame-load` events documented above.
- [x] Renderer-acknowledged native owning-document `render` and `load` notifications on `DocumentVisitLifecycle`: with a mounted `ExpoTurboRoot`, the originating document controller or form waits for React to acknowledge the exact committed generation at the current revision, after the response-owned revision is sealed. Frozen `render` detail is `{ generation, preview, renderMethod: "replace" | "morph", url }`; only a successful bounded current-document Refresh Stream morph reports `"morph"`. It fires after the registered React tree or root `renderError` fallback commits and before any applicable document autofocus. Controller-owned cached previews render before canonical fetch and never load; authoritative cached, network, refresh, and traversal documents render, publish terminal `completed` or `failed` state, then emit frozen `load` detail `{ generation, url }`, including committed GET `4xx`/`5xx` documents. Document-targeting forms include embedded response Streams in the revision seal, render successful and authoritative error XML, and load only successful documents after busy/submits-with cleanup and terminal publication. Strict Mode replay, observer-induced revisions, superseded generations, explicit post-commit controller cancellation, and final renderer disposal cannot duplicate or strand work; cancellation preserves installed completed/failed truth while suppressing render/load and response autofocus. Headless work emits neither event and does not wait.
- [x] Native notification-only owning-document `reload` on `DocumentVisitLifecycle`: an active `DocumentVisitController` GET whose raw fetch/body transport rejects or whose MIME admission fails dispatches frozen redacted `{ cause: "transport" | "content-type", reason: "request-failed" }` after it publishes `failed` and before ordinary error observers run. Current-document refresh and canonical preview revalidation remain eligible active GETs; request-lifecycle default prevention, cancellation, supersession, explicit restore, host traversal, parse/policy/commit errors, committed XML `4xx`/`5xx`, forms, Frames, and preload emit nothing. It is an observation/recovery hook, not browser hard reload, retry, page invalidation, or DOM behavior.
- [x] Turbo 8.0.23-compatible synchronous Stream render admission through an optional shared `StreamLifecycle`: every direct, Frame-response, form-response, embedded, and Cable-delivered action emits cancellable `before-stream-render` before action/target resolution. Prevention consumes only that action, reports `canceled`, and continues later siblings. Registered listeners may wrap the one-shot default renderer or replace it with an explicit synchronous native renderer result; pre-mutation invalid, asynchronous, or failing renderers become isolated redacted action errors without poisoning later siblings, while an asynchronous renderer rejected after mutating the session stops dispatch as committed truth. Frozen native-only `stream-action` notifications expose the final `applied`, `noop`, `canceled`, or `error` report after each action and before the next sibling, with stable listener snapshots and observer fault reporting. A successful default-rendered standalone exact-target `replace`/`update method="morph"` also emits frozen native-only `stream-morph` `{ action, index, targetId }` after its tree commit and immediately before `stream-action`; direct fragments, Cable messages, and negotiated Stream form responses are eligible, while a wrapper that invokes `renderDefault()` remains eligible. Complete renderer replacements, plain methods, selector-only/missing targets, canceled/no-op/error actions, and document/Frame-embedded Streams emit no `stream-morph`. It is a logical action-level native signal, not Turbo's per-DOM-node morph event or a physical-render boundary. The standalone Expo runtime shares one instance across direct controls, Frames, forms, and refresh dispatch. Browser `nextRepaint()` timing, asynchronous renderer overrides, per-element/attribute morph lifecycle, and Stream renderer-flush timing remain pending below.
- [x] Static React protocol renderer with stable `useSyncExternalStore` node snapshots, update-preserving component identity, same-ID replacement remounts, isolated subtree updates, registered error surfaces, mixed children, and non-rendering protocol nodes.
- [x] Ordered Turbo Stream fragment dispatch for `append`, `prepend`, `replace`, `update`, `remove`, `before`, and `after`, including target/selector precedence, transactional ID-collision preflight, payload cloning, no-ops, and isolated action errors. Exact `update target="…" method="morph"` performs one identity-safe child reconciliation; exact `replace target="…" method="morph"` performs the same reconciliation for one matching ordinary application-element root, retaining that outer node while its attributes and children update. Retention requires the same-parent stable ID and exact element shape; unkeyed, incompatible, and protocol-wrapper descendants remount. At each retained ordinary application parent, a direct child marked `data-turbo-permanent` is the narrow exception: both current and incoming children must be marked, have the same nonblank stable ID and exact shape, and the current node keeps its attributes and complete subtree while the incoming node is validation-only. It may reorder within that same parent. Protocol-envelope markers, markers on the target/outer root or any permanent ancestor, one-sided, unmatched, reparented, incompatible, duplicate, or nested-permanent nodes fail one action atomically. An incompatible child root still remounts its whole subtree even when authored descendant IDs recur. Every other method value retains Turbo's plain structural semantics.
- [x] Bounded current-document `refresh` Stream morph for exact lowercase `method="morph"`: after the existing 150 ms debounce, request-ID suppression, active-URL/idle-visit checks, and ordinary snapshot-free GET/history lifecycle, a successful XML response may perform one preflighted mutation only when it contains exactly one compatible nonpermanent ordinary application root with the same QName and the same root-ID presence/value. Same-parent stable-ID ordinary application elements with exact shape retain React/session/form/state identity; unkeyed or incompatible elements remount, and matched direct `data-turbo-permanent` children retain their current attributes and subtree under the existing strict rule. Protocol descendants, permanent roots/ancestors, changed or missing root IDs, reparenting, duplicate/out-of-subtree IDs, unmatched or nested permanent nodes, and every invalid plan fail before history, tree, revision, or disposal changes with no replacement fallback. The successful morph creates a fresh authoritative generation but deliberately performs no document autofocus. Authoritative `4xx`/`5xx` XML always uses ordinary replacement and reports `renderMethod: "replace"`; every other Refresh `method` value uses ordinary replacement.
- [x] Bounded Refresh Stream scroll policy for the current document: exact lowercase `scroll="preserve"` consumes a successful refresh without host scrolling. An absent, blank, differently cased, or other value requests a top reset through optional synchronous `DocumentRefreshScrollAdapter` `canReset()`/`reset()` only when its `DocumentVisitController` has a live `DocumentVisitLifecycle` and React acknowledges that exact committed generation. Missing or unavailable adapters consume a no-op; empty, authoritative `4xx`/`5xx`, headless, failed, stale, canceled, or superseded refreshes cannot reset. Adapter faults are redacted without rolling back the committed tree. Route/Frame/nested anchors, nested-container discovery, Frame-local scroll, generic host capture/persistence, page-metadata defaults, and physical-scroll completion remain unsupported.
- [x] Typed custom Stream-action registry with reserved/duplicate ownership checks, Zod-validated `data-*` parameters, shared target/template resolution, synchronous app-installed handlers, and isolated failures.
- [x] Host-injected `<turbo-cable-stream-source>` lifecycle for rendered exact source nodes: required nonblank channels, standard-channel signed-name admission, canonical sorted `data-*` identifier parameters, one refcounted subscription per full identifier within the injected registry, registry-owned serialized string delivery through the common Stream dispatcher, attribute-change/removal/unmount cleanup, late-callback suppression, and redacted subscribe/unsubscribe/message failures. Index membership alone never activates a source, and sources inside `template`, Stream, or source subtrees remain inert.
- [x] Redacted observable Cable subscription state: `CableStreamSourceRegistry.connectionSnapshot` is a frozen, revision-cached list of `{ nodeKey, state }` records, and `subscribeConnection(listener)` notifies semantic snapshot changes for listeners to reread. Adapter callbacks and source admission yield `connecting`, `connected`, `reconnecting`, `disconnected`, or `rejected`; shared canonical transports fan out their current state to each exact source, invalid descriptors become `disconnected`, and stale callbacks or observer failures cannot alter or leak state. Snapshots never expose channels, canonical identifiers, signed names, grants, or message data. This is an observability contract, not a socket client, authentication mechanism, reconnect policy, React/UI binding, or device-transport claim.
- [x] Document GET loader with shared Expo Turbo headers, credential-free same-origin HTTP(S) request/final-URL enforcement, MIME validation, parse-before-replace handling for valid `2xx`/`4xx`/`5xx` XML, explicit `204`/empty-`201`/canceled outcomes, owner-aware cancellation, and generation-safe stale-response suppression.
- [x] Final-ownership synchronous document tree-commit callback: it receives the exact frozen parsed candidate only after the last ordinary ownership check, runs under a session-shared guard that blocks reentrant document/Frame claims and makes exact-owner cancellation non-operative, rejects returned values/thenables, and leaves the tree unchanged on callback failure. The guard clears immediately before tree replacement so finalization listeners can start newer work from the committed tree without outer cleanup detaching it. The optional parsed-visit history coordination below uses this exact linearization point.
- [x] Host-neutral document snapshot foundations: production deep clones preserve exact keys, structure, indexes, locations, and the next safe mutation-key sequence without sharing mutable nodes. The Turbo-style cache defaults to ten entries, keys normalized credential-free HTTP(S) URLs without fragments while retaining query identity, refreshes LRU recency on get and put, returns fresh clones for restoration or eligible preview selection, exposes size/clear, and omits temporary subtrees only from the cached copy, never the active document: `data-turbo-temporary` by presence or the deprecated exact `data-turbo-cache="false"` alias; other `data-turbo-cache` values remain cacheable.
- [x] Explicit document-session snapshot capture and manual restore interop: capture requires the active tree's own absolute URL, cache writes reject missing or fragment-free mismatched tree URLs, misses are frozen no-ops, and hits reuse exact tree replacement so revisions, generations, disposal, fresh node identity, commit-before-finalization errors, and late-response invalidation remain consistent. This low-level primitive is separate from the host-traversal coordinator below.
- [x] Root-owned native cache policy metadata and preview selection: exact lowercase `data-turbo-cache-control="no-cache"` makes a document non-cacheable while leaving the independent previewable flag true, and `"no-preview"` remains cacheable but non-previewable. Absent, blank, invalid, differently cased, whitespace-padded, and nested values use the default cacheable/previewable policy. A skipped `no-cache` write leaves any older matching snapshot untouched. Preview lookup performs the normal LRU read before suppressing `no-preview`, so the retained snapshot remains available for unconditional restoration.
- [x] Programmatic safe full-document preload into a host-shared snapshot cache: an explicit fragment-free, root-visitable source performs one non-owning same-origin GET with ordinary Expo Turbo headers plus `X-Sec-Purpose: prefetch`, strictly admits one successful, nonempty Expo XML document with a validated same-origin final URL, applies snapshotted positive-integer parser limits and cache policy, and stores an independent temporary-pruned snapshot. A same-origin redirect retains the original request URL as the parser base, cache key, and report URL; it never aliases the final URL or changes active-tree/history/visit ownership. The URL is reserved before cache/request-ID/transport callbacks; existing hits allocate no request ID; equivalent in-flight URLs share one request; successful exact or all-request cancellation prevents late caching; and for a cacheable response a newer shared snapshot wins with a distinct `superseded` result. Host metadata is admitted once, request-ID/transport/response/parser failures are redacted, and `no-cache` reports a successful non-retained result without replacing a newer entry. Frame preload, browser-hover prefetch, TTL/request reuse, and device evidence remain unsupported.
- [x] Caller-owned document-preload leases: `DocumentPreloader.retain(source)` returns an immutable `{ promise, commit(), release() }` lease. A direct `preload()` or committed lease makes the shared operation durable; only the final uncommitted lease can abort its still-pending operation before the cache-write boundary. Releasing a lease never evicts a settled snapshot, and `cancel(source)`/`cancelAll()` remain explicit force controls.
- [x] Render-time automatic `data-turbo-preload` for semantic native document-link components: a host injects the shared `DocumentPreloadRequester` through `ExpoTurboProvider.documentPreloader`, and each rendered component using `useExpoTurboDocumentLink` treats marker presence—including `"false"`—as preload intent. After full marker admission, optional `DocumentAutomaticPreloadPolicy` receives the canonical URL; absence allows, literal `false` silently starts no cache/request work, and malformed/throwing/async results fail closed through the optional redacted renderer observer. The renderer rechecks the live node/raw `href`/marker admission before cache work, so policy-side mutation cannot warm the former destination; it does not apply to press-in prefetch or direct preloader use. Initial, whole-document, cached-preview, Frame, and Stream-inserted renders can discover links without guessing semantics from arbitrary XML tags; `template`, Stream, and stream-source subtrees remain inert. Enabled, navigable, root-visitable document destinations participate; fragment-bearing markers strip the fragment before cache reservation and transport, so fragment variants share one canonical request/snapshot. Method/Stream links, opt-outs, unsupported native target/download metadata, and Frame destinations are skipped, while an exact `_top` promotion may preload. Strict Mode and duplicate links reuse the underlying URL reservation; React cleanup never cancels host-owned shared work; stale failures are suppressed, and a still-current typed/redacted failure reaches the optional renderer error observer. This cache-only warming does not itself enable fragment activation, cached fragment previews, history/restoration, anchor lookup, or scrolling; a redirect never aliases its final URL or skips later canonical navigation. Frame response preload, browser-hover prefetch, TTL/request reuse, and physical-device evidence remain pending.
- [x] Explicit touch-native press-in prefetch through `useExpoTurboDocumentLinkPrefetch`: an app-owned semantic link may pass its stable fire-and-forget callback to `Pressable.onPressIn`, its `.cancel()` method to `onPressOut`, and invoke `.commit()` as the first synchronous statement in `onPress`; the standalone example does so. After full live-link admission, an optional `DocumentPrefetchPolicy` receives only the canonical URL; absent policy allows it, while literal `false` silently starts no lifecycle/cache/request work and malformed/throwing/async results fail closed through the optional redacted renderer observer. The policy runs before native lifecycle dispatch and is rechecked immediately before cache work, so policy- or listener-side mutation cannot warm an unobserved destination. An optional controller-owned `DocumentVisitLifecycle` then receives frozen cancellable native `before-prefetch` `{ nodeKey, url }` before the renderer invokes the injected cache-only `DocumentPreloader`; prevention allocates no preload/cache/request ID/request lifecycle work and does not affect a later activation. The renderer rechecks its exact live node, raw `href`, provider configuration, and admitted canonical destination after listeners, failing closed instead of warming a listener-mutated link. This deliberately differs from browser Turbo: native dispatch follows full native admission, has no DOM event target/bubbling, and then rechecks; browser Turbo dispatches first and captures its URL afterward. Native does not read browser document-wide `turbo-prefetch=false` metadata; a host may express global or per-location policy through `DocumentPrefetchPolicy`. Direct `DocumentPreloader` calls remain policy- and lifecycle-silent, while automatic `data-turbo-preload` warming remains press-in-policy- and lifecycle-silent but may use its separate `DocumentAutomaticPreloadPolicy`; an admitted press-in may still use the preloader's request lifecycle but never owns document visit/history/tree work. Repeated presses share one canonical `X-Sec-Purpose: prefetch` GET and a later visit may use the snapshot preview without waiting for prefetch. Only enabled same-origin root-visitable HTTP(S) document links with a different path/query are admitted, including a Frame-contained link only when current native target resolution promotes it to the owning document. An empty or case-insensitive `get` `data-turbo-method` remains prefetchable. Fragments/same-page links, every present browser `target`/download, other method values, Stream, Frame-local destinations, confirmation/UJS-style metadata, unsupported locations, and closest `data-turbo="false"` or `data-turbo-prefetch="false"` opt-outs are no-ops; a nearer non-false setting overrides an ancestor opt-out. Press-out releases on the next microtask so the normal native `onPressOut` then `onPress` sequence commits the same lease; an abandoned gesture releases only its exact uncommitted sole pending lease, without canceling direct, automatic, concurrent, or committed shared work, and cannot evict a settled shared snapshot. It never mutates the active tree/history, delays navigation, or claims browser hover, TTL, response reuse, or physical-device evidence. Current failures are redacted through the renderer observer and stale/unmounted link failures are suppressed.
- [x] Turbo 8.0.23-style cached previews for fragment-free ordinary top-level `advance` and history-backed `replace`: one `getPreview` hit starts the visit once, captures outgoing truth, commits the requested history proposal when configured, installs a provisional tree without autofocus, and still performs one canonical GET under the same lifecycle/progress epoch. `DocumentVisitSnapshot.previewVisible` and `DocumentSession.treeState.preview` stay true until an authoritative whole-document replacement; headless and React subscribers receive the same transitions even when finalization callbacks read state first. A canonical XML tree replaces the preview and owns a fresh autofocus opportunity, while `204`/blank-`201`, cancellation, and typed request failure leave it explicitly provisional. A canonical redirect performs Turbo's follow-up same-index `replace` with a fresh restoration identifier before the final tree. Exact tree-generation and session-wide document-claim checkpoints prevent cache, request-ID, and preview-finalization reentrancy from reclaiming newer work; commit-phase cancellation is ignored, release-to-revalidation cancellation is honored, and reported preview-finalization or host-clock timer failures cannot suppress the canonical GET. `no-preview`, fragments, and cache misses retain the network-only path. Frame previews, browser-hover behavior, TTL/response reuse, and device evidence remain pending.
- [x] Visit-driven outgoing snapshot capture for ordinary internal `advance` and history-backed explicit `replace` commits when the controller is configured with a cache. A preview hit selects its independent target clone first, then captures the latest outgoing tree immediately before requested history and the provisional replacement; canonical revalidation never recaches that preview as outgoing truth. A cache miss retains the guarded final-tree path after redirect admission, immediately before optional history and parsed-tree replacement. Valid committed error documents capture on the miss path; refreshes, empty responses, cancellation, request/parse failures, delegated or discarded redirects, and `no-cache` roots do not fabricate a snapshot. Explicit `restore` and host traversal use their cache-first paths below.
- [x] Dedicated synchronous native history-host boundary plus identity/restoration ledger: managed host state is validated and adopted without a write; unmanaged state receives a generated index-0 entry and must complete the exact injected `replace` before the ledger publishes it. Each public mutation is exclusive from identifier generation through any host write, so injected callbacks cannot reenter and publish competing state. Host failures, returned values, Promises, and thenables are rejected and redacted before ledger publication, leaving unmanaged initialization or the exact proposal retryable. The package cannot roll back external state if a host mutates before violating that contract. Traversal adoption derives Turbo's `forward` versus `back` direction; promoted Frame entries may reuse one restoration identifier across URLs/indexes; different identifiers may share an index; and frozen finite scroll positions remain keyed by identifier. The host contract is synchronous and atomic-on-error; managed traversal subscription and explicit document restoration are covered below.
- [x] Frozen history visit proposals with atomic host/ledger commitment: an ordinary `advance` chooses `push` versus same-location `replace` from the canonical requested URL and captured current entry; explicit replacement retains the index; redirect retargeting preserves that requested-location decision plus the generated identifier/index; and only the exact current-base proposal may commit. Commitment prevalidates and locks every history mutator, writes the exact frozen entry through the dedicated host adapter, then publishes the already-sealed ledger state without another fallible check. Host failure leaves the proposal exact, unbound, and retryable; forged, stale, superseded, reused-identifier, invalid, and overflowing proposals produce no host write.
- [x] Optional aligned history coordination for top-level `advance` and explicit `replace` visits: the controller canonicalizes equivalent active/history URLs and requires them to match before ownership. A preview miss commits snapshot → history → authoritative tree under the final parsed-tree guard; a preview hit commits outgoing snapshot → requested history → provisional tree first, then performs canonical revalidation without recapturing that preview. A same-URL final tree writes no second entry; a redirected final tree follows with a fresh same-index `replace`, matching Turbo's redirected-visit shape. Empty/failing/canceled revalidation retains the already-committed requested entry and explicit preview state because the host boundary cannot roll back a push. Same-path replace morph/scroll behavior is not claimed. Explicit document restoration and host-driven traversal are covered below; Frame-local `advance`/`replace` coordination is recorded below.
- [x] Turbo 8.0.23 current-document refresh history synchronization: with aligned optional history, an admitted refresh prepares a `replace` proposal before request ownership but deliberately captures no outgoing snapshot. A valid bounded successful morph or ordinary `2xx`/`4xx`/`5xx` replacement keeps the current restoration index, publishes a fresh identifier, retargets an admitted same-origin redirect, writes history before its tree mutation, and preserves historyless hosts' ordinary refresh path. The native history model normalizes Turbo's redirected follow-up visit into one atomic final-URL replace write. Stale captured URLs or active visits remain no-ops; empty, canceled, superseded, transport/MIME/parse-failed, discarded, concurrent-history-drifted, and history-host-failed refreshes publish neither the proposed entry nor a tree mutation. Other Refresh morph paths plus route/Frame/nested anchors, nested scroll discovery, visit/history restoration, and page-metadata scroll policy remain unsupported.
- [x] History-backed explicit top-level `restore` visits through `DocumentVisitController` and registered document links: a root-visitable, fragment-free target requires aligned initialized history and creates a fresh restoration identifier, using `push` for a different requested URL and same-index `replace` for the current URL. An unconditional cache hit—including `no-preview`—attempts the policy-governed outgoing snapshot (`no-cache` skips storage), commits history → tree, returns frozen `{ source: "snapshot", status: "restored" | "canceled", url }` evidence, and allocates no request ID or GET. A miss performs one guarded GET, retargets the original proposal to an admitted successful final redirect URL, and uses the ordinary parsed-tree commit path; authoritative redirected or unredirected `4xx`/`5xx` XML remains commit-capable before the lifecycle reports failure. Fragment-bearing requested/final URLs, initially unvisitable targets, successful redirects whose final location is unvisitable, historyless, misaligned, canceled, superseded, empty, transport/MIME/parse/root-policy-failed, snapshot-failed, and host-write-failed work publishes no proposed entry or replacement tree. Frame-local and generated-form `restore` remain fail-closed.
- [x] Host-driven back/forward traversal restoration through `DocumentVisitController.restoreTraversal(entry)`: a same-origin, fragment-free host entry is validated and adopted without another history write, exposes frozen direction/entry/restoration data, and supersedes older traversal work. An unconditional cache hit—including `no-preview`—captures the latest outgoing truth, retargets the fresh clone to the exact traversed URL, replaces the tree as final restoration truth under shared destination ownership, and performs no GET. A miss performs one history-neutral guarded GET and captures outgoing truth immediately before a parsed replacement. Redirects and concurrent history drift fail closed but a later host traversal can recover; lifecycle cancellation can stop cached restoration before replacement; empty `201`/`204` leaves host/ledger truth adopted with the prior tree. Failures never pretend to roll the already-moved host history back. Fragment/anchor traversal restoration and Frame-local/nested scroll remain unsupported; bounded root traversal restoration is recorded next.
- [x] Bounded root history-traversal scroll restoration: a host may retain a finite `{ x, y }` position for an already-bound `DocumentHistory` restoration identifier. On a fragment-free `restoreTraversal(entry)` only, a cache hit or a successful nonredirected network document carries that position to the exact React acknowledgement. After document autofocus and before `load`, the optional synchronous `DocumentHistoryScrollAdapter` calls `canRestore()` and, when true, `restore(position)` once. Missing/unavailable adapters, headless rendering, no saved position, stale/canceled/superseded/unacknowledged work, and network `4xx`/`5xx`/redirect/empty outcomes are no-ops; adapter faults are redacted without rolling back the committed tree. This is root-scoped: promoted Frame entries that share an identifier share its position, and Frame-local scroll has no contract. Package code neither samples nor persists native scroll, and does not apply it for ordinary visits, explicit `visit(..., { action: "restore" })`, anchor traversal, nested containers, page metadata, or physical-scroll completion.
- [x] Managed host traversal subscription through `subscribeDocumentHistoryTraversal`: a host-supplied source emits already-moved managed entries into the shared restoration controller, every newer event invalidates older traversal work before its own admission, valid rapid back/forward entries adopt immediately, and only the newest settlement reaches the explicit result/error callbacks. Unsubscribe invalidates late events/settlements before an exact synchronous cleanup that returns `undefined`, without canceling controller work already in progress. Setup and cleanup contract failures fail synchronously and redacted. The package boundary remains host-neutral; the standalone Expo Router mapping is recorded next, while fragments and generic host scroll capture/persistence remain pending.
- [x] Standalone Expo SDK 57 / Expo Router managed-history bridge in `example/expo`: one app-lifetime runtime/provider sits above the Router Stack, `/` replace-redirects without attaching history, and only the focused `[...expoTurboPath]` route projects the document root. The fixed fixture mapping exposes `https://example.test/demo` at `/demo` and `https://example.test/demo/linked` at `/demo/linked`, and statically exports both paths. A complete bridge-managed history entry may carry an exact canonical fragment-free query URL: its full document URL remains authoritative in opaque, versioned metadata while the catch-all validates the pathname, so query order, duplicates, empty values, `+`, and percent encoding survive managed cold starts, pushes, replacements, and back/forward traversal. The `expoTurboPath` catch-all and three `__expo_turbo_*` fields are reserved; non-reserved external Expo Router query parameters remain host-owned and do not map to document queries. Unsupported paths, encoded separators, and fragments fail closed; malformed, unencoded, noncanonical, or path-mismatched managed metadata is not adopted. One catch-all array plus three namespaced string params carry the canonical path, restoration identifier, index, and opaque document URL metadata. A same-document managed cold start adopts without a GET or Router write. A different-document managed cold start performs one root-visitable, fragment-free exact GET and adopts history with the exact `2xx`/`4xx`/`5xx` XML tree without changing Router state. A different-document unmanaged deep link performs the same guarded GET, then initializes unmanaged history and repairs the focused route inside the guarded tree commit. Redirect flags or changed final URLs, empty responses, wrong MIME, parse/network failure, stale route state, and focus loss commit neither history nor tree and remain retryable after refocus. Advances synchronously push `[...expoTurboPath]` with the next path and metadata; replacements synchronously update both on the focused route. Every rejected/no-op/partial/thrown write resets and verifies the complete captured Router state before remaining retryable; failed rollback makes the bridge terminal and visible. Router state events translate popped/restored managed entries into the shared traversal controller while suppressing bridge-owned write and rollback echoes. The route-owned root `ScrollView` records finite positions while idle and restores them through the same traversal contract; generic host route/query/fragment mapping beyond the two fixture paths, persistent scroll capture, and Frame/route-anchor/nested scroll remain unsupported.
- [x] Static-export hydration readiness for the standalone Expo Router bridge: the focused route waits for the root navigation container's one-shot `ready` event, or the next microtask when it is already ready, before attaching history. Fresh `/demo`, `/demo/linked`, and `/` boots therefore cannot mistake Expo Router's pre-hydration `setParams` no-op for a committed same-key repair. The gallery permanently exercises both a path-changing `advance` and path-changing `replace`; production web export evidence covers direct deep links, reload, stable replacement history length, and back/forward restoration.
- [x] Observable document GET visit lifecycle with `initialized`/`started`/`completed`/`failed`/`canceled` snapshots, post-lease busy accessibility, Turbo's 500 ms delayed visual progress, authoritative error-document classification, host-owned React binding, owner-safe cancellation, and stale timer/request suppression. Blocked or synchronously superseded claims never publish a false `started` state; only an exact successful cancellation publishes `canceled`, while stale/not-owned and protected final-commit cancellation are no-ops. Abort-listener and host-clock cleanup reentrancy cannot clobber newer work; timer setup/cleanup failures are redacted and cannot suppress transport or strand the lifecycle.
- [x] App-owned plain top-level native document links through `useExpoTurboDocumentLink`, with same-origin GET admission and latest-visit ownership delegated to the observable document controller plus fail-closed stale-node, stale-`href`, unsupported-metadata, and out-of-contract fragment activation.
- [x] Bounded same-document native anchor requests through `useExpoTurboDocumentLink`: an enabled exact-live top-level ordinary application link may resolve only a nonempty once-decoded fragment whose canonical origin/path/query exactly matches the active document, and only to an exact live top-level ordinary application-element ID. With `ExpoTurboProvider.documentAnchorScroll`, synchronous `DocumentAnchorScrollAdapter.scrollTo(id, "start")` requests the host scroll and returns frozen `{ kind: "anchor", status: "requested", targetId, url }` evidence; a normal cancellable semantic `click` runs first and the link/target are revalidated after it. It starts no fetch, document/Frame visit, history/router/tree mutation, or non-click visit lifecycle. Busy documents, missing/stale/ineligible sources or targets, Frame context or any `data-turbo-frame`, action/generated-form/Stream/confirmation/UJS metadata, opt-out, browser target/download metadata, missing adapters, and adapter faults fail closed or reject redacted. The request is not a physical-scroll-completion guarantee and supplies no URL/history update, route/cross-document/Frame/nested-anchor behavior, target discovery, or alignment variants.
- [x] Explicit native `mailto:` and `tel:` document-link delegation through the host navigation adapter from top-level, Frame-contained, named-Frame-targeted, and opted-out contexts. URL-parseable non-authority forms normalize before delegation; empty telephone subscribers, fragments, raw C0/DEL controls, credentials, `javascript:`, `data:`, `file:`, `blob:`, `sms:`, and arbitrary schemes fail closed; disabled and unsupported-metadata checks run first; no fetch or request ownership changes.
- [x] Presence-based disabled native links: any authored `disabled` value makes the exact live link inert before URL, Frame, opt-out, fetch, or navigation handling; activation returns a frozen ignored result without disturbing current request ownership, and the Expo example projects authored/pending disabled state onto its `Pressable` accessibility state.
- [x] Host-injected link delegation for credential-free cross-origin HTTP(S) and the closest `data-turbo="false"`, with nested opt-in override, absolute URL delivery, awaited adapter failures, frozen typed results, and no document-controller ownership changes.
- [x] Turbo 8.0.23-compatible confirm-only native link activation: `data-turbo-confirm` alone is inert during activation and follows the same document, Frame, delegated, disabled, and request-supersession paths as a plain link. Functional confirmation is applied only by the generated form-link path below; confirm metadata suppresses press-in prefetch, while browser-hover behavior remains pending.
- [x] Turbo 8.0.23-compatible browser link eligibility: an absent, empty, or exact `_self` `target` follows the current native document/Frame/delegation path. Every other browser target and any present `download` attribute fail closed before URL resolution or request ownership because no native browsing-context or download adapter exists yet; disabled presence still wins first.
- [x] Exact Turbo 8.0.23 link-owned action recognition: only lowercase `advance`, `replace`, and `restore` are actions. Present blank, invalid, differently cased, and whitespace-padded values are non-actions that retain the ordinary document, Frame, or delegated path and explicitly mask destination-Frame inheritance. Browser-bypassed scheme, cross-origin/root-delegated, and opted-out paths ignore action metadata; exact `advance`, history-backed `replace`, and history-backed cache-first `restore` use the owning-document path for top-level promotion, while local Frames use configured `advance`/`replace` history. Historyless document `replace`/`restore` and local Frame `restore` fail closed before request ownership; disabled presence still wins first.
- [x] Exact destination-Frame action inheritance for registered native links and programmatic Frame visits: when the caller supplies no action, the resolved destination Frame contributes only an exact lowercase `advance`, `replace`, or `restore`; invalid Frame values remain ordinary loads, explicit link non-actions mask inheritance, and `_top`/external paths do not inherit. With `FrameHistoryCoordinator` configured, inherited local `advance`/`replace` use the destination controller's stable history scope; local `restore` remains whole-document-only and fails closed.
- [x] Turbo 8.0.23 generated-form links through a host-configured `FormLinkSubmissionController`: raw `data-turbo-method` or `data-turbo-stream` presence enters the shared form submission/ownership/response path only after URL, target, root, scheme, and opt-out admission, before the native semantic-link `click` lifecycle. Ordered decoded query pairs become hidden string entries with duplicates and empty values preserved, empty names omitted, and ASCII-case-insensitive `_charset_` rewritten to `UTF-8`; the query is removed from the action. Blank/invalid methods become GET, exact lowercase `dialog` fails closed, POST/PUT/PATCH/DELETE use their direct HTTP verbs with `_method` left as ordinary data, Stream presence negotiates Stream responses, nonempty confirmation uses the injected adapter, and destination Frame/default/`_self`/`_parent`/`_top` targeting matches the source link while the temporary form has no source-Frame origin or submitter. Exact document or destination-Frame `advance`/`replace` metadata enters the shared form history path; an omitted or invalid explicit action may inherit an exact destination-Frame action, while exact document or Frame `restore` fails before request-ID allocation. Frame actions require the exact destination to be mounted with history coordination. With global `formMode="off"`, an otherwise eligible same-origin generated link instead ignores form/Frame/action/confirmation metadata and delegates its ordinary URL through `NavigationAdapter.visit(url, "advance")`; a missing host adapter still fails closed.
- [x] Turbo 8.0.23 root visitability for top-level `advance`/history-backed `replace`/history-backed `restore` visits and coordinator-wired promoted native visits, using document-scoped `data-turbo-root` (default `/`), the exact path-prefix and case-sensitive extension policy, and pre-ownership host delegation. Interactive links are classified before Frame capture. Successful top-level redirects are reclassified against the response root before commit; an admitted local Frame promotion retains its same-origin primary final URL without a second root check, matching Turbo's Frame path. Direct Frame `src` and ordinary programmatic Frame loads remain root-agnostic. Unlike `advance`/`replace`, an unvisitable explicit `restore` fails closed instead of delegating to host navigation.
- [x] Exact Turbo Frame response extraction with mounted-wrapper preservation, final canonical response `src` tracking, child replacement, embedded Stream execution/consumption, and typed missing-frame failures.
- [x] Turbo 8.0.23-style paired `data-turbo-permanent` retention during ordinary Frame replacement: matching nonblank IDs marked on both current and incoming application-element descendants retain the current object, attributes, complete subtree, React/session/form state, and disposal registrations while moving to the incoming position within that Frame. Unmarked or one-sided same-ID nodes still remount structurally; tag/QName and same-parent shape do not gate a pair. The Frame wrapper and protocol-wrapper descendants are not candidates, an outer retained permanent owns its current nested subtree, and permanent wrapper/ancestor markers never block ordinary replacement. A result that would duplicate an active ID still fails preflight because the native tree remains uniquely addressable.
- [x] Same-origin Frame GET loader with Expo Turbo headers/MIME enforcement, `204` handling, redirected URL ownership, explicit cancellation, and per-frame late-response suppression.
- [x] Bounded native Frame refresh morph for a direct `FrameController.reload()` only: a connected, enabled mounted Frame with a nonblank current `src` and exact lowercase `refresh="morph"` captures the mode before its request. If the first matching response Frame is reached without an intermediary recurse fetch, the mounted Frame wrapper and its existing attributes remain intact except for its canonical final `src`, then its children recursively retain only same-parent ordinary application elements with matching stable ID and exact shape; unkeyed, incompatible, and protocol descendants remount. React/session/form/state identity is preserved only for those retained application elements. Eligible application descendants use the same matched same-parent `data-turbo-permanent` rule: both sides must be marked and exactly compatible, and the current node keeps its attributes and complete subtree. Mounted/response Frame wrappers and permanent ancestors remain ineligible; one-sided, unmatched, reparented, protocol, nested-permanent, duplicate, or out-of-subtree plans fail atomically with no replacement fallback. Initial/eager/lazy loads, `load()`, `setSource()`, same-source `visit()`, link/form/history/recurse paths, nested-Frame reload cascade, Stream Frame targets, and all other document/current-document refresh morph paths remain replacement or unsupported.
- [x] Final-ownership synchronous Frame commit boundary: a matching response is parsed once, extracted, and structurally preflighted against the exact active document before a frozen callback runs under a session-wide guard that blocks reentrant document/Frame claims and makes exact cancellation inert. Callback failure or a returned value/thenable prevents the prepared Frame mutation from beginning; the callback itself must be atomic-on-error and must not mutate the document session or reenter package controllers. Stale ownership, duplicate active-document IDs, and non-Frame outcomes leave the Frame unchanged. Successful mutation preserves the mounted wrapper and unrelated node identities while updating children and the final canonical response `src`, with an internal prepared option for same-tree logical document-URL retargeting. Finalization may start newer work, stale embedded Streams then stop with `interrupted: true`, and `FrameCommitError` distinguishes committed truth from pre-commit failure.
- [x] Optional promoted Frame history through `FrameHistoryCoordinator`: explicit or inherited local `advance` always pushes—including the same canonical URL—while `replace` retains the current restoration index, and every direct visit or Frame-target form promotion by one exact mounted Frame controller reuses its restoration identifier. Direct Frame GET admission optionally captures the outgoing whole document while the Frame still has its prior `src`; the response-driven form path likewise captures only when a shared cache is configured and admits the final `src` before body buffering except for a potentially empty `201`. Matching successful form XML and matching direct-GET `2xx`/`4xx`/`5xx` XML commit against the history entry current at response completion, so independent Frames publish in completion order; a newer admitted whole-document visit or document-targeting form submission invalidates an older promotion before it can write history or children. The final guarded path applies optional snapshot storage → history → Frame children/final `src`/logical document URL. Empty, canceled, MIME/parse/missing-Frame/preflight-failed, stale, snapshot-failed, and host-write-failed work changes neither history, logical document URL, nor Frame children, but retains an already-exposed requested/final `src`. The guard blocks package session/tree mutation APIs during the host write; active protocol nodes, child/attribute collections, and returned Frame/stream-source index arrays remain readonly and frozen while package mutations update protected backing state. When no cache is configured—or an intermediate concurrent snapshot is absent—host traversal falls back to one canonical whole-document GET and never replays a Frame GET. Frame-local `restore`, scroll/anchor restoration, and Frame preview rendering remain pending.
- [x] Turbo 8.0.23 lazy-appearance Frame history promotion: the first actual visibility re-reads the live Frame and exact lowercase `data-turbo-action`; `advance`/`replace` prepare the existing history plan before the loader allocates a request ID or canonicalizes `src`, preserving snapshot-before-source timing and the mounted controller's restoration scope across later explicit visits. Immediate, synchronously reported, and delayed visibility share the same promoted request. Exact `restore`, missing coordination, unvisitable sources, and misaligned history fail once through controller error state before request ownership; invalid, blank, or differently cased values remain ordinary history-neutral lazy loads. Eager loading plus manual `load()`/`reload()` remain history-neutral.
- [x] Frame target resolution for submitter/element/Frame-default/current-Frame precedence, named Frames, `_self`, `_parent`, and `_top`, with missing named targets falling back to the current Frame and `_top`, unavailable `_parent`, or disabled Frame targets promoting to the owning document.
- [x] Identity-bound observable Frame controller with eager `src` loading, explicit lazy/manual loading, same-source reload, renderer-acknowledged terminal busy/complete state for mounted matching GET replacements, and cancellation/rebinding on disable, source removal, same-ID wrapper replacement, or disconnect.
- [x] Opt-in React Frame lifecycle wiring that connects mounted controllers, subscribes to lifecycle state, eagerly loads sources, and cancels/disposes requests when a Frame subtree unmounts.
- [x] Host-rendered Frame GET lifecycle and accessibility binding with a stable boundary, nearest-Frame hook, frozen native busy state, nested scoping, error containment, same-ID replacement rebinding, and no synthetic XML attributes.
- [x] One-shot native autofocus for every ordinary authoritative whole-document tree generation, including the initial `DocumentSession` tree, full replacements, and cached restorations: raw `autofocus` presence contributes stable authored-ID application-element descendants in document order, traversing nested Frames while excluding protocol wrappers and `template`/Stream/source subtrees. Cached preview generations deliberately suppress autofocus; their later canonical replacement owns a fresh opportunity. A bounded successful current-document Refresh Stream morph also creates a fresh authoritative generation, but deliberately suppresses document autofocus. Each pending claim is bound to its exact session, tree generation, document, ordered candidates, and candidate node identities; the renderer consumes it only after descendant handles mount, so Strict Mode and multiple providers cannot duplicate focus. Reinstalling even the same authoritative tree creates a new generation and may focus again, while in-place Frame or standalone Stream mutations never create document autofocus work. A missing adapter consumes the generation as a no-op, unavailable handles are skipped, and synchronous adapter contract failures are redacted without rolling back the committed logical tree.
- [x] One-shot native autofocus after controller-owned matching Frame GET and matching Frame-target form XML responses configured with that exact shared mounted Frame registry: once Frame children and embedded Streams settle, raw `autofocus` presence contributes stable authored-ID application-element descendants from the exact final mounted Frame in document order. Nested Frames are traversed, but Frame and other protocol wrappers plus `template`/Stream/source subtrees are excluded. The internal intent stays bound to its originating session, wrapper, ordered candidates, and candidate node identities, so unrelated Frame mutations do not suppress it while same-ID replacement does; after replacement children mount, an optional synchronous `AutofocusAdapter` skips unavailable native handles and focuses the first available candidate exactly once, including under React Strict Mode. Missing adapters consume the intent as a no-op, missing handles are skipped, and adapter contract failures are redacted through the Frame error surface without rolling back committed children. Direct headless Frame application, standalone Stream/morph mutations, autofocus scrolling, browser focusability parity, and physical-device evidence are not claimed.
- [x] Bounded native standalone Stream-message autofocus for direct fragments, Cable messages, and `text/vnd.turbo-stream.html` form responses: one completed message—when its dispatch path has a response guard, one that still owns it—selects only its first stable authored-ID application candidate with raw `autofocus` in Stream/template order from a default exact-target structural `append`, `prepend`, `before`, `after`, `replace`, or `update`; the four insertion actions retain structural semantics with any `method` metadata. Exact-target `replace`/`update method="morph"` are also admitted only when their whole template payload contains no `data-turbo-permanent`. An earlier still-mounted intent wins over later standalone messages before the next renderer commit; removal or identity-changing replacement discards it and lets a later eligible message claim the next candidate, while an ordinary candidate remains eligible when bounded morph reconciliation retains its exact node identity. Once descendants mount at that or a later session revision, the optional synchronous `AutofocusAdapter` attempts the candidate only when `getFocusedId()` exists and returns `undefined`; existing logical focus, a missing snapshot method or adapter, an unavailable handle, and adapter failures are all one-shot no-ops or redacted errors. Cancellation, supersession, selectors, permanent-bearing or other morphs, custom replacement renderers, no-ops, and document/Frame-embedded Streams create no intent. This does not claim generated IDs, DOM focusability or focus restoration, Turbo Bardo/permanent-node autofocus, scrolling, browser repaint timing, or physical-device evidence.
- [x] Native Frame-boundary autoscroll after controller-owned matching Frame GET and matching Frame-target form XML responses configured with that exact shared mounted Frame registry: raw `autoscroll` presence on the mounted or matching response Frame opts in, including `autoscroll="false"`; only the mounted wrapper supplies `data-autoscroll-block` (`start`, `center`, `end`, or `nearest`, default `end`) and `data-autoscroll-behavior` (`smooth`, otherwise `auto`). After replacement children and embedded Streams mount, an optional synchronous `FrameAutoscrollAdapter` scrolls the registered exact Frame boundary only when `canScroll(frameId)` is true, then Frame autofocus and terminal lifecycle work continue in order. Missing/unavailable boundaries consume a one-shot no-op; invalid adapter results and throws are redacted without rolling back committed children. The standalone Expo gallery registers non-collapsible Frame boundaries plus its root `ScrollView` with cached geometry; smooth scrolling starts there but does not claim physical completion. Headless application, standalone Stream/morph scrolling, arbitrary-node scroll discovery, and physical-device evidence remain pending.
- [x] Injected lazy-Frame visibility lifecycle that observes while mounted, loads on first visibility, stops observing when the request starts, and reports automatic-load failures.
- [x] Concrete standalone Expo visibility adapter for ordinary Frame layout inside the gallery's root and registered nested `ScrollView` regions: unmeasured Frames default hidden; non-collapsible native Frame anchors require positive intersection with the shared window-coordinate intersection of every registered clipping viewport; root/nested layout, scrolling, content-size, and window-dimension changes remeasure; and stale Frame/container callbacks, same-ID container replacement, disposal, and duplicate boolean transitions are suppressed. The gallery includes a nested lazy-Frame scenario plus a horizontal `FlatList` scenario: every direct list row has an explicit stable Frame ID, and loading additionally requires current native viewability membership rebuilt from the full snapshot. Buffered, recycled, stale-key, stale-index, removed, and replaced rows cannot be admitted by geometry alone. This is not a claim for arbitrary virtualizers or physical-device measurement evidence, which remain part of the complete gallery gate.
- [x] Bounded Frame `recurse` extraction with whitespace-token matching, same-origin intermediary GETs, independent request IDs, redirected-base resolution, URL-loop/depth rejection, and canonical source ownership.
- [x] Shared programmatic Frame visit API for current, named, `_self`, `_parent`, and `_top` targets, with credential-free, fragment-free HTTP(S) admission, exact destination-Frame action inheritance, ordinary same-origin Frame loading and same-source reload, optional root-visitable local `advance`/`replace` history coordination, history-backed top-level `restore`, plus awaited owning-document or cross-origin host-navigation delegation whose failures preserve Frame request/source ownership.
- [x] Unless the closest `data-turbo` setting opts out, root-visitable registered native links inside the nearest Frame and top-level links whose non-`_top` target identifies an active enabled Frame enter the shared Frame visit path, including Frame-default/element target precedence, `_self`, `_parent`, `_top`, current-Frame fallback for a missing named target, and isolated Frame/document request ownership. Top-level `_top`, missing, and disabled targets follow ordinary top-level activation; opted-out links delegate through host navigation.
- [x] Route-free Rails Engine, distinct Expo Turbo XML MIME type, confined host-owned `.xml.erb` document rendering, explicit validated `Turbo-Frame` request helpers, and opt-in literal target IDs for persisted records across `record`, `document`, `frame`, `list`, `form`, `error`, and `loading` roles. `expo_turbo_frame_tag` normalizes model classes with Rails' `dom_id`, then delegates the resulting literal Frame IDs to `turbo-rails` without installing its HTML layout concern. A controller must declare its component and style-token capabilities to render a document; when declared, the same policy also admits its Frame, Stream, and raw controller-broadcast output only when it uses those components (including deliberate aliases), exact unprefixed protocol wrappers, and native-equivalent bounded `style-tokens`. Malformed or undeclared output fails before sending bytes or enqueueing a controller broadcast; context-free stream APIs remain structural-only. The same concern provides a cache key that preserves existing `Vary` dimensions, adds `Turbo-Frame`, and separates full-document and valid Frame-ID conditional validators without choosing host cache policy. It exposes `expo_turbo_stream`/`render_expo_turbo_stream`; the builder emits standard sibling `text/vnd.turbo-stream.html` fragments for the eight built-in actions plus selector variants from positional or keyword raw XML, captured template markup, explicit root-confined XML partials, inferred record partials, a root-confined XML layout around a captured block, or XML-format renderables whose limited context can render only root-confined XML partials. It also emits standard public `Turbo::StreamsChannel` sources; callers can send pre-rendered markup, or send and caller-thread-debounce pre-rendered Refresh tags by the exact Expo stream name and request ID, all on a normalized `:expo` stream suffix that keeps Expo payloads separate from browser HTML streams. Queued broadcasts resolve the exact stream name and markup before enqueue; they do not serialize host models or render templates later. Its opt-in `expo_turbo/rails/testing` entrypoint provides strict structural XML document and sibling Stream-fragment parsing for host assertions without eager Nokogiri loading. The document renderer validates one strict structural XML document and rejects blank or duplicate literal IDs across the complete response. The gem does not validate arbitrary component props or provide protected channels; the separate Rails example proves one anonymous Redis-backed public Stream delivery. The gem runs against exact `turbo-rails` 2.0.10/2.0.23 pins.
- [x] Public package-boundary checks scan TypeScript sources, every manifest dependency section, emitted JavaScript/declarations, and source-map source content. They reject browser Turbo, Expo Router, React Query, known private package scopes, app aliases, and source-root escapes without treating public documentation text as an import.
- [x] Independently installed Expo and Rails examples in public CI, including a native component tree rendered from XML in the Expo gallery.

### In progress / not yet supported

- [ ] Physical iOS VoiceOver, Android TalkBack, and browser screen-reader evidence for disabled document links plus form busy state, submitter pending/disabled and submits-with presentation, confirmation, terminal-result announcements, retry, and dismissal.
- [ ] Remaining Turbo link/navigation behavior: generic host route/query/fragment mapping beyond the example's two fixed canonical paths, explicit same-path `replace` morph/scroll behavior, explicit-restore direction/scroll host mapping, cross-document/route/Frame/nested-anchor behavior and history updates, native download/non-current browsing-context adapters, Frame preload/previews, browser-hover prefetch, and TTL/request reuse.
- [ ] Autofocus scrolling, arbitrary-node scroll adapters, arbitrary virtualizer support, and physical-device autofocus/autoscroll evidence.
- [ ] Remaining successful-control semantics: automatic XML/DOM control discovery, browser select defaulting, native File/Blob-valued entries, automatic or inherited `dirname` resolution, and complete eligible-control coverage.
- [ ] A deliberately constrained Rails `text/plain` parameter/method-override profile (the browser format cannot be decoded losslessly), plus native multipart/File/Blob upload transport.
- [ ] Morph-aware `422` native field-state preservation.
- [ ] Remaining lifecycle-event compatibility: broader Frame morph plus per-element/attribute morph events and browser DOM/physical-paint timing. Browser `nextRepaint()` and Stream renderer-flush timing, asynchronous Stream renderer overrides, other current-document refresh morph paths, browser DOM-attribute/repaint timing, and promoted-Frame visual/`before-cache` ordering also remain pending.
- [ ] Remaining native morphing: all Frame morph paths other than the bounded direct reload-morph case, document/current-document refresh morph other than the bounded successful Refresh Stream path above, identity-preserving ID reparenting and richer anonymous matching, global/browser-wide `data-turbo-permanent` semantics beyond paired ordinary Frame replacement (including protocol descendants, cross-Frame/document pairing, and independently reconciled nested permanent subtrees), component-specific input-state/focus/scroll preservation, nested-Frame reload cascade, and per-element morph lifecycle events.
- [ ] Remaining Rails integration: protected Channels and the complete standalone Rails response/scenario matrix.
- [ ] Action Cable credentials/tickets, heartbeat monitoring, AppState/network lifecycle, automatic reconnect/retry policy beyond an explicit server-directed rotation, protected-channel authorization, frame-aware reconciliation, and physical-device evidence. The current injected one-socket adapter is intentionally not a production connection manager.
- [ ] Complete standalone compatibility gallery, differential conformance suite, and physical iOS/Android evidence.
- [ ] Stable npm/RubyGems publication. No registry release is supported yet.

## TypeScript API boundaries

The lifecycle API now includes notification-only owning-document `reload` plus renderer-acknowledged owning-document and Frame `render`/`load` notifications through `expo-turbo/core`.

The root package and explicit `expo-turbo/core`, `expo-turbo/adapters`, and `expo-turbo/registry` subpaths expose the current version, errors, inspector, parser, deterministic diagnostic serializer, typed logical, request, semantic-link click, form-submission, owning-document visit/reload, Frame-missing/render/load, and Stream lifecycle events, state-reference resolution, addressable tree/session/state with subtree disposal, independent document-tree cloning, fragment-free LRU snapshots, root cache-policy metadata, explicit session snapshot capture/manual restore, safe programmatic full-document preload, ordinary cached-preview revalidation with observable provisional tree state, visit-driven outgoing snapshot capture, a host-acknowledged history identity/restoration ledger with atomic staged proposal commitment, optional aligned parsed `advance`/`replace` history coordination, history-backed cache-first explicit document restoration, optional promoted Frame and mounted Frame-target form `advance`/`replace` coordination, host-driven cached-or-network traversal restoration, managed host traversal subscription, exact document-request start acknowledgement and owner-aware cancellation, host-owned document-scoped identity-bound form controls, successful string-entry collection, immutable active-form request planning, atomic destination-aware submission proposals, generated form-link submission, session-shared exact-form submission activity and terminal reporting, safe fresh-request recovery, duplicate/supersede policy, host-injected async form confirmation and exact-terminal announcement adapters, caller-scoped execution with classified unapplied form-response candidates, shared-destination form submission with canonical response application, selectors, structural/custom/refresh Stream dispatch, session-bound `CableStreamSourceRegistry` lifecycle, delivery, and redacted per-source connection snapshots, bounded recent-request tracking, document GET loading with controlled admission plus guarded parsed-tree and cached-tree commit callbacks, root-aware visit and refresh control, Frame target/request/recurse/controller/programmatic-visit APIs, semantic style adapters, typed component/component-action/custom-Stream-action registries, value and presence codecs, and host-adapter contracts. `expo-turbo/react` exposes the provider, root renderer, optional rendered-source Cable leases, provider-injected automatic preload for rendered semantic document links, typed component-action/document/scoped-state/style/disposal hooks, explicit form ownership and live control registration/collection/planning/proposal hooks, observable form activity, terminal result state, a host-defined form boundary, safe explicit retry, an at-most-once host announcement attempt per terminal revision, exact activated-submitter pending/effective-disabled/submits-with presentation with replay-safe final-scope cancellation, document-tree native-link activation with document-load, Frame-visit, generated-form submission, delegated-navigation, click-canceled, or disabled-ignored results, explicit form state boundaries, automatic Frame state inheritance, host-defined document and Frame boundaries, nearest-document/Frame binding hooks, node and lifecycle subscription hooks, and error surface. `expo-turbo/testing` remains a reserved module boundary and intentionally exports no runtime APIs yet. Deep source imports are unsupported.

React hosts keep root-scroll capture and persistence themselves—for example, by writing finite coordinates with `history.updateRestorationData(entry.restorationIdentifier, { scrollPosition })`—and may supply `ExpoTurboProvider.documentHistoryScroll`, the synchronous `DocumentHistoryScrollAdapter`, to consume that data only after a `restoreTraversal` document receives exact React acknowledgement. This requires the controller's live `DocumentVisitLifecycle`. `documentRefreshScroll` remains the separate Refresh Stream top-reset path. `documentAnchorScroll` is a separate, bounded same-document anchor-request hook described above; none of these contracts implements Frame autoscroll, route/cross-document/nested anchors, generic browser history, or physical-scroll completion.

Non-canceled form-response reports expose `responseUrl` as the validated final transport URL. That field does not imply that the active document URL or a Frame `src` adopted it. Applied document and direct Stream reports expose `streams.interrupted`; applied Frame reports expose `frame.streams.interrupted`; empty and canceled reports have no Stream report. Read these variant-specific fields with `applicationDestination` and the application/status fields to determine canonical session state.

The adapter surface is host-neutral. Core source does not import Expo Router, an Action Cable client, an app API client, or private application hooks.

## Status and compatibility

The intended baseline is Turbo 8.0.23, Rails/Action Cable 8.1.3, and `turbo-rails` 2.0.23, with the gem also testing `turbo-rails` 2.0.10 compatibility. Those targets are planning constraints until the public conformance suite proves them.

## Changelog

**2026-07-21**:

- Changed: Added `DocumentReconnectReconciler`, an opt-in wrapper for `DocumentRefreshController` used as `CableStreamSourceRegistry.reconnectRefresh`.
- Why: A server-directed Cable reconnect can complete while an owning document visit is still in flight; dropping that one recovery request unnecessarily leaves the newly settled current document without canonical reconciliation.
- Impact: The wrapper retains only the latest recovery request until the visit sequence next becomes idle, then lets `DocumentRefreshController` perform its existing URL-ownership and debounce checks. A changed document, disposal, stale ownership, or a still-active visit starts no extra GET. Ordinary Stream `refresh`, replay/retry policy, and Frame-local reconciliation remain separate.

- Changed: Added opt-in current-document reconciliation for a confirmed server-directed Action Cable reconnect. `CableStreamSourceRegistry` now sends one frozen scroll-preserving request to its host-injected `reconnectRefresh` requester after the first active `connected(true)` callback in a reconnect cycle.
- Why: A subscriber can miss XML broadcasts while a server-directed socket rotation is in progress; a bounded canonical GET is the safe way to converge an idle current document without inventing replay or retry semantics.
- Impact: Initial connections, terminal disconnects, stale/disposed transports, and ordinary Turbo Stream `refresh` stayed separate. This initial idle-only handoff was later extended by `DocumentReconnectReconciler`; Frame-aware reconciliation remains open work.

- Changed: The injected Action Cable adapter now honors a valid server `disconnect` with `reconnect: true` by rotating its socket and resubscribing its exact active identifiers after the replacement `welcome`.
- Why: Action Cable can explicitly direct clients to reconnect during a server restart; treating that frame like an ordinary terminal close broke the protocol's re-confirmation path.
- Impact: Confirmed records enter `reconnecting` and receive `connected(true)` only after the replacement confirmation; unconfirmed records receive `connected(false)`. Close/error, malformed frames, unsupported protocols, `reconnect: false`, command failures, credentials, timers, lifecycle integration, and automatic retry remain out of scope.

- Changed: Added a separate opt-in native-only Rails Frame-form panel to the Expo gallery. It loads the canonical form, renders authoritative validation XML, and follows the successful redirect back to the canonical Frame URL.
- Why: The prior Rails form evidence was desktop-only and intentionally kept out of the static Action Cable proof.
- Impact: The app now has a bounded interactive Rails form path alongside the static anonymous Cable proof. Renderer and desktop checks cover it, but CORS, device networking, release builds, and physical-device evidence remain open.

- Changed: Added a provider-rendered desktop smoke for the real standalone-Rails Frame form. It mounts `ExpoTurboProvider`/`ExpoTurboRoot`, drives the rendered `TextInput` and submitter through the canonical Rails request, renders the server-owned `422` error, then follows the `303` response and verifies the replacement input resets.
- Why: The existing core smoke proved the request/response controller but not React registration, rendered control activation, or Frame replacement.
- Impact: CI now covers both public form layers against Rails without placing the form in the deliberately static live-Cable panel. It remains desktop-only and is not native-device evidence.

- Changed: Added a desktop CI smoke for a real standalone-Rails Frame form: canonical GET, server-rendered matching `422` XML, and an adapter-followed `303` back to the canonical Frame URL.
- Why: Fixture and core tests did not prove the public form controller against an actual Rails request/response boundary.
- Impact: CI now verifies the URL-encoded request contract and server-authoritative validation/recovery on Bun. This does not wire the static live-Cable gallery to the form or provide native-device evidence.

- Changed: Added a desktop CI smoke from the public Cable registry and WebSocket adapter through the standalone Rails `/cable` endpoint and Redis-backed XML Stream broadcast.
- Why: The previous controlled adapter and Rails Redis checks did not prove their real endpoint-to-client chain together.
- Impact: CI now verifies `welcome`, confirmation, one XML replacement, an exact outbound unsubscribe command, and socket closure on Bun. This is not native-device, authentication, reconnect, or protected-channel evidence.

- Changed: The standalone Rails demo now uses Redis-backed Action Cable in development and tests its public Expo XML Stream broadcast through a real subscriber.
- Why: The existing in-memory adapter could not verify broker delivery or namespace isolation.
- Impact: Rails demo checks now require Redis; CI supplies a local Redis service. This remains anonymous and excludes auth, protected channels, reconnect, and device evidence.

## License

MIT. See [LICENSE](./LICENSE) and [NOTICE.md](./NOTICE.md).

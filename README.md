# expo-turbo

Host-neutral XML protocol runtime foundation for Expo React Native and Rails, targeting Turbo 8.0.23 semantics.

> [!IMPORTANT]
> This repository implements an early XML tree, typed registry, React renderer with opt-in document/Frame lifecycle, and Rails XML foundation. It does not yet implement or claim complete Turbo compatibility, and no package has been published.

## Repository layout

- `src/` — public TypeScript package source.
- `rails/` — public `expo_turbo-rails` gem and non-route-owning Rails Engine.
- `example/expo/` — standalone native consumer and compatibility gallery.
- `example/rails/` — standalone Rails and Action Cable host.
- `protocol/` — shared protocol manifest and conformance fixtures.
- `docs/` — public architecture, compatibility, and release guidance.

The repository root is the publishable TypeScript package. It is intentionally not a package-manager workspace; both example applications keep independent dependency state.

## Development

The TypeScript package requires Bun 1.3.14 or newer:

```sh
bun install
bun run check
```

The Ruby scaffold requires Ruby 3.2 or newer:

```sh
cd rails
bundle install
bundle exec ruby "$(bundle show rake)/exe/rake"
bundle exec ruby "$(bundle show rake)/exe/rake" build
```

Install each example independently, then run both checks from the repository root:

```sh
cd example/expo && bun install --frozen-lockfile
cd ../rails && bundle install
cd ../..
bun run examples:check
```

See [CONTRIBUTING.md](./CONTRIBUTING.md) before proposing changes.

## Support checklist

A checked item is implemented and covered by the public test suite in the current source snapshot. It does not imply complete Turbo compatibility; the package remains pre-release until the full conformance and physical-device gates pass.

### At a glance

- [x] **Package surface:** ESM TypeScript package with explicit `core`, `adapters`, `react`, `registry`, and `testing` boundaries.
- [x] **XML and document tree:** strict namespace-aware document and Turbo Stream-fragment parsing, limits, deterministic XML/tree diagnostics, stable IDs, and structural selectors.
- [x] **Native rendering:** typed component registries, schema/attribute admission, component actions, scoped state, and a React renderer for registered XML components.
- [x] **Native XML direction:** validated `dir="ltr|rtl|auto"` inheritance across the document root, registered components, and Frames, exposed through `useExpoTurboDirection()`. The package stays React-only; the example maps that context to React Native layout/text props without changing global device direction.
- [x] **Visits, links, and Frames:** same-origin document/Frame loading, bounded history/cache/preload behavior, link activation, Frame targeting/lifecycle, and selected native focus/scroll equivalents. The standalone gallery visibly exercises an automatic cached document preview followed by its canonical response, one default Refresh Stream update that returns its owning root `ScrollView` to the top after React acknowledges the replacement, exact same-document and cold cross-document Expo Go root anchors, a fragment-free root-visitable same-path `replace` visit that reads the active root's refresh metadata, and a cached-history root-scroll checkpoint restored after native Back. The same-path fixture uses `data-turbo-refresh-method="morph"`; its absent scroll setting defaults to a post-acknowledgement root reset, while exact `data-turbo-refresh-scroll="preserve"` opts out. Focused runtime coverage proves same-document anchors begin neither a fetch nor a Router-history write, a cold cross-document anchor waits for its canonical document GET and history adoption, the same-path visit skips a cached preview in favor of its canonical GET and retains its compatible stable card, and the checkpoint restores the remounted root container without another document GET. iOS Simulator/Expo Go flows visibly reach both root-anchor targets, commit the same-path morph back at the root, return to the checkpoint, and activate the Frame-scoped link so its replacement field accepts input without a second tap. Android interaction, physical-device, and release-build confirmation remain pending.
- [x] **Provisional Frame preview and canonical revalidation:** a host may opt a `FrameRequestLoader` with a shared `FramePreloadCache` into `preloadBehavior: "preview"`. Activation consumes only an exact Frame-and-fragment-free-URL cached response, commits it as replacement-only provisional content, keeps the controller busy/incomplete with `previewVisible: true`, and starts one canonical GET with a fresh request ID. Preview embedded Streams, autofocus, autoscroll, morph reloads, Frame history, and `frame-load` are suppressed; the acknowledged canonical response owns the normal lifecycle and clears preview state. An empty canonical response leaves the preview visible as terminal fallback. Consume-only one-use preload remains the default. A Maestro-only iPhone 17 Pro Simulator/Expo Go flow visibly captures both the busy cached preview and the canonical response-owned fragment target.
- [x] **Example native Expo Go anchor:** the standalone native host recovers an initial or later exact Expo Go `exp:`/`exps:` URL via `expo-linking` after Expo Router has consumed its hash. An initial `/--/` path and ordered query must match the cold catch-all route before it supplies one decoded, nonblank, control-free target ID. If that route owns a different document, the normal guarded canonical bootstrap GET and history adoption complete before the deferred request waits for layout; an initial same-document anchor has no fetch or Router-history write. A later event remains same-document only: if Expo Router has stripped the existing managed metadata, the host restores that exact entry in place before scrolling. iPhone 17 Pro Simulator/Expo Go runs visibly reach `#native-anchor-target` from a cold link and live event, `#nested-native-anchor-target` from a cold nested link, plus `#linked-native-anchor-target` from a cold `/demo/linked` link. Frame targets, cross-document nested targets, later cross-document events beyond the bounded item below, Android interaction, physical-device, and release-build confirmation remain pending.
- [x] **Live cross-document Expo Go anchors (example-host scope):** the same host handles later exact raw anchor events after Expo Router creates a metadata-free destination route while `/demo/linked` is active. It validates the raw route path and ordered query, stamps that existing native route with a fresh managed entry, transfers the recovered fragment once to the destination owner, then lets ordinary traversal fetch canonical XML before the anchor adapter retries against final layout; it does not issue another Router push. iPhone 17 Pro Simulator/Expo Go evidence covers `/demo/linked` → `/demo#native-anchor-target`, `/demo/linked` → `/demo#nested-native-anchor-target`, and `/demo/linked` → `/demo/routes/ios-proof/details?source=gallery&tag=a&tag=b&empty=#generic-native-anchor-target`, including one native Back step for the generic route. This remains an example-host proof; Frame targets, other host route codecs, Android interaction, physical-device, and release-build confirmation remain pending.
- [x] **Declared nested same-document anchor (example-host scope):** `DemoScrollRegion` explicitly registers its native `ScrollView` as an anchor container, and descendant `DemoAnchorTarget` registrations inherit that ownership. A visible same-document link scrolls only the declared inner container. A cold or later cross-document exact Expo Go fragment additionally measures and reveals that container through the owning root before applying the inner offset; the deferred request waits for both owners. Focused registry/runtime coverage and iPhone 17 Pro Simulator/Expo Go interactions visibly reach `Nested native anchor target`. Frame-owned targets, arbitrary container discovery, Android interaction, physical devices, and release builds remain pending.
- [x] **Nested Frame refresh morph:** a direct outer `FrameController.reload()` with exact `refresh="morph"` retains eligible nested Frame wrappers and current children, ignores stale nested response content, then reloads each eligible child after the outer renderer acknowledgement. The standalone Rails panel has iOS Simulator/Expo Go evidence; Android, physical-device, and release-build confirmation remain pending.
- [x] **Forms:** registered string-valued controls plus bounded host-provided Blob/file entries for `multipart/form-data`, constraint-validation hooks, URL-encoded and bounded `text/plain` planning, submission lifecycle, and authoritative XML/Stream/redirect/empty response handling. The Expo example exposes an opt-in native-only Frame-form panel against the standalone Rails host. Its explicit `Save name and preserve local draft` action returns a standard `422` Turbo Stream `replace target="demo-form" method="morph"`: compatible application IDs retain the native input's component-owned draft while Rails applies the server validation error. The response intentionally carries a blank input `value` so the controlled renderer and real-Rails smoke prove that this is local-state preservation, not a hidden replacement; ordinary `422` XML continues to replace the Frame and own rendered values. An iPhone Simulator/Expo Go run now submits `invalid local draft`, visibly receives `This demo name is unavailable`, and confirms that exact local draft remains mounted. A controlled renderer test and separate desktop provider/root smoke cover initial Frame loading, server-owned `422` XML, a successful `204` that preserves the mounted Frame and edited input, and an adapter-followed canonical `303` GET with replacement-input reset. Its native consent `Switch` uses the same `checkable` control descriptor: an unchecked control contributes no entry, a checked control contributes only `profile[terms]=accepted`, and the Rails host returns an accessible matching `422` XML error for omission or an unexpected value. The adjacent app-owned plan chooser renders two native radio controls through the core `select` descriptor: no selection contributes no `profile[plan]`; a selected plan contributes exactly one value. Rails returns matching `422` XML for omission or an unsupported value and follows a valid selection with the canonical `303` Frame GET. A focused iPhone Simulator/Expo Go run executes consent omission → matching `422` → accepted switch → canonical reset, then plan omission → matching `422` → `Pro` selection → canonical reset, along with the real Rails first-name sequence: URL-encoded `invalid` returns matching `422` XML; `Ada` with `204` keeps the mounted Frame and locally edited input, so the prior `422` error remains; then `Ada` with `303` follows the canonical GET and replaces the Frame with an empty input and no error. The same run also submits the example's exact `text/plain` profile (`invalid` returns matching `422` XML; `Ada` follows `303`) and its fallback `expo-turbo-upload.txt` Blob attachment (successful `303` then canonical Frame GET). The native Files control retains one selected `text/plain` attachment in document state, so the Rails-owned explicit demo retry returns matching `422` XML with an accessible error and leaves that attachment ready for the normal retry. The iPhone Simulator/Expo Go proof downloads a provider-managed 39-byte `text/plain` fixture through Safari into Files, selects it through the system picker, submits its cache-backed Expo `File`, receives that matching `422`, retains the displayed selection, and then follows Rails's `303` canonical GET. Rails receives an `UploadedFile` with `text/plain` and a safe cache-generated `.txt` filename; this does not claim that the provider display filename survives multipart serialization. The multipart adapter creates `FormData` without an author-controlled `Content-Type`; Rails admits only a safe-filename, UTF-8 `text/plain` file from 1–64 KiB and discards its bytes. This plan chooser is not generic XML/DOM select discovery, a native platform picker, or a browser control matrix. Android interaction, release-build, physical-device, and broader host-upload-profile evidence remain pending.
- [x] **Turbo Streams:** built-in and registered custom actions, target/selector dispatch, refresh handling, and identity-safe cleanup.
- [x] **Standalone Rails HTTP Stream proof:** the native-only Rails document exposes a server-authored `DemoDocumentLink data-turbo-stream` that issues a GET with the negotiated Stream/XML `Accept` header and applies the Rails endpoint's ordered `update` and `append` siblings in the active document. Controlled renderer and real-host smoke tests cover the exact path; it is not generic document navigation, an unsafe generated link, authenticated traffic, or physical-device release evidence.
- [x] **Standalone Rails response-outcome proof:** one host-owned scenario endpoint covers canonical `200` XML, empty `201`/`204`, followed `303`, renderable `422`/`500` XML, wrong MIME, correct-MIME malformed XML, bounded delayed-document supersession, and matching/delayed/missing Frames. Rails request coverage verifies the exact status, MIME, redirect, and Frame wires. A real-host native smoke drives the public loaders, commits authoritative documents and Frames, preserves the active tree for empty/wrong-MIME/malformed responses, confirms the older delayed document is canceled by newer ownership, and raises the typed missing-Frame error without corrupting the mounted Frame. This endpoint is evidence infrastructure rather than a product API or a complete gallery.
- [x] **Formatted Rails Stream outer morph:** `replace target="…" method="morph"` ignores indentation-only template text, reconciles one exact compatible application or identified Frame root, and preserves eligible local native state. Retained Frame attributes reconcile through the mounted controller: a changed source cancels stale ownership and follows the new eager/lazy policy, while an ID change transfers registry, request, visibility, and history ownership to a new controller without replacing compatible Frame children. The standalone Rails `?mode=morph` response, focused core/renderer tests, executable conformance fixtures, and an iPhone Simulator/Expo Go run prove the application-root message update while a local counter remains mounted. Android interaction, release-build, physical-device, and visible Frame-root gallery confirmation remain pending.
- [x] **Rails current-document Refresh morph:** a native-only Rails panel loads an ordinary application-root XML document with no protocol descendants. Its first Rails-authored `data-turbo-stream` link receives a standard request-id-free `<turbo-stream action="refresh" method="morph">`, then the injected controller debounces one canonical document GET. Its second link first replaces a visible status node, then echoes that link's originating request ID in a `refresh method="morph"`; the controller recognizes the recent ID and suppresses the duplicate canonical GET. The changing Rails timestamp and retained local counter prove a committed document morph, while the echoed-status path proves bounded self-refresh suppression rather than a no-op. Controlled renderer, real-Rails smoke, and iPhone Simulator/Expo Go evidence cover this bounded path; Android interaction, release-build, and physical-device confirmation remain later work.
- [x] **Action Cable transport, lifecycle, and bounded network recovery:** strict `actioncable-v1-json` codec, a pure same-origin endpoint resolver, and an injected one-socket WebSocket adapter. The adapter preserves opaque identifiers, waits for `welcome` and per-identifier confirmation, retries all active unconfirmed identifiers on one shared 500 ms timer, releases that timer on confirmation, rejection, final unsubscribe, socket detachment, or disposal, routes exact string Stream deliveries, and honors a valid server `disconnect` with `reconnect: true`. An explicit heartbeat uses the host scheduler and time source to make one stale-socket replacement. The public `LifecycleCableAdapter` keeps logical subscriptions stable across injected lifecycle and reachability changes, disposes credential-bearing transports while backgrounded or offline, ignores stale generations, and accepts either a synchronous transport or an asynchronous host credential/transport factory. Concurrent logical sources share one pending factory; a result that arrives after suspension, explicit host credential rotation, or disposal is itself disposed without attachment. `rotateCredentials()` immediately disposes the current generation and obtains a fresh host snapshot while retaining logical subscriptions. The same host-selected finite exponential retry policy covers terminal transport and asynchronous credential-factory failures. Retry timers pause offline/background, a reachable/active edge immediately requests one fresh host transport snapshot, successful confirmation resets the cycle, exhaustion becomes terminal, and subscription rejection never retries. The registry's `reconnectRefresh` handoff drives document- or Frame-scoped canonical reconciliation only after re-confirmation. The Expo example uses AppState plus `expo-network`, retains one runtime, selects a `1s, 2s, 4s, 8s, 16s` five-attempt policy capped at 30 seconds, and covers offline/online recovery, a later terminal socket retry, re-confirmation, and Frame reconciliation. Maestro-only iPhone Simulator flows cover background/foreground and a live Rails-server stop/restart: the UI reports recovery only after socket re-confirmation plus Frame reconciliation, then sends and renders another Rails Stream. No macOS UI automation is used. Credentials, identity-change signaling, retry values, platform-state mapping, browser/device origin policy, and Redis configuration remain host-owned. Android interaction, release-build, authenticated-host, and physical-device evidence remain pending.
- [x] **Example-owned Cable AppState and reachability boundary:** the native standalone-Rails panel maps AppState and Expo Network into the public lifecycle/reachability contracts, retains one document runtime, and recreates only its credential-bearing socket generation. Deterministic renderer coverage proves background/foreground, offline/online, bounded terminal retry, re-confirmation, Frame reconciliation, timer cancellation, and disposal. Retry values and network interpretation are explicit example policy rather than hidden package defaults.
- [x] **Protected Rails Cable boundary:** the gem emits a custom protected-channel descriptor with a gem-specific verifier token and client-visible grant, resolves a host subject only when that channel subscribes, authorizes the decoded Expo stream through host callbacks, and broadcasts on the opaque token rather than the public `:expo` topic. A standard `Turbo::StreamsChannel` cannot verify or receive that token. The standalone Rails/Expo example fetches one short-lived, no-store ticket for each credential-bearing transport generation and sends it only in an `X-Expo-Turbo-Demo-Ticket` native WebSocket header—never in the Cable URL. Its explicit rotation control models a host identity/credential-change signal by disposing the current socket and requesting a fresh ticket without rebuilding the document runtime. The standalone host also versions every issued ticket and exposes one local-only revocation control that invalidates prior generations and targets its own identified Action Cable subject with `disconnect reconnect: false`. Deterministic coverage proves lifecycle and explicit host rotations receive different header snapshots and revoked tickets no longer resolve. Maestro-only iPhone Simulator/Expo Go flows prove initial admission, background/foreground renewal, explicit client rotation, and targeted server revocation; each recovery obtains a fresh ticket, re-confirms the protected source, reconciles its canonical Frame, and receives a later protected XML replacement. Production credential issuance, identity mapping, tenant-aware/distributed expiry and revocation policy, Android interaction, release builds, and physical-device evidence remain host work.
- [x] **Rails foundation:** a route-free Engine, Expo Turbo XML rendering, Frame/Stream helpers, template-capability validation, and tests against `turbo-rails` 2.0.10 and 2.0.23.
- [x] **Cross-runtime contract:** source-only shared XML fixtures exercise the TypeScript package and Rails gem against the same protocol manifest.
- [x] **Machine-readable compatibility ledger:** manifest v2 records 38 current feature-family dispositions as `exact`, `native-equivalent`, `n-a`, or `incomplete`, each with rationale and repository evidence. TypeScript and Ruby CI require unique structured IDs, live evidence paths, and test/spec evidence for every supported disposition; incomplete release work remains visible instead of being inferred away by green fixture tests.
- [x] **Cross-runtime Frame envelope:** one shared Turbo 8.0.23-attributed Frame fixture covers canonical `id`, `src`, `target`, `loading`, `disabled`, `autoscroll`, `refresh`, `recurse`, visit-action, and application-child bytes. The TypeScript parser admits the exact fixture and the Rails helper must emit the same normalized envelope.
- [x] **Cross-runtime built-in Stream envelopes:** one shared Turbo 8.0.23-attributed sibling fragment covers `append`, `prepend`, `replace`, `update`, `remove`, `before`, `after`, and `refresh`, including exact target/selector, morph, request-ID, scroll, and ordered template-root metadata. TypeScript and Ruby normalize the same XML bytes to the same compact assertions, and the Rails TagBuilder must emit that complete shared record.
- [x] **Examples and bundle checks:** independently installed Expo and Rails examples; Expo checks export web, iOS, and Android bundles. Native exports catch bundle drift, not release-build or physical-device behavior.
- [ ] **Still required for a production release:** production host-specific Action Cable credentials/tickets, protected-resource policy and expiry/revocation teardown, authenticated host-native socket integration, Frame-aware physical-device lifecycle/network integration, complete conformance coverage, physical-device/accessibility evidence, and npm/RubyGems publication.

The manual **Release** workflow defaults to a non-publishing candidate run on merged `main`: it builds and clean-installs the exact npm tarball and Ruby gem, records a paired checksum manifest, verifies its GitHub OIDC repository, `main` ref, and `release` environment claims, and signs both artifact digests with GitHub/Sigstore build provenance while preserving the offline bundle. Its separately approval-gated `publish` mode accepts only a successful candidate-run ID for the exact current `main` commit, reuses those downloaded bytes without rebuilding, verifies the frozen checksums and signed provenance, checks the registry-download SHA-256 values, and creates the source tag/GitHub release only after both registry writes succeed. Registry publication is necessarily sequential rather than cross-registry atomic; a failure stops loudly without a source tag or GitHub release. The protected `release` environment requires review and permits deployments only from `main`. The first unscoped npm publication remains a deliberate human 2FA bootstrap because npm trusted publishing can be configured only after the package exists; later stable runs use the configured npm and RubyGems trusted publishers.

### Detailed implemented surface

- [x] Public-source ESM package with explicit `core`, `adapters`, `react`, `registry`, and `testing` exports.
- [x] Host-neutral fetch, form-confirmation, form-announcement, document-visit-announcement, navigation, Cable subscription-interface, strict Action Cable v1 JSON codec, pure `resolveActionCableEndpoint` admission, injected single-lifetime `ActionCableV1WebSocketAdapter`, lifecycle, visibility, focus, bounded same-document `DocumentAnchorScrollAdapter` requests, one-shot `DocumentRefreshScrollAdapter` reset, bounded root-history `DocumentHistoryScrollAdapter` restoration, style, storage, observability, clock, request-ID, and restoration-ID adapter contracts. `resolveActionCableEndpoint` maps one credential-free HTTP(S) origin plus an explicit same-origin path to a `ws:`/`wss:` URL and rejects userinfo, queries, fragments, origins with paths, and authority-changing or normalizing mount paths; it does not select hosts, carry tickets, or create sockets. The Cable adapter uses that host-supplied `ws:`/`wss:` endpoint and exact `actioncable-v1-json` protocol, sends subscriptions only after `welcome`, reports `connected` only after an exact confirmation, shares one server subscription per opaque identifier, retries active unconfirmed groups at Action Cable's 500 ms confirmation-guarantee cadence, and closes when its final local subscription leaves. A valid server `disconnect` with `reconnect: true` rotates live identifiers only after the replacement `welcome` and re-confirmation. Its optional `heartbeat` monitor uses the injected scheduler plus a host time source to observe every valid frame, replace one socket after more than six silent seconds, and avoid retrying a replacement that never receives a valid frame. An optional registry `reconnectRefresh` requester receives one frozen `{ baseUrl, scroll: "preserve", sourceKeys }` request after the first active `connected(true)` in a reconnect cycle; `sourceKeys` are logical source-node keys, never Cable credentials or messages. Initial connections, terminal transitions, and ordinary Stream `refresh` do not invoke it. Socket close/error, malformed input, an unsupported negotiated protocol, `reconnect: false`, or command failure is terminal and reported as a redacted `SubscriptionError`; the adapter owns no credentials, generic connection retry/backoff timing, authentication, URL derivation, or native lifecycle/network binding.
- [x] Host-owned document-visit status announcements: after a React boundary commit, `documentAnnouncements` receives only a frozen `started`, `completed`, `failed`, or `canceled` status. It suppresses initial, stale, same-status progress, and Strict Mode/multiple-provider duplicates; host code owns localized copy, platform delivery, and error presentation. The example maps the contract to iOS `AccessibilityInfo` and persistent web live regions. Physical screen-reader and Android interaction evidence remain pending.
- [x] Bounded Cable recovery through two explicit host opt-ins. `DocumentReconnectReconciler` may wrap `DocumentRefreshController` before injection as `CableStreamSourceRegistry.reconnectRefresh`; if recovery arrives during an owning document visit, it retains only the latest frozen handoff until the visit becomes idle, then delegates to the existing URL-owning/debounced document refresh path. `FrameReconnectReconciler` instead consumes the source-aware handoff with the session, source registry, a non-creating mounted Frame lookup, document refresh requester, and document visit controller. It captures exact source identities, waits for captured sources to leave `reconnecting`, lets an active document-level source win, otherwise reloads only eligible outermost mounted Frames in document order, and revalidates every source, Frame, and controller before each serial reload. Busy Frames and active document visits defer; stale, removed, terminal, disabled, disconnected, unmounted, or source-less scopes drop without a document fallback. Both paths redact deferred/reload failures, do not retry sockets or replay messages, and leave ordinary Stream `refresh` semantics unchanged. The standalone Rails Cable panel wires the Frame-aware path to its sole eager-Frame source and separately admits ordinary refresh Stream dispatch through its document-refresh controller; focused runtime and Rails/Redis smoke coverage verify that this remains distinct from Frame-only reconnect recovery. Physical-device evidence remains pending.
- [x] Typed protocol errors, pinned version baselines, and an injected development inspector.
- [x] Expo example CI bundles web, iOS, and Android exports; the native exports produce Hermes bytecode and catch Metro/runtime dependency drift, but do not substitute for release-build, simulator, or physical-device evidence.
- [x] Strict, namespace-aware XML documents and ordered multi-root Turbo Stream fragment parsing.
- [x] Source-controlled executable Stream behavior fixtures against the Turbo 8.0.23 baseline: the public dispatcher applies shared initial-document and sibling-Stream bytes, then verifies complete ordered action reports, canonical final trees, and selected retained/replaced/removed logical identities for action-error isolation, append collision replacement, exact `target` precedence over `targets`, per-target payload cloning, and every built-in structural action's absent-template result. This is the first behavioral conformance slice, not yet the complete browser differential suite.
- [x] Executable native-equivalent Stream morph conformance for compatible exact outer `replace` (application and identified Frame roots), exact child `update`, and selector child `update`: target and stable-child identities are retained, omitted children are removed, Frame attributes reconcile, selector payloads are independently cloned, and the complete canonical result is manifest-owned. Broader document/Frame morph shapes and full browser differential coverage remain below.
- [x] Pre-parse byte/depth/DOCTYPE/processing-instruction guards plus node, attribute, text, and Stream-action limits.
- [x] Shared text normalization with inherited `xml:space`, explicit `default` reset, preserved CDATA, collapsed XML whitespace, and deterministic fixture/diagnostic serialization.
- [x] Shared XML `dir` admission (`ltr`, `rtl`, or `auto`) with root/component/Frame context inheritance, invalid-value rejection, and the React-only `useExpoTurboDirection()` hook. Hosts map direction to their own native layout and text APIs; this is not browser bidi, CSS cascade, or global device-direction behavior.
- [x] Addressable mixed-content tree with deterministic keys, unique ID lookup, parent links, Frame/source indexes, comments, and CDATA.
- [x] Case-sensitive XML selector queries for lists, tags, IDs, classes, attributes, combinators, and structural pseudo-selectors without stale-result caching.
- [x] Typed component/module registry composition, Zod prop admission, explicit attribute codecs, child policies, aliases/deprecations, duplicate ownership errors, deterministic capabilities, and capability-visible morph-state policy. Components preserve mounted React state by default while they remain under the same rendered parent; `morphState: "reset"` remounts only their retained component boundary after a committed native morph so unsafe native resources can clean up without replacing the logical XML node. Cross-parent moves follow the explicit React ancestry boundary below. When the optional focus snapshot reports that exact node before commit, the renderer restores it after the replacement handle mounts only if focus became empty; focus that moved elsewhere wins. A successful restoration may ask the optional autofocus-scroll adapter to reveal the exact node. Ordinary non-morph mutations do not trigger that reset.
- [x] Typed component-action definitions/modules with Zod params, injected state, provider-backed React invocation, serialized execution, and ordered `onSuccess`/`onError`/`onEnd` lifecycle.
- [x] Document-scoped external state store with stable per-key snapshots, fine-grained subscriptions, no-op suppression, component-action sharing, React bindings, and Strict Mode replay-safe provider-unmount disposal.
- [x] Core identity-bound Frame/form state-scope registry with child-update preservation, replacement/removal cleanup, explicit disposal, and stable-ID reuse isolation.
- [x] React Frame/form state inheritance with automatic Frame boundaries, explicit form boundaries, nearest-scope bindings/actions, update preservation, and replacement cleanup.
- [x] Host-neutral, identity-bound registry for caller-admitted successful entries: enabled named string values, checked checkables, prefiltered ordered multi-values, and explicit host-provided Blob/file pairs owned by the nearest form or an exact-ID `form` association. Explicit ownership overrides ancestry, entries follow whole-document XML order, repeated/empty entries are preserved, the activated submitter follows Turbo 8.0.23's append-last ordering, and exact-node cleanup follows control or form removal/replacement.
- [x] Bounded explicit native multi-name string-entry snapshots: one exact registered control with no outer `name` may contribute at most `MAX_FORM_CONTROL_ENTRIES_PER_CONTROL` (256) ordered `{ name, value }` pairs at that control's whole-document position. Entry names and values are strings and may be empty; pair order and duplicate names/values are preserved, and the hidden-control `_charset_` rewrite does not apply. Exact form ownership/external association, direct and fieldset disabledness, datalist exclusion, optional validity, immutable updates, and exact-node cleanup remain shared with other controls. The host supplies the snapshot; this does not discover XML/DOM controls or infer `dirname`.
- [x] Bounded explicit native multipart entry snapshots: an `entries` descriptor may contribute a frozen `{ blob, filename }` value only for a `multipart/form-data` request. Core limits each Blob to 10 MiB, the logical aggregate to 20 MiB, and filenames to 255 UTF-8 bytes; the immutable semantic body intentionally has no MIME boundary or `Content-Type`, and the host `FetchAdapter` converts it to `FormData` immediately before fetch. This is not DOM control discovery, a native picker, URI-file support, or a generic server upload policy.
- [x] Example-owned selectable text-file adapter: the standalone Expo panel invokes the system Files picker only from a user press, copies one chosen `text/plain` file into the app cache, retains that cache-backed Expo `File` as the existing host-owned Blob/file entry, and leaves the fallback attachment intact on cancellation. It admits a safe filename and a 1–64 KiB file only; the public package remains picker- and URI-agnostic, and individual hosts own their own picker, file lifetime, and upload policy.
- [x] Explicit native select snapshots: a disabled or unnamed select emits nothing; otherwise selected options emit in host-supplied option order only when neither directly disabled nor inside a disabled `optgroup`, preserving repeated and empty values. An explicit value, including `""`, wins; otherwise a host-supplied raw text snapshot is stripped and collapsed using HTML ASCII whitespace. A host may opt one single-choice snapshot into `defaultSelection: "first-enabled"`; only when no option is selected does core emit the first option that is neither directly disabled nor inside a disabled group, matching browser size-one fallback without mutating the host snapshot. Explicit selectedness—including a selected disabled option—suppresses that fallback. Hosts still own current selectedness, order, group state, and XML/native-widget discovery.
- [x] Capability-visible native fieldset semantics for explicitly registered controls: component definitions declare `fieldset` or `legend` roles, core evaluates every live disabled-fieldset ancestor independently, only the first direct semantic legend exempts its descendants, external `form` ownership does not change physical ancestry, inherited-disabled controls are omitted, inherited-disabled submitters are rejected, and React projects the effective disabled/accessibility state. Automatic XML/DOM control discovery remains unsupported.
- [x] Capability-visible native datalist successful-entry semantics for explicitly registered controls: component definitions declare a `datalist` role, physical datalist ancestry omits ordinary entries from every descriptor family regardless of external `form` ownership, an explicitly selected submitter still follows Turbo 8.0.23's manual append-last behavior, and React leaves the control's disabled/accessibility state unchanged. Automatic XML/DOM discovery, suggestion behavior, and child hiding remain unsupported and host-owned.
- [x] Explicit native hidden-control entries: enabled named hidden descriptors emit their caller value or `""` when absent, while only an ASCII-case-insensitive `_charset_` hidden name replaces that value with `UTF-8`. Authored name casing and XML order are preserved; duplicates emit independently; disabled, unnamed, empty-name, fieldset-barred, and datalist-barred controls stay unsuccessful; ordinary value descriptors and the manually appended submitter are not rewritten; automatic XML/DOM hidden-input discovery remains unsupported.
- [x] Value/hidden direction companions: a nonempty explicit host-admitted companion name plus resolved `ltr`/`rtl` is emitted immediately after its owning value, including empty primary values and hidden `_charset_` replacement. Rendered controls may instead use the shared XML `dirname` attribute. The React form-control hook uses the nearest exact `ltr`/`rtl`; for a rendered string-value control with absent or `auto` direction, it applies HTML's Unicode first-strong rule to the current live value. A nonempty value with no strong character is `ltr`; an empty value falls back through inherited exact direction and then the provider's `defaultDirection` (`ltr` when omitted). The Expo host maps that default from React Native `I18nManager.isRTL`. An explicit descriptor always wins. Hidden controls require resolved XML direction, and missing/blank `dirname` emits no companion.
- [x] Turbo 8.0.23's manual activated-submitter append: only a nonempty submitter name plus raw value or `""` is appended last. Image-button `.x`/`.y` coordinates and submitter `dirname` sidecars from the browser's optional-submitter entry-list algorithm are deliberately absent because pinned Turbo constructs `FormData(form)` without that submitter.
- [x] App-installed native form-associated component equivalents through registered components and `useExpoTurboFormControl`, including bounded multi-name string and explicit Blob/file `entries` snapshots. Browser custom elements and `ElementInternals` lifecycle/state callbacks are N/A on React Native; picker/URI-file behavior and broader upload profiles remain host work.
- [x] Explicit React form ownership and live native control registration with `formOwner: true` component declarations, current-value updates, registration-bound submitter selection, nearest-form isolation, exact-ID external controls/bindings/submitters, target insertion/removal/same-ID replacement rebinding, Strict Mode replay safety, and stale-owner proposal rejection. Association changes form transport ownership only; it does not move React/state-scope ancestry.
- [x] Turbo 8.0.23-compatible native form interception modes: document form controls default to `on`, support global `off` and form-ancestor `optin`, classify the form and an activated internal or external submitter against their independent live `data-turbo` ancestry, and expose the synchronous decision through core and React. Explicit planning/submission APIs remain intentional host-owned bypasses.
- [x] Pure immutable form request planning from raw form/submitter metadata and at most `MAX_FORM_REQUEST_ENTRIES` (1,024) successful entries: credential-free same-origin HTTP(S) action resolution; submitter/form action, method, and enctype precedence; GET Stream opt-in and automatic unsafe Stream headers; GET query replacement; URL-encoded and bounded `text/plain` unsafe bodies; bounded semantic `multipart/form-data` bodies; Rails `_method` normalization; submitter-entry consistency; and shared protocol headers. Explicit action fragments fail closed; Blob/file entries are admitted only for multipart.
- [x] Staged one-read form-request input admission: every top-level planner field is read once at its admission stage, and form, submitter, protocol, and entry scalar values are immediately cloned and frozen before any later field can mutate them. Same-origin/credential URL admission happens before entries are read. Throwing property reads, revoked Proxies, entry-array traps, and entry-field failures become package-owned `RequestError`s without retaining host causes or values; the exact `AbortSignal` reference remains intentionally live. Active request-plan, proposal, submit, and retry entry points likewise stage and redact their outer option and protocol reads before composing or dispatching a request.
- [x] Bounded HTML/native-equivalent `text/plain` request serialization for string-only raw-body endpoints: normalized scalar names and values remain unescaped in exact `name=value\r\n` order with duplicates, empty names and values, and literal delimiters. Each emitted pair ends in CRLF, an empty list emits an empty body, and the final UTF-8 representation is limited to `MAX_FORM_TEXT_PLAIN_BODY_BYTES` (1 MiB) during admission. GET remains URL-encoded and bodyless; direct unsafe methods keep `_method` as ordinary data, while Rails-normalized POST works only when the host intentionally consumes the raw body. Rails body-based method overrides stay rejected because Rack does not parse `text/plain`, and the standard explicitly makes the format ambiguous.
- [x] Immutable active-form request planning in core and React from current form/submitter attributes, registration-bound submitter identity, live XML-ordered entries, and caller-supplied protocol metadata/signal; it performs no fetch, destination targeting, pending UI, validation, or response handling.
- [x] Atomic active-form submission proposals in core and React with Turbo 8.0.23 submitter/form/Frame target precedence, distinct inside/outside-Frame blank and missing-target behavior, top-level named-Frame capture, internally derived `Turbo-Frame` metadata, browser-target rejection, and hidden exact form/submitter/destination/tree-generation identity; proposals perform no fetch or request-ownership change.
- [x] Caller-scoped form request execution lanes that admit only planner-issued requests, create the owning abort signal before planning, forward the exact request once, supersede only within that lane, settle cancellation even when an adapter ignores the signal, suppress late fetch/body results, enforce credential-free same-origin final URLs and negotiated MIME, and return frozen unapplied `xml`, `stream`, `empty`, or `canceled` candidates with `2xx`/`4xx`/`5xx` classification. Candidate creation does not resolve a destination or mutate the active tree.
- [x] Destination-aware form request ownership through `FormSubmissionController`: exact proposals are re-admitted before ownership, document and exact-Frame lanes are shared with GET loaders across instances, pre-admission failures cannot cancel an incumbent, shared-destination supersession remains owner-safe, an explicit same-form supersede can change destinations safely, distinct Frames remain concurrent, and the exact request is fetched once.
- [x] Canonical ownership-safe form response disposition through `FormSubmissionController`: the exact lease is retained and rechecked before staged mutations, and reentrant supersession stops and reports undispatched actions; negotiated Streams dispatch for `2xx`/`4xx`/`5xx`; successful matching-Frame, origin-Frame error, or document Expo XML, including authoritative server-rendered `422`, commits without refetch; ordinary `204`/blank-`201` are no-ops while adapter-followed empty Frame redirects update `src`; successful documents adopt the validated final URL, adapter-followed Frame XML updates `src`, and top-level `4xx`/`5xx` preserve the active document URL; unsafe nonredirect document `200`, invalid responses, or stale candidates cannot begin mutation.
- [x] Optional Turbo 8.0.23 form-response snapshot invalidation against a host-shared `DocumentSnapshotCache`: unsafe successful document XML clears before strict parse/application, while authoritative document `4xx`/`5xx` clears after replacement commits, including committed finalization failure; any unsafe successful or failed Frame response clears after exact response admission and before MIME/body/Frame parsing, including unsafe `204`/blank-`201`, wrong MIME, malformed XML, and missing Frames. Safe successful XML, document empty responses, direct Stream responses, denial, cancellation, supersession, and network failure preserve the cache. Hosts must inject the same cache instance used by document visits and Frame history.
- [x] Optional Turbo 8.0.23 form history/action coordination through aligned document history and exact mounted Frame controllers: the activated submitter's first-present `data-turbo-action` masks the form value, and only exact lowercase `advance`, `replace`, or `restore` is recognized. Successful document XML proposes from the validated final response URL; default/`advance` pushes except when that final URL equals the active entry, while `replace` retains the current index. Safe GET application optionally captures the outgoing snapshot when a shared cache is configured, then commits history → tree; unsafe success clears that cache before history → tree. For an exact active Frame destination, explicit or inherited `advance`/`replace` uses that Frame controller's existing `FrameHistoryCoordinator` and stable restoration scope without issuing a Frame GET: the response path optionally stages the latest outgoing whole-document snapshot, admits the final `src` before buffering except for a potentially empty `201`, then commits optional snapshot storage → history → Frame children/logical document URL only for successful matching XML. Direct Frame visits and form promotions therefore share one restoration identifier. Authoritative `4xx`/`5xx`, direct Streams, empty responses, denial, cancellation, supersession, transport/MIME/parse/missing-Frame failure, history drift, snapshot failure, and host-write failure publish no form history entry; Frame `restore` remains whole-document-only and fails closed.
- [x] Session-shared exact-form submission activity in core and React: observable busy state tied to the immutable planner-issued request; atomic rapid-duplicate prevention by default with an explicit same-form `supersede` override; pending/effective-disabled state plus nonempty raw `data-turbo-submits-with` presentation for only the activated submitter; and deterministic cleanup on success, failure, abort, form removal, or final mounted React form-scope/provider unmount before document, Frame, or Stream response application. Strict Mode replay and multiple providers share exact-form ownership; headless callers cancel explicitly. The standalone Expo example exercises this path.
- [x] Host-injected async native form confirmation through `FormSubmissionController`: the immutable proposal captures submitter-first `data-turbo-confirm` by attribute presence, including empty and whitespace values; confirmation receives a cancellation signal and must return a literal boolean; visible busy/submitter state, destination ownership, and fetch begin only after acceptance. Denial, duplicate prevention, explicit cancellation, replacement/removal, final form-scope/provider unmount, adapter failure, and stale or superseded approval cannot send or revive a request. The standalone Expo example supplies a native Alert adapter plus a settling browser-confirm fallback.
- [x] Turbo 8.0.23-compatible logical form submission lifecycle through an optional shared `FormSubmissionLifecycle`: `submit-start` follows resumed `before-fetch-request`, native history/navigation admission, and exact busy/submitter presentation immediately before physical fetch; `submit-end` clears presentation at response admission before body buffering, or at pre-response failure/abort, and precedes XML parsing or document/Frame/Stream application. Both are frozen synchronous notifications with stable listener snapshots and isolated redacted faults. Start exposes one frozen identity-bound handle with safe node/destination/request metadata, live `waiting`/`stopping`/`stopped` state, and exact `stop()` cancellation; end mirrors Turbo's top-level handle-only, response (`success` plus frozen status/redirect metadata), or redacted-error shape. Every emitted start receives one end across response, failure, abort, removal, or supersession, while validation failure, duplicate prevention, confirmation denial, and pre-start request/history cancellation emit neither. Registered forms, retries, and generated-form links share the controller lifecycle; the standalone Expo runtime provides one app-owned instance and a rendered-form smoke test.
- [x] Host-owned synchronous native constraint validation before interactive submit/retry: registered value, checkable, same-name multiple, select, and multi-name entry-list controls may provide a frozen valid or invalid snapshot with host-owned message copy; every eligible invalid control is returned in whole-document order, including unnamed and externally associated controls, while disabled/fieldset-barred, datalist-barred, hidden, and submitter controls are skipped. Form `novalidate` and the exact activated submitter's `formnovalidate` use boolean-presence semantics. An invalid attempt resolves as `status: "invalid"`, focuses only the first invalid logical node through an injected `FocusAdapter`, and starts no confirmation, busy/pending, terminal, ownership, or fetch work. Focus is an exact synchronous host boundary: thrown failures, returned values, Promises, and thenables fail redacted. Pure entry collection, request planning, and proposal creation remain deliberately validation-free, server `422` XML remains authoritative, and browser `invalid` events/default validation UI are N/A. The standalone Expo example uses an app-owned Zod rule plus identity-safe `TextInput` handles and user-driven focus tracking; physical-device and screen-reader evidence remains pending.
- [x] Exact-form terminal submission reporting and host-rendered recovery in core and React: a session-shared frozen snapshot distinguishes applied, empty, canceled, successful-but-unapplied, uncommitted failure, and committed-with-error outcomes only after busy/submitter cleanup; unapplied truth records visit prevention, lifecycle supersession, or awaited outside-root/excluded-extension navigation without pretending a completed mutation was canceled. Exact-attempt settlement prevents stale or superseded work from overwriting newer truth. A provider-registered form boundary can dismiss results or explicitly retry only a safely retryable failure with a fresh request ID; retry reuses the exact live submitter and replans from current registered values, while `retryFailure` refuses unsafe, unapplied, or already-committed outcomes.
- [x] Host-injected exact-form terminal live announcements in React: each new non-`none` redacted terminal revision is offered to the adapter at most once per exact form only after busy and activated-submitter cleanup; initial or cleared state, pre-start denial, stale or superseded settlement, Strict Mode replay, multiple providers, removal, and same-key replacement do not duplicate or leak an announcement. Hosts own localized copy and platform delivery; the standalone Expo example maps the contract to React Native `AccessibilityInfo` and persistent polite/assertive web live regions.
- [x] Bounded typed state-reference decoding with exact `{"$state":"key"}` values, `{{state:key}}` scalar interpolation, recursive arrays/objects, nearest-scope action admission, and typed failures.
- [x] Bounded semantic style adapters with schema-owned `style-tokens`, frozen capability manifests, token/group/component limits, deterministic precedence, explicit React resolution, and an example-owned native corpus.
- [x] Deterministic descendant-first subtree disposal hooks with identity-safe replacement handling, explicit unregister, typed cleanup errors, and once-only registered-component cleanup on logical or React unmount.
- [x] Typed logical event bus with serial dispatch, cancellable notifications, nested pause/resume, stable listener snapshots, unsubscribe, and post-failure queue recovery.
- [x] Native `before-cache` equivalent on the optional `DocumentVisitLifecycle` for ordinary cacheable outgoing-document captures owned by network visits, cached previews, explicit cached restores, cached or network host traversal, successful safe-GET document forms, and promoted Frame history. Direct Frame promotion emits before its request source is exposed; a promoted Frame form emits after successful Expo XML response admission but before the redirected source or body is exposed. The frozen notification-only event runs outside the destination commit guard so synchronous listener mutations are included, then exact ownership, history, and tree checks run again before guarded snapshot/history commitment and replacement-tree application. Listener snapshots are stable and observer failures are isolated and redacted. Initially `no-cache` documents, refresh, preload/manual capture, failed/empty/Stream form responses, unsafe history-neutral document/forms, and history-neutral Frame work emit nothing; browser next-task cloning and renderer-dependent Frame visual timing are not claimed.
- [x] Native `frame-missing` equivalent through an optional shared `FrameLifecycle` for eligible direct, redirected, bounded-recurse, and Frame-form XML responses that omit the expected application Frame. Failed cross-target forms transfer from the destination to the origin Frame before dispatch. The frozen cancellable event exposes only `{ frameId, response: { redirected, status, url }, visit }`; unprevented handling retains the typed `FrameMissingError`, while prevention produces explicit non-applied `prevented` truth. The package's missing-response path performs no tree mutation before the optional visitor. `visit()` records one `advance`/`replace` intent; an optional trusted response visitor receives the already-buffered XML only after the exact Frame/form lease is released, so host-controlled promotion needs no response refetch and ordinary listeners never receive body or headers. Matching Frames, primary `204`, negotiated Turbo Stream responses, MIME/parse failures, loop/depth errors, and work already stale, canceled, or superseded before dispatch emit nothing; listener-side supersession cancels continuation and visitor execution after the event already emitted. Browser DOM bubbling, literal `Content missing` markup, raw `Response` exposure, and browser physical-paint timing are not claimed.
- [x] Turbo 8.0.23-compatible native `before-frame-render` through the optional shared `FrameLifecycle`: after a direct/recurse Frame GET or Frame-targeted form has matched and preflighted its exact response Frame, the frozen logical event exposes `{ frameId, newFrame, renderMethod: "replace" | "morph", url, render }` before the prepared Frame mutation, embedded Streams, autofocus, or renderer acknowledgement. `newFrame` is the parsed, stream-extracted response Frame; exact direct reloads with `refresh="morph"` report `"morph"` by default, while other matching GET/visit and Frame-form responses report `"replace"`. A synchronous listener may call `pause()` during dispatch and later `resume()` the exact response, matching the browser event's prevent-and-`detail.resume()` interception without treating prevention as permanent cancellation. While paused, no promoted history or tree work commits; same-Frame supersession settles the older response promptly without waiting for a host resume, and a resumed response must reacquire and revalidate exact ownership. A stable synchronous wrapper must then select exactly once with `renderDefault()` or `renderMorph()` and return `undefined`; `renderMorph()` opts the ordinary response into the package-owned bounded child morph after a second atomic preflight. The context exposes no session or arbitrary renderer. Empty, Stream-only, missing, promoted, malformed, wrong-MIME, canceled, and superseded responses emit nothing. This deliberately does not claim browser DOM elements, arbitrary native-tree replacement, or physical-paint timing.
- [x] Standalone real-host controlled Frame-morph proof: the Expo gallery exposes direct-reload, ordinary-visit, and explicitly paused ordinary-visit controls against the Rails outer/inner Frame endpoints. Both ordinary visits select `renderMorph()` through `before-frame-render`; the paused path visibly retains the previous outer and nested content with an accessible host resume control before the exact response commits. Resumption retains the stable outer application node and mounted nested Frame, ignores the incoming stale nested payload, and starts the nested canonical reload only after outer renderer acknowledgement. Focused native-renderer and live Rails evidence cover all three paths.
- [x] Maestro-only iOS Simulator interaction for the controlled ordinary Frame morph proof: on the dedicated iPhone 17 Pro / iOS 26.5 simulator, Expo Go opens the live Rails gallery, the flow dismisses initial form autofocus, reacquires the stable long-scroll control, triggers the ordinary `renderMorph()` visit, and visibly observes both `Outer Frame response` and the acknowledged `Inner Frame response`. No AppleScript or macOS UI automation is used.
- [x] Maestro-only iOS Simulator interaction for paused `before-frame-render`: on the same dedicated simulator and live Rails host, the flow starts an ordinary outer Frame visit, observes both the accessible `Outer Frame render paused` state and unchanged loading content, taps the explicit resume control, then visibly observes the outer response and acknowledged nested canonical response. The flow uses Maestro exclusively for UI interaction.
- [x] Turbo 8.0.23-compatible logical `before-frame-morph` through the same `FrameLifecycle`: every selected package-owned Frame morph emits the frozen notification `{ currentFrame, frameId, newFrame, url }` after `before-frame-render` chooses the bounded morph renderer and immediately before prepared child reconciliation. This includes exact direct reload morphs plus ordinary matching Frame GET/visit and Frame-target form XML whose wrapper calls `renderMorph()`. Eligible retained nested Frames emit their own event when an acknowledged outer morph later reloads them. The handles are read-only protocol nodes rather than DOM elements; listener faults are isolated and redacted, and the event cannot cancel or replace the morph.
- [x] Renderer-acknowledged native `frame-render` and `frame-load` notifications through an optional shared `FrameLifecycle`: every controller-owned matching Frame GET prepares an exact mounted-Frame ticket before mutation, whether or not public lifecycle observers exist. After replacement or selected-morph children and embedded Streams settle, React acknowledges only the live Frame at the sealed revision and applies Frame autofocus; the controller then publishes terminal GET state (`busy: false`, `complete: true`) before it dispatches frozen `frame-render` `{ frameId, renderMethod: "replace" | "morph", url }`, rechecks exact ownership, and dispatches frozen `frame-load` `{ frameId, url }`. An outer morph starts its bounded nested reload cascade only after the same acknowledgement and, when configured, those notifications; each child rechecks its exact mounted/connected/enabled/source/permanent/ancestor state before reloading. Frame-target form XML preserves renderer acknowledgement and reports the selected method without making Frame-controller `busy`/`complete` own the submission. Promoted GETs and successful promoted Frame forms emit these exact mounted-Frame notifications before their synthetic owning-document lifecycle, without duplication if later finalization fails. A listener-started reload, Frame replacement, disposal, or supersession suppresses stale notifications. Headless, empty, Stream-only, missing, canceled, superseded, unavailable, and same-ID-replaced Frames emit neither post-render event and do not wait; a failed acknowledgement releases the visual wait without either event. Browser DOM attributes and browser physical-paint/repaint timing are not claimed.
- [x] Turbo 8.0.23-compatible automatic Frame response promotion for an exact sole XML root with `data-turbo-visit-control="reload"`. Eligible direct and bounded-recurse Frame GETs plus Frame-targeted form responses promote the exact buffered `2xx`/`4xx`/`5xx` response through the shared trusted response visitor only after the exact Frame/form lease is released. The marker wins before matching-Frame extraction or embedded Stream application, always requests an owning-document `advance` with reason `visit-control-reload`, and bypasses Frame child mutation, autofocus, Frame history, and `frame-missing`. Failed cross-target forms transfer promotion to their origin Frame; primary direct and form responses retain Turbo-compatible canonical `src` publication, while intermediary recurse responses do not retarget it. Missing or failed visitors fail closed with redacted errors, and stale or superseded ownership cannot promote.
- [x] Optional typed request lifecycle for document loads/preloads, primary and recurse Frame GETs, and form execution/submission: request-local `before-fetch-request` can pause, cancel, or mutate a controlled URL/method/header/body draft, with every mutation re-admitted through transport-specific same-origin, method, body, negotiation, cache-key, and protected-header rules before adapter fetch. `before-fetch-response` can prevent body reading/application with an explicit terminal result. Non-abort transport rejection emits redacted, cancellable `fetch-request-error`; prevention suppresses default visit/Frame/form/refresh/traversal/automatic-preload error delegation while the request still rejects and cleanup still runs. Aborts and late settlements remain cancellation-only, listener failures are redacted, and one paused request never blocks another. An explicitly retried adapter-invoked GET must remain GET after live replanning and lifecycle mutation; failures before adapter invocation retain the existing safe-retry behavior.
- [x] Turbo 8.0.23-compatible semantic-link `click` admission through the exact `DocumentVisitLifecycle` owned by the injected document controller. Every exact-live, enabled, supported, root-visitable plain non-anchor link emits one frozen cancellable `{ nodeKey, url }` event before top-level or Frame ownership; its canonical destination is captured before listener dispatch, so listener-side document URL changes cannot re-resolve a relative link. Prevention returns frozen native no-work evidence and allocates no request/history/navigation owner. Generated-form links, ordinary forms, opt-outs, delegated outside-root/cross-origin/extension URLs, `mailto:`/`tel:`, disabled links, host-delegated target/download metadata, and non-anchor stale activations emit no click. The bounded same-document anchor path is the documented exception: it emits `click` under its narrower admission/revalidation contract without root-policy or visit ownership. React Native has no DOM `MouseEvent`, bubbling path, or automatic browser fallback after prevention.
- [x] Optional synchronous owning-document lifecycle for `DocumentVisitController`, successful document responses from `FormSubmissionController`, and promoted Frames through `FrameHistoryCoordinator`: cancellable `before-visit` runs after canonical proposal admission but before ordinary document history/tree application. Notification-only `visit` runs once after exact ownership and `started` publication for network visits, cached previews, explicit cached restores, current-document refresh, and cached or network host traversal. Host traversal deliberately skips `before-visit`; preview revalidation does not repeat the original event. A successful document form response proposes the final response URL after request and form-activity cleanup, using an explicit action or Turbo's default same-location-redirect `replace`: prevention or listener supersession returns successful `unapplied` truth, while outside-root/excluded-extension final URLs await one required host navigation and emit no `visit`; only root-visitable URLs emit `visit` and proceed to strict parse/history/tree application. Because Turbo proposes a Visit before rendering, a later strict parse failure retains that pair. A successful root-visitable document redirect emits Turbo's second final-URL `visit` with `replace` only after final history/tree commitment, without another `before-visit`, fetch, or controller start; redirected HTTP-error documents, empty, discarded, and stale outcomes emit no follow-up. Exact `advance`/`replace` Frame GET, lazy-appearance, and successful Frame-form promotions instead emit one final-response-URL pair only after history, Frame children, logical document URL, embedded Streams, controller completion or form cleanup, and autofocus intent are logically committed; when the exact mounted Frame is rendered, acknowledgement, visual effects, and `frame-render`/`frame-load` occur first. Late prevention never rolls back that truth; listener supersession suppresses only the stale `visit`; outside-root/excluded-extension final URLs await the coordinator's host navigation and emit no `visit`; matching direct-GET `4xx`/`5xx` Frames may promote, while failed Frame forms do not. With request lifecycle installed, a controller-owned document GET orders `before-visit` → `before-fetch-request` listeners → `visit` → pause/cancellation settlement → physical fetch; a document form response instead finishes its request lifecycle and cleanup before `before-visit` → `visit`. Listener snapshots are stable, async returns fail redacted, notification faults are observer-only with redacted asynchronous surfacing when unhandled, and reentrant newer proposals remain authoritative. Headless promoted Frames retain the logical post-commit lifecycle without inventing visual events.
- [x] Renderer-acknowledged native owning-document `render` and `load` notifications on `DocumentVisitLifecycle`: with a mounted `ExpoTurboRoot`, the originating document controller or form waits for React to acknowledge the exact committed generation at the current revision, after the response-owned revision is sealed. Frozen `render` detail is `{ generation, preview, renderMethod: "replace" | "morph", url }`; a successful bounded current-document morph reports `"morph"`. It fires after the registered React tree or root `renderError` fallback commits and before any applicable document autofocus. Controller-owned cached previews render before canonical fetch and never load; authoritative cached, network, refresh, and traversal documents render, publish terminal `completed` or `failed` state, then emit frozen `load` detail `{ generation, url }`, including committed GET `4xx`/`5xx` documents. Document-targeting forms include embedded response Streams in the revision seal, render successful and authoritative error XML, and load only successful documents after busy/submits-with cleanup and terminal publication. Strict Mode replay, observer-induced revisions, superseded generations, explicit post-commit controller cancellation, and final renderer disposal cannot duplicate or strand work; cancellation preserves installed completed/failed truth while suppressing render/load and response autofocus. Headless work emits neither event and does not wait.
- [x] Turbo 8.0.23 page-level morph completion on `DocumentVisitLifecycle`: every successful controller-owned bounded current-document morph synchronously emits frozen notification-only `morph` detail `{ currentDocument, generation, newDocument, url }` immediately after the logical transaction commits and before renderer acknowledgement. `currentDocument` is the retained active logical document, `newDocument` is the admitted source document, and listeners therefore observe committed tree truth even in a headless host. Replacement/error documents, failed preflight, empty/canceled/superseded work, Frame morphs, and Stream morphs remain silent. Observer faults are isolated and cannot roll back the committed morph; browser DOM event targets, bubbling, and physical-paint timing remain outside the native contract.
- [x] Awaited owning-document `before-render` admission for every controller-owned network GET, cached preview/final snapshot application, and successful document-form XML, including reused prefetch responses, canonical preview revalidation, refresh, explicit restore, and cached or network history restoration. After strict XML or cache-clone admission and successful morph preflight but before snapshot/history mutation, render-ticket creation, embedded Stream dispatch, or logical tree mutation, the optional `DocumentVisitLifecycle` receives stable `before-render` with frozen `{ currentDocument, newDocument, renderMethod, url, render }` detail. A synchronous listener may cancel with `preventDefault()`, pause/resume the exact destination owner, or install an asynchronous wrapper around the one-shot `renderDefault()` continuation. Cancellation and request/form supersession preserve the active document; invalid listeners/renderers fail redacted before commit. Successful form prevention returns explicit `render-prevented` unapplied truth after the already-completed request and visit lifecycle.
- [x] Native notification-only owning-document `reload` on `DocumentVisitLifecycle`: an active `DocumentVisitController` GET whose raw fetch/body transport rejects or whose MIME admission fails dispatches frozen redacted `{ cause: "transport" | "content-type", reason: "request-failed" }` after it publishes `failed` and before ordinary error observers run. Current-document refresh and canonical preview revalidation remain eligible active GETs; request-lifecycle default prevention, cancellation, supersession, explicit restore, host traversal, parse/policy/commit errors, committed XML `4xx`/`5xx`, forms, Frames, and preload emit nothing. It is an observation/recovery hook, not browser hard reload, retry, page invalidation, or DOM behavior.
- [x] Turbo 8.0.23-compatible awaited Stream render admission through an optional shared `StreamLifecycle`: every direct, Frame-response, form-response, embedded, and Cable-delivered action emits cancellable `before-stream-render` before action/target resolution. Prevention consumes only that action, reports `canceled`, and continues later siblings. Registered listeners may asynchronously wrap the one-shot `renderDefault()` continuation or replace it with an explicit native renderer result; the dispatcher awaits each renderer before its `stream-action` notification and before the next sibling. An optional host-owned `streamRenderScheduler.beforeRender()` provides the native equivalent of Turbo's pre-render `nextRepaint()` scheduling point after admission and before the selected renderer, without claiming a browser paint. Rejected or invalid pre-mutation work is an isolated redacted action error; any scheduler, renderer, or asynchronous custom action that mutates and then fails stops dispatch as committed truth. Cable delivery, embedded Frame Streams, negotiated Stream forms, and direct fragments share this serialized path. A successful default-rendered standalone exact-target `replace`/`update method="morph"` also emits frozen native-only `stream-morph` after its tree commit and immediately before `stream-action`. Complete renderer replacements, plain methods, selector-only/missing targets, canceled/no-op/error actions, and document/Frame-embedded Streams emit no `stream-morph`. Logical ordering is supported; physical browser repaint parity is not claimed.
- [x] Static React protocol renderer with stable `useSyncExternalStore` node snapshots, update-preserving component identity, same-ID replacement remounts, isolated subtree updates, registered error surfaces, mixed children, and non-rendering protocol nodes.
- [x] Explicit cross-parent native morph boundary: a compatible stable-ID application node keeps its logical tree object, key, and session/form/external-state ownership when it moves between ordinary parents, but React Native remounts its registered component because the rendered component ancestry changed. Mounted component-local React state is preserved only while reconciliation retains the same React parent (including same-parent keyed reorder and anonymous-wrapper matching). The old component instance unmounts and its `useNodeDisposal` cleanup runs before the new instance owns fresh component resources; the logical node itself remains active. The renderer snapshots optional `getMorphFocusedId()` immediately before every native morph transaction. After descendant layout, it restores the exact stable key when that morph either retained the node with a new morph revision or replaced an incompatible application node under the same ID; ordinary replacement remains ineligible. Focus is restored only when it became empty and the new handle is available, while focus that moved elsewhere during commit wins. A successful restoration may use the existing optional autofocus-scroll adapter to reveal the key.
- [x] Ordered Turbo Stream fragment dispatch for `append`, `prepend`, `replace`, `update`, `remove`, `before`, and `after`, including target/selector precedence, transactional ID-collision preflight, payload cloning, no-ops, and isolated action errors. Exact `update target="…" method="morph"` performs one identity-safe child reconciliation for an ordinary application element or mounted Frame; a Frame retains its wrapper attributes and active controller while its children reconcile; exact `replace target="…" method="morph"` performs the same reconciliation for one matching ordinary application-element root, retaining that outer node while its attributes and children update. The retained outer root may change or remove its `id`; a new ID already owned by an active descendant fails the isolated action during preflight. Selector-addressed `update` and `replace` morphs reconcile each ordinary application target in document order from independent ID-less payload clones; ID-bearing multi-target payloads fail before mutation. An exact-shape stable ID retains its ordinary application element even when the incoming tree moves it to another ordinary parent within the same morph root; newly introduced or incompatible ordinary wrappers are planned recursively so they can receive retained descendants. Same-parent ID-less application wrappers of an exact QName first match by an intersection between their persistent descendant-ID sets, preserving the correct wrapper across reorder; only wrappers with no persistent descendant IDs use deterministic exact-shape fallback. As in Idiomorph, one new compatible anonymous wrapper is inserted instead of consuming the first current wrapper when at least two later incoming siblings can retain at least two current candidates. Incompatible stable-ID elements remount at their incoming parent, and protocol-wrapper descendants remount unless moving them would reuse an active protocol ID, which fails closed. A current ordinary application element, Frame, or Cable stream source carrying `data-turbo-permanent` and a nonblank stable ID is opaque: when one compatible incoming identity exists anywhere in the morph root, the current node moves to that position without changing attributes, descendants, state, controller/subscription ownership, or morph revision; if omitted directly from the root, it remains at its current position. A permanent Frame is also excluded from the refresh-morph reload cascade, while a mounted permanent Cable source keeps its exact live transport through the renderer's lease handoff. Incoming-only markers are admitted normally and become permanent for later morphs, while nested permanent state remains owned by the retained outer permanent subtree. Permanent Stream/template envelopes remain invalid protocol input; marker-bearing supported nodes without stable IDs, incompatible same-ID permanent identities, duplicate/out-of-root IDs, or omission of an unmatched ordinary ancestor that still contains a permanent descendant fail one action atomically. Every other method value retains Turbo's plain structural semantics.
- [x] Bounded current-document morph for exact lowercase `method="morph"` Refresh Streams and same-path replace visits: after the existing admission, request-ID suppression, active-URL/idle-visit checks, and ordinary snapshot-free GET/history lifecycle, a successful XML response may perform one preflighted mutation only when it contains exactly one nonpermanent ordinary application root. A same-QName root retains its identity and reconciles an added, changed, or removed `id`; an incompatible root is structurally replaced while compatible stable descendants retain logical identity across the boundary. A new root ID already owned by an active descendant fails preflight. Exact-shape stable IDs retain ordinary application elements plus their logical session/form/external-state ownership across ordinary-parent moves within the document; mounted component-local React state follows the explicit same-parent boundary above and therefore remounts beneath a replaced root. Same-parent anonymous exact-QName wrappers use persistent descendant-ID intersections before an ordinal fallback limited to wrappers without persistent IDs, while incompatible ordinary wrappers can remount around retained stable descendants. Current stable-ID `data-turbo-permanent` application, Frame, and Cable stream-source descendants remain logically opaque and may move across ordinary parents; directly omitted permanent children stay in place, incoming-only markers are admitted normally, retained permanent Frames keep their current controller/source/children without entering the post-render reload cascade, and mounted permanent Cable sources retain their descriptor and subscription. Matching Turbo 8.0.23 page morphs, an outermost nonpermanent mounted `turbo-frame[src][refresh="morph"]` with an absent or source-compatible incoming peer retains its exact identity and children, then reloads after the committed React render; nested eligible Frames remain owned by that outer reload, while changed-source/plain Frames and nonpermanent Cable sources reconcile structurally. Permanent roots/ancestors, unsupported nonpermanent protocol reparenting, malformed/incompatible permanent identities, duplicate/out-of-subtree IDs, omitted unmatched permanent-bearing ancestors, and every invalid plan fail before history, tree, revision, or disposal changes with no replacement fallback. The successful morph creates a fresh authoritative generation but deliberately performs no document autofocus. Authoritative `4xx`/`5xx` XML always uses ordinary replacement and reports `renderMethod: "replace"`; every other Refresh `method` value uses ordinary replacement.
- [x] Turbo 8.0.23-style paired permanence during ordinary document replacement: an outermost identified application element, Frame, or Cable stream source marked `data-turbo-permanent` in both the current and admitted destination document retains its exact logical node, attributes, complete client-owned subtree, session state, Frame controller, or live Cable transport in the incoming position. Pairing is by stable ID like Turbo's Bardo rather than QName, requires both markers, and applies to network/cache/preview/form/snapshot paths because they share the session replacement boundary. The retired tree receives an independent structural clone, nested pairs remain owned by the retained outer permanent, and any active-ID collision rolls both trees back before generation/revision/disposal changes. React Native component-local state still follows the explicit ancestry boundary: replacing the registered application root remounts a retained application component, while its logical/session ownership remains intact; Frame and Cable leases coalesce that renderer handoff without restarting their runtime owners.
- [x] Bounded Refresh Stream scroll policy for the current document: exact lowercase `scroll="preserve"` consumes a successful refresh without host scrolling. An absent, blank, differently cased, or other value requests a top reset through optional synchronous `DocumentRefreshScrollAdapter` `canReset()`/`reset()` only when its `DocumentVisitController` has a live `DocumentVisitLifecycle` and React acknowledges that exact committed generation. Missing or unavailable adapters consume a no-op; empty, authoritative `4xx`/`5xx`, headless, failed, stale, canceled, or superseded refreshes cannot reset. Adapter faults are redacted without rolling back the committed tree. Route/Frame/nested anchors, nested-container discovery, Frame-local scroll, generic host capture/persistence, page-metadata defaults, and physical-scroll completion remain unsupported.
- [x] Typed custom Stream-action registry with reserved/duplicate ownership checks, Zod-validated `data-*` parameters, shared target/template resolution, synchronous app-installed handlers, and isolated failures.
- [x] Host-injected `<turbo-cable-stream-source>` lifecycle for rendered exact source nodes: required nonblank channels, standard-channel signed-name admission, canonical sorted `data-*` identifier parameters, one refcounted subscription per full identifier within the injected registry, registry-owned serialized string delivery through the common Stream dispatcher, attribute-change/removal/unmount cleanup, late-callback suppression, and redacted subscribe/unsubscribe/message failures. Index membership alone never activates a source, and sources inside `template`, Stream, or source subtrees remain inert.
- [x] Redacted observable Cable subscription state: `CableStreamSourceRegistry.connectionSnapshot` is a frozen, revision-cached list of `{ nodeKey, state }` records, and `subscribeConnection(listener)` notifies semantic snapshot changes for listeners to reread. Adapter callbacks and source admission yield `connecting`, `connected`, `reconnecting`, `disconnected`, or `rejected`; shared canonical transports fan out their current state to each exact source, invalid descriptors become `disconnected`, and stale callbacks or observer failures cannot alter or leak state. Snapshots never expose channels, canonical identifiers, signed names, grants, or message data. This is an observability contract, not a socket client, authentication mechanism, reconnect policy, React/UI binding, or device-transport claim.
- [x] Document GET loader with shared Expo Turbo headers, credential-free same-origin HTTP(S) request/final-URL enforcement, MIME validation, parse-before-replace handling for valid `2xx`/`4xx`/`5xx` XML, explicit `204`/empty-`201`/canceled outcomes, owner-aware cancellation, and generation-safe stale-response suppression.
- [x] Final-ownership synchronous document tree-commit callback: it receives the exact frozen parsed candidate only after the last ordinary ownership check, runs under a session-shared guard that blocks reentrant document/Frame claims and makes exact-owner cancellation non-operative, rejects returned values/thenables, and leaves the tree unchanged on callback failure. The guard clears immediately before tree replacement so finalization listeners can start newer work from the committed tree without outer cleanup detaching it. The optional parsed-visit history coordination below uses this exact linearization point.
- [x] Host-neutral document snapshot foundations: production deep clones preserve exact keys, structure, indexes, locations, and the next safe mutation-key sequence without sharing mutable nodes. The Turbo-style cache defaults to ten entries, keys normalized credential-free HTTP(S) URLs without fragments while retaining query identity, refreshes LRU recency on get and put, returns fresh clones for restoration or eligible preview selection, exposes size/clear, and omits temporary subtrees only from the cached copy, never the active document: `data-turbo-temporary` by presence or the deprecated exact `data-turbo-cache="false"` alias; other `data-turbo-cache` values remain cacheable.
- [x] Explicit document-session snapshot capture and manual restore interop: capture requires the active tree's own absolute URL, cache writes reject missing or fragment-free mismatched tree URLs, misses are frozen no-ops, and hits reuse exact tree replacement so revisions, generations, disposal, fresh node identity, commit-before-finalization errors, and late-response invalidation remain consistent. This low-level primitive is separate from the host-traversal coordinator below.
- [x] Root-owned native cache policy metadata and preview selection: exact lowercase `data-turbo-cache-control="no-cache"` makes a document non-cacheable while leaving the independent previewable flag true, and `"no-preview"` remains cacheable but non-previewable. Absent, blank, invalid, differently cased, whitespace-padded, and nested values use the default cacheable/previewable policy. A skipped `no-cache` write leaves any older matching snapshot untouched. Preview lookup performs the normal LRU read before suppressing `no-preview`, so the retained snapshot remains available for unconditional restoration.
- [x] Programmatic safe full-document marker preload into a host-shared snapshot cache: an explicit fragment-free, root-visitable source performs one non-owning same-origin GET with ordinary Expo Turbo headers plus `X-Sec-Purpose: prefetch`, strictly admits one successful, nonempty Expo XML document with a validated same-origin final URL, applies snapshotted positive-integer parser limits and cache policy, and stores an independent temporary-pruned snapshot. A same-origin redirect retains the original request URL as the parser base, cache key, and report URL; it never aliases the final URL or changes active-tree/history/visit ownership. The URL is reserved before cache/request-ID/transport callbacks; existing hits allocate no request ID; equivalent in-flight URLs share one request; successful exact or all-request cancellation prevents late caching; and for a cacheable response a newer shared snapshot wins with a distinct `superseded` result. Host metadata is admitted once, request-ID/transport/response/parser failures are redacted, and `no-cache` reports a successful non-retained result without replacing a newer entry. Like Turbo 8.0.23's `data-turbo-preload`, these ten-entry LRU snapshots have no TTL; interactive request reuse is the separate press-in path below.
- [x] Exact one-use Frame response preload: `FramePreloader` performs a non-owning same-origin GET with ordinary Frame headers plus `X-Sec-Purpose: prefetch`, requires one successful nonempty Expo XML response containing the exact active destination Frame, and stores immutable response bytes in a ten-entry LRU keyed by both Frame ID and fragment-free requested URL. Cache warming never mutates the live tree, Frame state, history, recent request IDs, or renderer; duplicate cache/in-flight work shares the exact key, cancellation prevents late storage, wrong-MIME/missing-Frame/visit-control responses fail closed, and another Frame cannot consume the entry. `FrameRequestLoader` takes the response once after claiming the exact mounted Frame and applies it through its normal parse, lifecycle, commit, embedded-Stream, autofocus, scroll, and terminal path without another physical fetch; the preload request ID becomes recent only at activation. This is response reuse, not a provisional Frame preview or canonical revalidation.
- [x] Caller-owned document-preload leases: `DocumentPreloader.retain(source)` returns an immutable `{ promise, commit(), release() }` lease. A direct `preload()` makes shared marker-preload work durable. With an injected `DocumentPrefetchCache`, a committed retained lease instead publishes its same pending or settled parsed response to that one-shot cache without polluting the durable snapshot LRU; compatibility hosts without that cache retain the earlier durable-commit behavior. Only the final uncommitted lease can abort its still-pending operation before either commit boundary, and `cancel(source)`/`cancelAll()` remain explicit force controls.
- [x] Render-time automatic `data-turbo-preload` for semantic native document-link components: a host injects shared document and/or Frame preload requesters through `ExpoTurboProvider`. Each rendered component using `useExpoTurboDocumentLink` treats marker presence—including `"false"`—as preload intent. Eligible document destinations retain the policy-governed snapshot path described below. An eligible local or named Frame destination instead strips its fragment, warms the exact Frame-and-URL response cache, and leaves the mounted Frame untouched; activation consumes or previews that response according to the host's `FrameRequestLoader` configuration, with the preview/revalidation proof recorded below. Method/Stream links, opt-outs, unsupported native target/download metadata, missing/disabled Frames, and unsafe destinations are skipped, while exact `_top` promotion remains a document preload. Strict Mode, duplicate links, commits, and unrelated tree revisions do not duplicate an admitted cached Frame response, and current failures reach only the optional redacted renderer observer.
- [x] Explicit touch-native press-in prefetch through `useExpoTurboDocumentLinkPrefetch`: an app-owned semantic link may pass its stable fire-and-forget callback to `Pressable.onPressIn`, its `.cancel()` method to `onPressOut`, and invoke `.commit()` as the first synchronous statement in `onPress`; the standalone example does so. After full live-link admission, optional host policy and cancellable `before-prefetch` lifecycle run before the renderer retains one canonical `X-Sec-Purpose: prefetch` GET. Rechecks after every injected boundary fail closed if the exact live node, raw `href`, provider configuration, policy, or destination changed. With a shared `DocumentPrefetchCache` on the preloader and visit controller, commit publishes that same pending or settled raw response into a one-entry cache with Turbo 8.0.23's 10-second TTL. Activation consumes it once and passes its original request ID, status, MIME type, body, redirect flag, and same-origin final URL through the ordinary document loader without a second physical GET, a second request-lifecycle dispatch, preview state, or a durable marker-preload entry. The lease retains its shared marker-preload Promise identity while exposing a separate activation-facing settlement: a captured renderable response—including `4xx`/`5xx`, wrong MIME, empty, or malformed XML—is not misreported as a press-in transport failure before activation, and the ordinary visit path remains the sole owner of its classification and observer behavior. Successful redirects retarget history and emit the final replace visit; authoritative `4xx`/`5xx` XML follows the ordinary failed-but-rendered document outcome, and the activated request ID enters refresh-loop suppression. Expired, evicted, missing, uncommitted, or root-external final responses follow the ordinary snapshot-preview/network visit path. Only enabled same-origin root-visitable HTTP(S) document links with a different path/query are admitted, including a Frame-contained link only when target resolution promotes it to the owning document. Fragments/same-page links, browser `target`/download, non-GET methods, Stream, Frame-local destinations, confirmation/UJS metadata, unsupported locations, and closest `data-turbo="false"` or `data-turbo-prefetch="false"` opt-outs are no-ops. Press-out releases on the next microtask so native `onPressOut` followed by `onPress` can commit the same lease; an abandoned gesture aborts only its exact final uncommitted pending lease. Browser hover is N/A on touch-native React Native; press-in is the native equivalent. Focused package/example tests and a Maestro-only iPhone 17 Pro Simulator/Expo Go flow prove that one press installs the prefetched authoritative redirected document and never reaches the fixture's canonical fallback; Android interaction, release builds, and physical-device proof remain pending.
- [x] Turbo 8.0.23-style cached previews for fragment-free ordinary top-level `advance` and history-backed `replace`: one `getPreview` hit starts the visit once, captures outgoing truth, commits the requested history proposal when configured, installs a provisional tree without autofocus, and still performs one canonical GET under the same lifecycle/progress epoch. `DocumentVisitSnapshot.previewVisible` and `DocumentSession.treeState.preview` stay true until an authoritative whole-document replacement; headless and React subscribers receive the same transitions even when finalization callbacks read state first. A canonical XML tree replaces the preview and owns a fresh autofocus opportunity, while `204`/blank-`201`, cancellation, and typed request failure leave it explicitly provisional. A canonical redirect performs Turbo's follow-up same-index `replace` with a fresh restoration identifier before the final tree. Exact tree-generation and session-wide document-claim checkpoints prevent cache, request-ID, and preview-finalization reentrancy from reclaiming newer work; commit-phase cancellation is ignored, release-to-revalidation cancellation is honored, and reported preview-finalization or host-clock timer failures cannot suppress the canonical GET. `no-preview`, fragments, and cache misses retain the network-only path. Frame preview proof and interactive press-in response reuse are separate completed rows below; document-preview physical-device evidence remains pending.
- [x] Visit-driven outgoing snapshot capture for ordinary internal `advance` and history-backed explicit `replace` commits when the controller is configured with a cache. A preview hit selects its independent target clone first, then captures the latest outgoing tree immediately before requested history and the provisional replacement; canonical revalidation never recaches that preview as outgoing truth. A cache miss retains the guarded final-tree path after redirect admission, immediately before optional history and parsed-tree replacement. Valid committed error documents capture on the miss path; refreshes, empty responses, cancellation, request/parse failures, delegated or discarded redirects, and `no-cache` roots do not fabricate a snapshot. Explicit `restore` and host traversal use their cache-first paths below.
- [x] Dedicated synchronous native history-host boundary plus identity/restoration ledger: managed host state is validated and adopted without a write; unmanaged state receives a generated index-0 entry and must complete the exact injected `replace` before the ledger publishes it. Each public mutation is exclusive from identifier generation through any host write, so injected callbacks cannot reenter and publish competing state. Host failures, returned values, Promises, and thenables are rejected and redacted before ledger publication, leaving unmanaged initialization or the exact proposal retryable. The package cannot roll back external state if a host mutates before violating that contract. Traversal adoption derives Turbo's `forward` versus `back` direction; promoted Frame entries may reuse one restoration identifier across URLs/indexes; different identifiers may share an index; and frozen finite scroll positions remain keyed by identifier. The host contract is synchronous and atomic-on-error; managed traversal subscription and explicit document restoration are covered below.
- [x] Frozen history visit proposals with atomic host/ledger commitment: an ordinary `advance` chooses `push` versus same-location `replace` from the canonical requested URL and captured current entry; explicit replacement retains the index; redirect retargeting preserves that requested-location decision plus the generated identifier/index; and only the exact current-base proposal may commit. Commitment prevalidates and locks every history mutator, writes the exact frozen entry through the dedicated host adapter, then publishes the already-sealed ledger state without another fallible check. Host failure leaves the proposal exact, unbound, and retryable; forged, stale, superseded, reused-identifier, invalid, and overflowing proposals produce no host write.
- [x] Optional aligned history coordination for top-level `advance` and explicit `replace` visits: the controller canonicalizes equivalent active/history URLs and requires them to match before ownership. A preview miss commits snapshot → history → authoritative tree under the final parsed-tree guard; a preview hit commits outgoing snapshot → requested history → provisional tree first, then performs canonical revalidation without recapturing that preview. A same-URL final tree writes no second entry; a redirected final tree follows with a fresh same-index `replace`, matching Turbo's redirected-visit shape. Empty/failing/canceled revalidation retains the already-committed requested entry and explicit preview state because the host boundary cannot roll back a push. Same-path replace morph/scroll behavior is not claimed. Explicit document restoration and host-driven traversal are covered below; Frame-local `advance`/`replace` coordination is recorded below.
- [x] Turbo 8.0.23 current-document refresh history synchronization: with aligned optional history, an admitted refresh prepares a `replace` proposal before request ownership but deliberately captures no outgoing snapshot. A valid bounded successful morph or ordinary `2xx`/`4xx`/`5xx` replacement keeps the current restoration index, publishes a fresh identifier, retargets an admitted same-origin redirect, writes history before its tree mutation, and preserves historyless hosts' ordinary refresh path. The native history model normalizes Turbo's redirected follow-up visit into one atomic final-URL replace write. Stale captured URLs or active visits remain no-ops; empty, canceled, superseded, transport/MIME/parse-failed, discarded, concurrent-history-drifted, and history-host-failed refreshes publish neither the proposed entry nor a tree mutation. Other Refresh morph paths plus route/Frame/nested anchors, nested scroll discovery, visit/history restoration, and page-metadata scroll policy remain unsupported.
- [x] History-backed explicit top-level `restore` visits through `DocumentVisitController` and registered document links: a root-visitable, fragment-free target requires aligned initialized history and creates a fresh restoration identifier, using `push` for a different requested URL and same-index `replace` for the current URL. An unconditional cache hit—including `no-preview`—attempts the policy-governed outgoing snapshot (`no-cache` skips storage), commits history → tree, returns frozen `{ source: "snapshot", status: "restored" | "canceled", url }` evidence, and allocates no request ID or GET. A miss performs one guarded GET, retargets the original proposal to an admitted successful final redirect URL, and uses the ordinary parsed-tree commit path; authoritative redirected or unredirected `4xx`/`5xx` XML remains commit-capable before the lifecycle reports failure. Fragment-bearing requested/final URLs, initially unvisitable targets, successful redirects whose final location is unvisitable, historyless, misaligned, canceled, superseded, empty, transport/MIME/parse/root-policy-failed, snapshot-failed, and host-write-failed work publishes no proposed entry or replacement tree. Frame-local and generated-form `restore` remain fail-closed.
- [x] Host-driven back/forward traversal restoration through `DocumentVisitController.restoreTraversal(entry)`: a same-origin, fragment-free host entry is validated and adopted without another history write, exposes frozen direction/entry/restoration data, and supersedes older traversal work. An unconditional cache hit—including `no-preview`—captures the latest outgoing truth, retargets the fresh clone to the exact traversed URL, replaces the tree as final restoration truth under shared destination ownership, and performs no GET. A miss performs one history-neutral guarded GET and captures outgoing truth immediately before a parsed replacement. Redirects and concurrent history drift fail closed but a later host traversal can recover; lifecycle cancellation can stop cached restoration before replacement; empty `201`/`204` leaves host/ledger truth adopted with the prior tree. Failures never pretend to roll the already-moved host history back. Fragment/anchor traversal restoration and Frame-local/nested scroll remain unsupported; bounded root traversal restoration is recorded next.
- [x] Bounded root restore scroll plus visit direction: every document visit publishes frozen `back`/`forward`/`none` direction (`advance` defaults forward, `replace` none, and `restore` back). A host may pass a finite `{ x, y }` position to explicit `visit(..., { action: "restore" })`, while host traversal reads the position retained for its bound `DocumentHistory` restoration identifier. A cache hit or successful network document carries that position to the exact React acknowledgement. After document autofocus and before `load`, the optional synchronous `DocumentHistoryScrollAdapter` calls `canRestore()` and, when true, `restore(position)` once. Missing/unavailable adapters, headless rendering, no saved position, stale/canceled/superseded/unacknowledged work, and network `4xx`/`5xx`/redirect/empty outcomes are no-ops; adapter faults are redacted without rolling back the committed tree. This remains root-scoped: promoted Frame entries that share an identifier share its position, and Frame-local scroll has no contract. Package code neither samples nor persists native scroll and does not apply it for ordinary visits, anchor traversal, nested containers, page metadata, or physical-scroll completion.
- [x] Managed host traversal subscription through `subscribeDocumentHistoryTraversal`: a host-supplied source emits already-moved managed entries into the shared restoration controller, every newer event invalidates older traversal work before its own admission, valid rapid back/forward entries adopt immediately, and only the newest settlement reaches the explicit result/error callbacks. Unsubscribe invalidates late events/settlements before an exact synchronous cleanup that returns `undefined`, without canceling controller work already in progress. Setup and cleanup contract failures fail synchronously and redacted. The package boundary remains host-neutral; the standalone Expo Router mapping is recorded next, while fragments and generic host scroll capture/persistence remain pending.
- [x] Standalone Expo SDK 57 / Expo Router managed-history bridge in `example/expo`: one app-lifetime runtime/provider sits above the Router Stack, `/` replace-redirects without attaching history, and only the focused `[...expoTurboPath]` route projects the document root. Its strict example-owned codec maps canonical `https://example.test` paths rooted at `/demo` to one through eight safe catch-all segments (each at most 96 characters), rather than a fixed pathname list. It rejects noncanonical URLs, wrong origins/roots, credentials, fragments, trailing or empty/dot segments, controls, literal or encoded separators, malformed percent encoding, and path/metadata mismatches. Static export intentionally enumerates `/demo`, `/demo/linked`, and the gallery proof `/demo/routes/ios-proof/details`; the last proves native navigation to a generic nested route. A complete bridge-managed history entry may carry an exact canonical fragment-free query URL: its full document URL remains authoritative in opaque, versioned metadata while the catch-all validates the pathname, so query order, duplicates, empty values, `+`, and percent encoding survive managed cold starts, pushes, replacements, and back/forward traversal. The `expoTurboPath` catch-all and three `__expo_turbo_*` fields are reserved; non-reserved external Expo Router query parameters remain host-owned and do not map to document queries. One catch-all array plus three namespaced string params carry the canonical path, restoration identifier, index, and opaque document URL metadata. A same-document managed cold start adopts without a GET or Router write. A different-document managed cold start performs one root-visitable, fragment-free exact GET and adopts history with the exact `2xx`/`4xx`/`5xx` XML tree without changing Router state. A different-document unmanaged deep link performs the same guarded GET, then initializes unmanaged history and repairs the focused route inside the guarded tree commit. Redirect flags or changed final URLs, empty responses, wrong MIME, parse/network failure, stale route state, and focus loss commit neither history nor tree and remain retryable after refocus. Advances synchronously push `[...expoTurboPath]` with the next path and metadata; replacements synchronously update both on the focused route. Every rejected/no-op/partial/thrown write resets and verifies the complete captured Router state before remaining retryable; failed rollback makes the bridge terminal and visible. Router state events translate popped/restored managed entries into the shared traversal controller while suppressing bridge-owned write and rollback echoes. The route-owned root `ScrollView` records finite positions while idle and restores them through the same traversal contract; direct-deep-link query/fragment mapping, persistent scroll capture, and Frame/route-anchor/nested scroll remain unsupported.
- [x] Bounded unmanaged Expo Router direct-query mapping (superseding the older direct-query limitation above): a direct `route.path` must be rooted, fragment-free, and match the validated catch-all path. It supplies ordered query names and duplicates, while the non-reserved Router params must agree on names and values; the bridge then uses the Router-canonical query URL for the guarded initial GET and same-key metadata repair. This preserves the semantic ordered/repeated/empty query state the Router exposes, while deliberately allowing the router to normalize original query encoding. Direct-link fragments remain unsupported: current iOS Expo Go probes show the route state omits an incoming URL hash before this bridge runs.
- [x] Static-export hydration readiness for the standalone Expo Router bridge: the focused route waits for the root navigation container's one-shot `ready` event, or the next microtask when it is already ready, before attaching history. Fresh `/demo`, `/demo/linked`, and `/` boots therefore cannot mistake Expo Router's pre-hydration `setParams` no-op for a committed same-key repair. The gallery permanently exercises both a path-changing `advance` and path-changing `replace`; production web export evidence covers direct deep links, reload, stable replacement history length, and back/forward restoration.
- [x] Observable document GET visit lifecycle with `initialized`/`started`/`completed`/`failed`/`canceled` snapshots, post-lease busy accessibility, Turbo's 500 ms delayed visual progress, authoritative error-document classification, host-owned React binding, owner-safe cancellation, and stale timer/request suppression. Blocked or synchronously superseded claims never publish a false `started` state; only an exact successful cancellation publishes `canceled`, while stale/not-owned and protected final-commit cancellation are no-ops. Abort-listener and host-clock cleanup reentrancy cannot clobber newer work; timer setup/cleanup failures are redacted and cannot suppress transport or strand the lifecycle.
- [x] App-owned plain top-level native document links through `useExpoTurboDocumentLink`, with same-origin GET admission and latest-visit ownership delegated to the observable document controller plus fail-closed stale-node, stale-`href`, unsupported-metadata, and out-of-contract fragment activation.
- [x] Bounded same-document native anchor requests through `useExpoTurboDocumentLink`: an enabled exact-live top-level ordinary application link may resolve only a nonempty once-decoded fragment whose canonical origin/path/query exactly matches the active document, and only to an exact live top-level ordinary application-element ID. With `ExpoTurboProvider.documentAnchorScroll`, synchronous `DocumentAnchorScrollAdapter.scrollTo(id, "start")` requests the host scroll and returns frozen `{ kind: "anchor", status: "requested", targetId, url }` evidence; a normal cancellable semantic `click` runs first and the link/target are revalidated after it. It starts no fetch, document/Frame visit, history/router/tree mutation, or non-click visit lifecycle. The standalone gallery's example-owned `DemoAnchorTarget` registers either with the root or one explicitly declared nested `DemoScrollRegion`; visible nested links scroll only their inner container, while a deferred cold nested target first reveals its declared container through the root. Focused adapter/runtime coverage proves these requests have no fetch or Router-history write, and iOS Simulator/Expo Go flows visibly reach both target classes. Busy documents, missing/stale/ineligible sources or targets, Frame context or any `data-turbo-frame`, action/generated-form/Stream/confirmation/UJS metadata, opt-out, browser target/download metadata, missing adapters, and adapter faults fail closed or reject redacted. The request is not a physical-scroll-completion guarantee and supplies no URL/history update, cross-document nested/Frame behavior, arbitrary nested-container discovery, or alignment variants.
- [x] Explicit native `mailto:` and `tel:` document-link delegation through the host navigation adapter from top-level, Frame-contained, named-Frame-targeted, and opted-out contexts. URL-parseable non-authority forms normalize before delegation; empty telephone subscribers, fragments, raw C0/DEL controls, credentials, `javascript:`, `data:`, `file:`, `blob:`, `sms:`, and arbitrary schemes fail closed; disabled and unsupported-metadata checks run first; no fetch or request ownership changes.
- [x] Presence-based disabled native links: any authored `disabled` value makes the exact live link inert before URL, Frame, opt-out, fetch, or navigation handling; activation returns a frozen ignored result without disturbing current request ownership, and the Expo example projects authored/pending disabled state onto its `Pressable` accessibility state.
- [x] Host-injected link delegation for credential-free cross-origin HTTP(S) and the closest `data-turbo="false"`, with nested opt-in override, absolute URL delivery, awaited adapter failures, frozen typed results, and no document-controller ownership changes.
- [x] Turbo 8.0.23-compatible confirm-only native link activation: `data-turbo-confirm` alone is inert during activation and follows the same document, Frame, delegated, disabled, and request-supersession paths as a plain link. Functional confirmation is applied only by the generated form-link path below; confirm metadata suppresses press-in prefetch, while browser-hover behavior remains pending.
- [x] Turbo 8.0.23-compatible browser link eligibility with explicit native delegation: an absent, empty, or exact `_self` `target` follows the current document/Frame/delegation path. Every other browser target and any present `download` attribute bypass Turbo click/visit/request ownership and resolve through the optional host-owned `DocumentLinkAdapter`; download wins when both attributes are present, preserves a nonempty suggested filename, and both calls receive frozen canonical requests. Without the adapter they fail closed, and disabled presence still wins before either path.
- [x] Exact Turbo 8.0.23 link-owned action recognition: only lowercase `advance`, `replace`, and `restore` are actions. Present blank, invalid, differently cased, and whitespace-padded values are non-actions that retain the ordinary document, Frame, or delegated path and explicitly mask destination-Frame inheritance. Browser-bypassed scheme, cross-origin/root-delegated, and opted-out paths ignore action metadata; exact `advance`, history-backed `replace`, and history-backed cache-first `restore` use the owning-document path for top-level promotion, while local Frames use configured `advance`/`replace` history. Historyless document `replace`/`restore` and local Frame `restore` fail closed before request ownership; disabled presence still wins first.
- [x] Exact destination-Frame action inheritance for registered native links and programmatic Frame visits: when the caller supplies no action, the resolved destination Frame contributes only an exact lowercase `advance`, `replace`, or `restore`; invalid Frame values remain ordinary loads, explicit link non-actions mask inheritance, and `_top`/external paths do not inherit. With `FrameHistoryCoordinator` configured, inherited local `advance`/`replace` use the destination controller's stable history scope; local `restore` remains whole-document-only and fails closed.
- [x] Turbo 8.0.23 generated-form links through a host-configured `FormLinkSubmissionController`: raw `data-turbo-method` or `data-turbo-stream` presence enters the shared form submission/ownership/response path only after URL, target, root, scheme, and opt-out admission, before the native semantic-link `click` lifecycle. Ordered decoded query pairs become hidden string entries with duplicates and empty values preserved, empty names omitted, and ASCII-case-insensitive `_charset_` rewritten to `UTF-8`; the query is removed from the action. Blank/invalid methods become GET, exact lowercase `dialog` fails closed, POST/PUT/PATCH/DELETE use their direct HTTP verbs with `_method` left as ordinary data, Stream presence negotiates Stream responses, nonempty confirmation uses the injected adapter, and destination Frame/default/`_self`/`_parent`/`_top` targeting matches the source link while the temporary form has no source-Frame origin or submitter. Exact document or destination-Frame `advance`/`replace` metadata enters the shared form history path; an omitted or invalid explicit action may inherit an exact destination-Frame action, while exact document or Frame `restore` fails before request-ID allocation. Frame actions require the exact destination to be mounted with history coordination. With global `formMode="off"`, an otherwise eligible same-origin generated link instead ignores form/Frame/action/confirmation metadata and delegates its ordinary URL through `NavigationAdapter.visit(url, "advance")`; a missing host adapter still fails closed.
- [x] Turbo 8.0.23 root visitability for top-level `advance`/history-backed `replace`/history-backed `restore` visits and coordinator-wired promoted native visits, using document-scoped `data-turbo-root` (default `/`), the exact path-prefix and case-sensitive extension policy, and pre-ownership host delegation. Interactive links are classified before Frame capture. Successful top-level redirects are reclassified against the response root before commit; an admitted local Frame promotion retains its same-origin primary final URL without a second root check, matching Turbo's Frame path. Direct Frame `src` and ordinary programmatic Frame loads remain root-agnostic. Unlike `advance`/`replace`, an unvisitable explicit `restore` fails closed instead of delegating to host navigation.
- [x] Exact Turbo Frame response extraction with mounted-wrapper preservation, final canonical response `src` tracking, child replacement, embedded Stream execution/consumption, and typed missing-frame failures.
- [x] Turbo 8.0.23-style paired `data-turbo-permanent` retention during ordinary Frame replacement: matching nonblank IDs marked on both current and incoming application-element, nested Frame, or Cable stream-source descendants retain the current logical object, attributes, complete subtree, session/form/external state, Frame controller, or live Cable transport while moving to the incoming position within that Frame. Mounted component-local React state and component-owned disposal follow the same-parent ancestry boundary above; retained Frame/Cable leases coalesce renderer handoff without restarting runtime ownership. Unmarked or one-sided same-ID nodes still remount structurally; tag/QName and same-parent shape do not gate a pair. The mounted response Frame wrapper itself is not a candidate, an outer retained permanent owns its current nested subtree, and permanent wrapper/ancestor markers never block ordinary replacement. A result that would duplicate an active ID still fails preflight because the native tree remains uniquely addressable.
- [x] Same-origin Frame GET loader with Expo Turbo headers/MIME enforcement, `204` handling, redirected URL ownership, explicit cancellation, and per-frame late-response suppression.
- [x] Bounded native Frame morph reconciliation: a connected, enabled mounted Frame with a nonblank current `src` and exact lowercase `refresh="morph"` captures morph mode before a direct `FrameController.reload()` request; ordinary matching Frame GET/visit and Frame-form XML may select the same package-owned reconciler only through controlled `before-frame-render` `renderMorph()`. If the first matching response Frame is reached without an intermediary recurse fetch, the mounted Frame wrapper and its existing attributes remain intact except for its canonical final `src`, then its children recursively retain exact-shape stable-ID ordinary application elements across ordinary-parent moves within that Frame; incompatible ordinary wrappers may remount around retained stable descendants, same-parent anonymous exact-QName wrappers use persistent descendant-ID intersections before the state-free ordinal fallback, and protocol descendants remount unless a move would reuse an active protocol ID. Logical session/form/external-state ownership is preserved for retained application elements; mounted component-local React state follows the same-parent ancestry boundary above. Stable-ID permanent application, nested Frame, and Cable stream-source descendants remain logically opaque and may move anywhere inside the outer Frame even when the incoming marker is absent; directly omitted permanents stay in place, incoming-only markers are admitted normally, and an outer permanent owns nested permanent state. A retained permanent Frame keeps its mounted wrapper, controller, attributes, source, and current children and never joins the acknowledged nested reload cascade; a retained permanent Cable source keeps its current descriptor and live subscription. Other eligible nested Frames stay opaque during the outer transaction: their mounted wrappers, attributes, and current children remain while incoming nested content is ignored. The incoming nested Frame may be absent, or have the same ID with an absent, blank, or URL-equivalent `src`. After embedded Streams settle and React acknowledges the outer Frame, the controller finishes its render/load lifecycle, rechecks the exact mounted connected enabled child, its nonblank source, no permanent ancestor, and closest reloadable morph ancestor, then starts that child controller's own `reload()`; a child can start its descendant layer only after its own acknowledgement. Mounted/response Frame wrappers and permanent ancestors remain ineligible; unsupported nonpermanent protocol reparenting, supported permanent nodes without stable IDs, incompatible permanent identities, omitted unmatched permanent-bearing ancestors, duplicate IDs, or out-of-subtree plans fail atomically with no replacement fallback. All matching initial, eager, lazy, manual, link, form, history, visit, and recurse Frame responses remain replacements by default and may use morph only through explicit `renderMorph()` selection; Stream Frame targets remain governed by their separate Stream mutation contract.
- [x] Final-ownership synchronous Frame commit boundary: a matching response is parsed once, extracted, and structurally preflighted against the exact active document before a frozen callback runs under a session-wide guard that blocks reentrant document/Frame claims and makes exact cancellation inert. Callback failure or a returned value/thenable prevents the prepared Frame mutation from beginning; the callback itself must be atomic-on-error and must not mutate the document session or reenter package controllers. Stale ownership, duplicate active-document IDs, and non-Frame outcomes leave the Frame unchanged. Successful mutation preserves the mounted wrapper and unrelated node identities while updating children and the final canonical response `src`, with an internal prepared option for same-tree logical document-URL retargeting. Finalization may start newer work, stale embedded Streams then stop with `interrupted: true`, and `FrameCommitError` distinguishes committed truth from pre-commit failure.
- [x] Optional promoted Frame history through `FrameHistoryCoordinator`: explicit or inherited local `advance` always pushes—including the same canonical URL—while `replace` retains the current restoration index, and every direct visit or Frame-target form promotion by one exact mounted Frame controller reuses its restoration identifier. Direct Frame GET admission optionally captures the outgoing whole document while the Frame still has its prior `src`; the response-driven form path likewise captures only when a shared cache is configured and admits the final `src` before body buffering except for a potentially empty `201`. Matching successful form XML and matching direct-GET `2xx`/`4xx`/`5xx` XML commit against the history entry current at response completion, so independent Frames publish in completion order; a newer admitted whole-document visit or document-targeting form submission invalidates an older promotion before it can write history or children. The final guarded path applies optional snapshot storage → history → Frame children/final `src`/logical document URL. Empty, canceled, MIME/parse/missing-Frame/preflight-failed, stale, snapshot-failed, and host-write-failed work changes neither history, logical document URL, nor Frame children, but retains an already-exposed requested/final `src`. The guard blocks package session/tree mutation APIs during the host write; active protocol nodes, child/attribute collections, and returned Frame/stream-source index arrays remain readonly and frozen while package mutations update protected backing state. When no cache is configured—or an intermediate concurrent snapshot is absent—host traversal falls back to one canonical whole-document GET and never replays a Frame GET. Frame-local `restore`, scroll/anchor restoration, and Frame preview rendering remain pending.
- [x] Turbo 8.0.23 lazy-appearance Frame history promotion: the first actual visibility re-reads the live Frame and exact lowercase `data-turbo-action`; `advance`/`replace` prepare the existing history plan before the loader allocates a request ID or canonicalizes `src`, preserving snapshot-before-source timing and the mounted controller's restoration scope across later explicit visits. Immediate, synchronously reported, and delayed visibility share the same promoted request. Exact `restore`, missing coordination, unvisitable sources, and misaligned history fail once through controller error state before request ownership; invalid, blank, or differently cased values remain ordinary history-neutral lazy loads. Eager loading plus manual `load()`/`reload()` remain history-neutral.
- [x] Frame target resolution for submitter/element/Frame-default/current-Frame precedence, named Frames, `_self`, `_parent`, and `_top`, with missing named targets falling back to the current Frame and `_top`, unavailable `_parent`, or disabled Frame targets promoting to the owning document.
- [x] Identity-bound observable Frame controller with eager `src` loading, explicit lazy/manual loading, same-source reload, renderer-acknowledged terminal busy/complete state for mounted matching GET replacements, and cancellation/rebinding on disable, source removal, same-ID wrapper replacement, or disconnect.
- [x] Opt-in React Frame lifecycle wiring that connects mounted controllers, subscribes to lifecycle state, eagerly loads sources, and cancels/disposes requests when a Frame subtree unmounts.
- [x] Host-rendered Frame GET lifecycle and accessibility binding with a stable boundary, nearest-Frame hook, frozen native busy state, nested scoping, error containment, same-ID replacement rebinding, and no synthetic XML attributes.
- [x] One-shot native autofocus for every ordinary authoritative whole-document tree generation, including the initial `DocumentSession` tree, full replacements, and cached restorations: raw `autofocus` presence contributes stable authored-ID application-element descendants in document order, traversing nested Frames while excluding protocol wrappers and `template`/Stream/source subtrees. Cached preview generations deliberately suppress autofocus; their later canonical replacement owns a fresh opportunity. A bounded successful current-document Refresh Stream morph also creates a fresh authoritative generation, but deliberately suppresses document autofocus. Each pending claim is bound to its exact session, tree generation, document, ordered candidates, and candidate node identities; the renderer consumes it only after descendant handles mount, so Strict Mode and multiple providers cannot duplicate focus. Reinstalling even the same authoritative tree creates a new generation and may focus again, while in-place Frame or standalone Stream mutations never create document autofocus work. A missing adapter consumes the generation as a no-op, unavailable handles are skipped, and synchronous adapter contract failures are redacted without rolling back the committed logical tree.
- [x] One-shot native autofocus after controller-owned matching Frame GET and matching Frame-target form XML responses configured with that exact shared mounted Frame registry: once Frame children and embedded Streams settle, raw `autofocus` presence contributes stable authored-ID application-element descendants from the exact final mounted Frame in document order. Nested Frames are traversed, but Frame and other protocol wrappers plus `template`/Stream/source subtrees are excluded. The internal intent stays bound to its originating session, wrapper, ordered candidates, and candidate node identities, so unrelated Frame mutations do not suppress it while same-ID replacement does; after replacement children mount, an optional synchronous `AutofocusAdapter` skips unavailable native handles and focuses the first available candidate exactly once, including under React Strict Mode. Missing adapters consume the intent as a no-op, missing handles are skipped, and adapter contract failures are redacted through the Frame error surface without rolling back committed children. The gallery's Frame-scoped link has iPhone Simulator/Expo Go evidence: it renders `Frame link loaded` and the returned `Autofocused Frame field` accepts text without a second tap. Direct headless Frame application, standalone Stream/morph mutations, arbitrary-node autofocus scrolling, browser focusability parity, Android interaction, release-build, and physical-device evidence remain outside that proof.
- [x] Bounded native standalone Stream-message autofocus for direct fragments, Cable messages, and `text/vnd.turbo-stream.html` form responses: one completed message—when its dispatch path has a response guard, one that still owns it—selects only its first stable authored-ID application candidate with raw `autofocus` in Stream/template order from a default exact-target structural `append`, `prepend`, `before`, `after`, `replace`, or `update`; the four insertion actions retain structural semantics with any `method` metadata. Exact-target `replace`/`update method="morph"` are also admitted only when their whole template payload contains no `data-turbo-permanent`. An earlier still-mounted intent wins over later standalone messages before the next renderer commit; removal or identity-changing replacement discards it and lets a later eligible message claim the next candidate, while an ordinary candidate remains eligible when bounded morph reconciliation retains its exact node identity. Once descendants mount at that or a later session revision, the optional synchronous `AutofocusAdapter` attempts the candidate only when `getFocusedId()` exists and returns `undefined`; existing logical focus, a missing snapshot method or adapter, an unavailable handle, and adapter failures are all one-shot no-ops or redacted errors. Cancellation, supersession, selectors, permanent-bearing or other morphs, custom replacement renderers, no-ops, and document/Frame-embedded Streams create no intent. This does not claim generated IDs, DOM focusability or focus restoration, Turbo Bardo/permanent-node autofocus, generic container selection, browser repaint timing, or physical-device evidence.
- [x] Bounded post-focus native root-scroll handoff: `ExpoTurboProvider.autofocusScroll` accepts a synchronous `AutofocusScrollAdapter`. After a document, controller-owned Frame, or admitted standalone Stream autofocus candidate has successfully focused, the renderer calls `canScroll(nodeKey)` and then `scrollTo(nodeKey)` only when the host reports availability; an unavailable/missing adapter is a one-shot no-op. Throws, nonboolean availability, non-`undefined` returns, Promises, and thenables are redacted without rolling back the committed tree or the successful focus. The package neither discovers containers nor waits/retries for physical completion; ordinary document history/Refresh scrolling keeps its existing later ordering. The standalone Expo host registers only its root `ScrollView` and `DemoFormInput` handles, retains one request while those explicit measurements arrive, uses nearest-edge nonanimated scrolling, and has an iPhone 17 Pro Simulator/Expo Go flow that reveals and focuses the offscreen `Root autofocus scroll target`. Nested/arbitrary containers, virtualizers, Android interaction, release builds, and physical-device evidence remain pending.
- [x] Native Frame-boundary autoscroll after controller-owned matching Frame GET and matching Frame-target form XML responses configured with that exact shared mounted Frame registry: raw `autoscroll` presence on the mounted or matching response Frame opts in, including `autoscroll="false"`; only the mounted wrapper supplies `data-autoscroll-block` (`start`, `center`, `end`, or `nearest`, default `end`) and `data-autoscroll-behavior` (`smooth`, otherwise `auto`). After replacement children and embedded Streams mount, an optional synchronous `FrameAutoscrollAdapter` scrolls the registered exact Frame boundary only when `canScroll(frameId)` is true, then Frame autofocus and terminal lifecycle work continue in order. Missing/unavailable boundaries consume a one-shot no-op; invalid adapter results and throws are redacted without rolling back committed children. The standalone Expo gallery registers non-collapsible Frame boundaries plus its root `ScrollView` with cached geometry. An iPhone Simulator/Expo Go flow brings the lazy `preview-frame` into view, completes its recursive source, and visibly places that Frame at its registered `start` alignment. This is simulator evidence that smooth scrolling starts, not a physical-scroll completion claim. Headless application, standalone Stream/morph scrolling, arbitrary-node scroll discovery, Android interaction, and physical-device evidence remain pending.
- [x] Injected lazy-Frame visibility lifecycle that observes while mounted, loads on first visibility, stops observing when the request starts, and reports automatic-load failures.
- [x] Concrete standalone Expo visibility adapter for ordinary Frame layout inside the gallery's root and registered nested `ScrollView` regions: unmeasured Frames default hidden; non-collapsible native Frame anchors require positive intersection with the shared window-coordinate intersection of every registered clipping viewport; root/nested layout, scrolling, content-size, and window-dimension changes remeasure; and stale Frame/container callbacks, same-ID container replacement, disposal, and duplicate boolean transitions are suppressed. The gallery includes a nested lazy-Frame scenario plus a horizontal `FlatList` scenario: every direct list row has an explicit stable Frame ID, and loading additionally requires current native viewability membership rebuilt from the full snapshot. Buffered, recycled, stale-key, stale-index, removed, and replaced rows cannot be admitted by geometry alone. Focused tests cover the nested admission and one-time load; an iPhone Simulator/Expo Go flow scrolls the inner region and visibly renders `Nested lazy Frame loaded`. Android interaction, arbitrary virtualizers, physical-device, and release-build evidence remain part of the complete gallery gate.
- [x] Bounded Frame `recurse` extraction with whitespace-token matching, same-origin intermediary GETs, independent request IDs, redirected-base resolution, URL-loop/depth rejection, and canonical source ownership.
- [x] Shared programmatic Frame visit API for current, named, `_self`, `_parent`, and `_top` targets, with credential-free, fragment-free HTTP(S) admission, exact destination-Frame action inheritance, ordinary same-origin Frame loading and same-source reload, optional root-visitable local `advance`/`replace` history coordination, history-backed top-level `restore`, plus awaited owning-document or cross-origin host-navigation delegation whose failures preserve Frame request/source ownership.
- [x] Unless the closest `data-turbo` setting opts out, root-visitable registered native links inside the nearest Frame and top-level links whose non-`_top` target identifies an active enabled Frame enter the shared Frame visit path, including Frame-default/element target precedence, `_self`, `_parent`, `_top`, current-Frame fallback for a missing named target, and isolated Frame/document request ownership. Top-level `_top`, missing, and disabled targets follow ordinary top-level activation; opted-out links delegate through host navigation.
- [x] Route-free Rails Engine, distinct Expo Turbo XML MIME type, confined host-owned `.xml.erb` document rendering, explicit validated `Turbo-Frame` request helpers, and opt-in literal target IDs for persisted records across `record`, `document`, `frame`, `list`, `form`, `error`, and `loading` roles. `expo_turbo_frame_tag` normalizes model classes with Rails' `dom_id`, then delegates the resulting literal Frame IDs to `turbo-rails` without installing its HTML layout concern. A controller must declare its component and style-token capabilities to render a document; when declared, the same policy also admits its Frame, Stream, and raw controller-broadcast output only when it uses those components (including deliberate aliases), exact unprefixed protocol wrappers, and native-equivalent bounded `style-tokens`. Malformed or undeclared output fails before sending bytes or enqueueing a controller broadcast; context-free stream APIs remain structural-only. The same concern provides a cache key that preserves existing `Vary` dimensions, adds `Turbo-Frame`, and separates full-document and valid Frame-ID conditional validators without choosing host cache policy. It exposes `expo_turbo_stream`/`render_expo_turbo_stream`; the builder emits standard sibling `text/vnd.turbo-stream.html` fragments for the eight built-in actions plus selector variants from positional or keyword raw XML, captured template markup, explicit root-confined XML partials, inferred record partials, a root-confined XML layout around a captured block, or XML-format renderables whose limited context can render only root-confined XML partials. It also emits standard public `Turbo::StreamsChannel` sources; callers can send pre-rendered markup, or send and caller-thread-debounce pre-rendered Refresh tags by the exact Expo stream name and request ID, all on a normalized `:expo` stream suffix that keeps Expo payloads separate from browser HTML streams. Queued broadcasts resolve the exact stream name and markup before enqueue; they do not serialize host models or render templates later. Its opt-in `expo_turbo/rails/testing` entrypoint provides strict structural XML document and sibling Stream-fragment parsing for host assertions without eager Nokogiri loading. The document renderer validates one strict structural XML document and rejects blank or duplicate literal IDs across the complete response. Its optional protected Channel uses a separate verifier and opaque pubsub token, then delegates subject/grant/resource authorization to host callbacks; the separate Rails example remains anonymous. The gem does not validate arbitrary component props. The gem runs against exact `turbo-rails` 2.0.10/2.0.23 pins.
- [x] Public package-boundary checks scan TypeScript sources, every manifest dependency section, emitted JavaScript/declarations, and source-map source content. They reject browser Turbo, Expo Router, React Query, known private package scopes, app aliases, and source-root escapes without treating public documentation text as an import.
- [x] Independently installed Expo and Rails examples in public CI, including a native component tree rendered from XML in the Expo gallery.

### Recent verification and remaining work

- [x] Bounded generic nested Expo Router path proof: the gallery link opens `/demo/routes/ios-proof/details?source=gallery&tag=a&tag=b&empty=` through the safe catch-all codec, renders the generic-route confirmation, and retains the ordered repeated/empty query URL in opaque history metadata. Focused bridge/runtime coverage and an iPhone 17 Pro Simulator/Expo Go interaction prove this route; Android interaction, release-build, and physical-device evidence remain pending.
- [x] Bounded unmanaged direct-query proof: a cold iPhone 17 Pro Simulator/Expo Go link to `/demo/routes/ios-proof/details?source=direct&tag=a&tag=b&empty=&plus=+&encoded=%20` reaches the generic route after strict path/query coherence checks, performs the guarded document GET, and repairs the focused route with canonical history metadata. Repeated keys and empty values remain ordered; the Router's canonical URL may normalize the original query spelling. Android interaction, release-build, and physical-device evidence remain pending.
- [x] Bounded cold cross-document Expo Go anchor proof: `exp://127.0.0.1:8081/--/demo/linked#linked-native-anchor-target` validates against the cold catch-all route, performs the existing canonical GET and same-key managed-history repair for `/demo/linked`, then waits for that new document's root target layout before scrolling. Focused bridge/runtime coverage and an iPhone 17 Pro Simulator/Expo Go interaction visibly reach the linked target. Frame targets, other host routes, Android interaction, release-build, and physical-device evidence remain pending.
- [x] Bounded nested-container anchor proof: the gallery's visible link inside `nested-scroll-region` targets `#nested-native-anchor-target`; ownership is inherited through the example's declared container context, so only the inner `ScrollView` moves. A cold exact `exp://127.0.0.1:8081/--/demo#nested-native-anchor-target` launch first reveals that declared container through the root and then applies the inner target offset. Focused registry/runtime tests and no-tap iPhone 17 Pro Simulator/Expo Go evidence pass. Cross-document nested targets, Frame targets, arbitrary discovery, Android interaction, release builds, and physical devices remain pending.
- [x] Bounded current-Frame anchor proof: an exact same-document fragment link inside a mounted Frame may scroll to an ordinary application target in that same nearest Frame. The package revalidates source, target, and Frame identity before calling the host adapter; the example measures the nested target against its registered document-content View and scrolls the root without a Frame request or history write. Focused renderer/runtime tests and an iPhone 17 Pro Simulator/Expo Go interaction visibly reach the target. Cross-Frame and Frame-wrapper targets, Android interaction, release builds, and physical devices remain pending.
- [x] Turbo Frame-target resolution for same-document anchors: the existing destination resolver maps `_self`, `_parent`, `_top`, enabled named Frames, missing/disabled fallbacks, and an enclosing Frame's default `target`; activation proceeds only when the ordinary application target belongs to that resolved document/Frame scope. Frame wrappers and mismatched scopes still fail closed, while a resolved anchor starts no request or history write. Focused resolver/renderer/runtime tests pass, and an iPhone 17 Pro Simulator/Expo Go interaction visibly jumps from a top-level link into the named `link-frame` target. Cross-Frame and Frame-wrapper targets, Android interaction, release builds, and physical devices remain pending.
- [x] Bounded same-path `replace` refresh: a fragment-free root-visitable explicit `replace` whose requested and active document URLs have the same pathname reads `data-turbo-refresh-method` and `data-turbo-refresh-scroll` only from the active XML root. Exact `morph` requests the existing success-only compatible-root morph; exact `preserve` suppresses the otherwise post-render root-scroll reset. The path skips snapshot preview but still issues its canonical GET and replaces the focused history entry. The gallery's `/demo/linked?replace=morph` fixture changes only to `revision=next`, retains its stable application card, and returns from the lower control to the reset root after React acknowledges the committed tree. Focused core/example tests and an iPhone 17 Pro Simulator/Expo Go interaction cover that scope; Android interaction, release-build, and physical-device evidence remain pending.
- [x] Direct and cross-document Frame fragments: Frame-targeted links retain a validated nonempty fragment for logical click/result truth, issue the GET against the fragment-free URL, keep the canonical Frame `src` fragment-free, and—after the matching response is committed and acknowledged—revalidate that the target belongs to the exact destination Frame before requesting native scroll. Promoted `advance`/`replace` Frame history retains the fragment while transport and redirected response ownership remain fragment-free. The standalone gallery loads `link-frame`, then reaches its response-owned `frame-linked-fragment-target`; focused core/renderer/runtime tests cover transport, scope, scrolling, and history. A Maestro-driven iPhone 17 Pro Simulator/Expo Go interaction visibly reaches the response-owned target; Android interaction, release-build, and physical-device evidence remain pending.
- [x] Exact Frame response preload and provisional-preview proof: the gallery marks its query-scoped cross-document Frame-fragment link with `data-turbo-preload`, shares one `FramePreloadCache` between `FramePreloader` and preview-enabled mounted Frame controllers, and renders distinct cached preview XML while a delayed canonical GET remains active. Focused core/renderer/runtime tests prove no preload-time live-tree mutation, exact Frame/URL isolation, one-use preview consumption, suppressed preview Streams/side effects, canonical replacement, and fragment scrolling only after canonical acknowledgement. A Maestro-only iPhone 17 Pro Simulator/Expo Go flow visibly captures the busy preview, its canonical replacement, and the response-owned fragment target; Android interaction, release builds, and physical-device evidence remain pending.
- [ ] Physical iOS VoiceOver, Android TalkBack, and browser screen-reader evidence for disabled document links plus form busy state, submitter pending/disabled and submits-with presentation, confirmation, terminal-result announcements, retry, and dismissal.
- [x] Explicit native equivalents for browser-owned link contexts: authored non-`_self` targets and `download` links never invent an implicit React Native fallback. The optional host adapter receives only frozen, resolved `{ target, url }` or `{ filename?, url }` requests after strict live-node admission; download wins when both attributes are present, missing adapters fail closed, and host failures are redacted. Concrete deep-link, file-storage, and external-context presentation remains app policy rather than package compatibility work.
- [x] Native autofocus/autoscroll compatibility uses explicit host-registered focus targets, post-focus reveal handles, and mounted Frame boundaries. Automatic browser-style focusability, arbitrary scroll-container discovery, and implicit virtualizer traversal are N/A on React Native; hosts register the exact nested container or virtualized target they own. Android interaction, release-build, and physical-device confirmation remain part of the device-conformance gate rather than this capability disposition.
- [x] Automatic browser DOM control discovery is N/A on React Native. App-installed components explicitly register live native descriptors; server-authored `name`, `value`, `checked`, or submitter-looking XML attributes never synthesize a successful value without that host registration. Registered descriptors cover the native-equivalent successful-control ordering and semantics above.
- [x] The standalone Rails example accepts one deliberately constrained `text/plain` POST profile: UTF-8 `profile[first_name]` followed by `commit=save`, in that exact order with CRLF line endings and a final CRLF. It does not install a generic Rails parameter decoder or body-based method override.
- [ ] Android multipart interaction, release-build and physical-device confirmation, broader host picker/URI-file integration, broader host upload profiles, and any broader host-specific `text/plain` profile.
- [x] Opt-in `422` Turbo Stream `replace method="morph"` for compatible application IDs, including the standalone Rails form's controlled local-draft preservation proof. Ordinary XML Frame validation remains replacement-only.
- [x] Shared logical morph lifecycle for every implemented current-document, direct Frame reload, and exact Stream morph path: frozen `before-morph-element` callbacks may veto a matched element or an omitted-element removal; `before-morph-attribute` may independently veto an update/addition or removal; and `morph-element` observes successfully committed retained elements in child-before-parent completion order. Listener snapshots are stable, callbacks must be synchronous and return `undefined`, reentrant session/tree mutation fails closed, and notification faults are isolated. Native logical handles replace DOM targets/bubbling; permanent and opaque nested-Frame plans stay silent.
- [ ] Remaining lifecycle-event compatibility: browser DOM targets/bubbling and physical-paint timing; physical Stream renderer-flush timing; and browser DOM-attribute/repaint timing.
- [x] Browser-only morph state is N/A on React Native: there is no DOM active-element cursor heuristic, uncontrolled-input selection range, automatic scroll-tree capture, or way to move one mounted component instance between React parents. The native contract is the bounded package-owned reconciler used by Stream targets, current-document refreshes, direct Frame reloads, and ordinary matching Frame responses explicitly selected through `before-frame-render`: stable logical identity survives compatible moves, same-parent component state is retained unless its definition selects reset, cross-parent moves remount under the new React parent while preserving logical/session ownership, and registered focus/reveal handles restore an exact stable target when possible.
- [x] Complete standalone Rails response boundary matrix: canonical `200` XML; empty `201` and `204`; followed `303` canonical GET; authoritative `422`/`500` XML; wrong MIME; correct-MIME malformed XML; a bounded delayed document canceled by a newer native request; matching, delayed, and missing Frames; normal/morph/refresh HTTP Streams; authoritative Frame-form `422`, `204`, and `303`; and public/protected Cable delivery. Rails request specs assert the exact wire boundaries and the real-host native smoke drives them through the public loaders without a JSON fallback.
- [ ] Remaining Rails integration: final product-host route/view policy, protected-resource authorization, credentials, and expiry/revocation mapping.
- [ ] production host-specific Action Cable credentials/tickets, authenticated native socket integration, protected-resource expiry/authorization/device proof, and Frame-aware physical-device lifecycle/network evidence. The package now owns host-neutral lifecycle, reachability, and finite retry coordination; each host still owns credentials, network interpretation, and retry values.
- [ ] Complete standalone compatibility gallery, differential conformance suite, and physical iOS/Android evidence.
- [ ] Stable npm/RubyGems publication. No registry release is supported yet.

## TypeScript API boundaries

The lifecycle API now includes awaited/cancellable owning-document network, snapshot, and successful form `before-render`, notification-only owning-document `reload` and page-level `morph`, renderer-acknowledged owning-document and Frame `render`/`load`, and cancellable per-element/per-attribute morph notifications through `expo-turbo/core`.

The root package and explicit `expo-turbo/core`, `expo-turbo/adapters`, and `expo-turbo/registry` subpaths expose the current version, errors, inspector, parser, deterministic diagnostic serializer, typed logical, request, semantic-link click, form-submission, owning-document visit/reload, Frame-missing/render/load, and Stream lifecycle events, state-reference resolution, addressable tree/session/state with subtree disposal, independent document-tree cloning, fragment-free LRU snapshots, root cache-policy metadata, explicit session snapshot capture/manual restore, safe programmatic full-document preload, ordinary cached-preview revalidation with observable provisional tree state, visit-driven outgoing snapshot capture, a host-acknowledged history identity/restoration ledger with atomic staged proposal commitment, optional aligned parsed `advance`/`replace` history coordination, history-backed cache-first explicit document restoration, optional promoted Frame and mounted Frame-target form `advance`/`replace` coordination, host-driven cached-or-network traversal restoration, managed host traversal subscription, exact document-request start acknowledgement and owner-aware cancellation, host-owned document-scoped identity-bound form controls, successful string-entry collection, immutable active-form request planning, atomic destination-aware submission proposals, generated form-link submission, session-shared exact-form submission activity and terminal reporting, safe fresh-request recovery, duplicate/supersede policy, host-injected async form confirmation and exact-terminal announcement adapters, caller-scoped execution with classified unapplied form-response candidates, shared-destination form submission with canonical response application, selectors, structural/custom/refresh Stream dispatch, session-bound `CableStreamSourceRegistry` lifecycle, delivery, and redacted per-source connection snapshots, bounded recent-request tracking, document GET loading with controlled admission plus guarded parsed-tree and cached-tree commit callbacks, root-aware visit and refresh control, Frame target/request/recurse/controller/programmatic-visit APIs, semantic style adapters, typed component/component-action/custom-Stream-action registries, value and presence codecs, and host-adapter contracts. `expo-turbo/react` exposes the provider, root renderer, optional rendered-source Cable leases, provider-injected automatic preload for rendered semantic document links, typed component-action/document/scoped-state/style/disposal hooks, explicit form ownership and live control registration/collection/planning/proposal hooks, observable form activity, terminal result state, a host-defined form boundary, safe explicit retry, an at-most-once host announcement attempt per terminal revision, exact activated-submitter pending/effective-disabled/submits-with presentation with replay-safe final-scope cancellation, document-tree native-link activation with document-load, Frame-visit, generated-form submission, delegated-navigation, click-canceled, or disabled-ignored results, explicit form state boundaries, automatic Frame state inheritance, host-defined document and Frame boundaries, nearest-document/Frame binding hooks, node and lifecycle subscription hooks, and error surface. `expo-turbo/testing` remains a reserved module boundary and intentionally exports no runtime APIs yet. Deep source imports are unsupported.

React hosts keep root-scroll capture and persistence themselves—for example, by writing finite coordinates with `history.updateRestorationData(entry.restorationIdentifier, { scrollPosition })`—and may supply `ExpoTurboProvider.documentHistoryScroll`, the synchronous `DocumentHistoryScrollAdapter`, to consume that data only after a `restoreTraversal` document receives exact React acknowledgement. This requires the controller's live `DocumentVisitLifecycle`. The example captures only its root gallery `ScrollView` and defers a focused Router traversal until its mounted document boundary has retained the renderer and registered that container. `documentRefreshScroll` remains the separate Refresh Stream top-reset path. `documentAnchorScroll` is a separate, bounded same-document anchor-request hook described above; the package does not discover scroll containers, while the example now maps one explicitly declared nested region. None of these contracts implements Frame autoscroll, route/cross-document anchor ownership, generic browser history, arbitrary nested-container discovery, or physical-scroll completion.

Non-canceled form-response reports expose `responseUrl` as the validated final transport URL. That field does not imply that the active document URL or a Frame `src` adopted it. Applied document and direct Stream reports expose `streams.interrupted`; applied Frame reports expose `frame.streams.interrupted`; empty and canceled reports have no Stream report. Read these variant-specific fields with `applicationDestination` and the application/status fields to determine canonical session state.

The adapter surface is host-neutral. Core source does not import Expo Router, an Action Cable client, an app API client, or private application hooks.

## Status and compatibility

The intended baseline is Turbo 8.0.23, Rails/Action Cable 8.1.3, and `turbo-rails` 2.0.23, with the gem also testing `turbo-rails` 2.0.10 compatibility. Those targets are planning constraints until the public conformance suite proves them.

## Changelog

**2026-07-23**:

- Changed: Added a Maestro-only smoke for the installed standalone iOS and Android Release applications and documented the independent source-consumer build order.
- Why: Static Expo exports and Expo Go did not prove that the generated native application could bundle the package's public `dist/` exports and launch without Metro.
- Impact: A clean detached checkout now builds the root package before the example's `file:../..` install, produces an iPhone Simulator Release app and a signed Android emulator Release APK, and passes direct gallery and mounted-Frame assertions through Maestro on both platforms. Physical iOS/Android evidence remains open.

**2026-07-23**:

- Changed: Completed the standalone Rails response boundary matrix with canonical `200`, empty `201`, followed `303`, malformed correct-MIME XML, and a bounded delayed document in addition to the existing `204`, `422`, `500`, wrong-MIME, Frame, form, Stream, and Cable cases.
- Why: The public host needed real transport fixtures for every document loader branch, including redirect final-URL truth, parser failure, and cancellation by a newer request.
- Impact: Rails request specs assert the exact responses and the real-host native smoke proves successful redirect adoption, empty `201`, malformed XML rejection without tree mutation, and delayed-request cancellation. Product-host authorization remains separate.

**2026-07-23**:

- Changed: Admitted shared XML `dirname` metadata and let rendered value/hidden controls derive its sidecar from the nearest exact `ltr`/`rtl` direction; rendered string-value controls with absent or `auto` direction now use Unicode first-strong inference over their current live value.
- Why: Core already supported explicit directionality snapshots, but a host component had to duplicate stable server-authored `dirname` and resolve automatic text direction manually.
- Impact: The sidecar follows its primary successful value in ordinary document order, explicit host directionality wins, `R`/`AL` first-strong values submit `rtl`, `L` and nonempty no-strong values submit `ltr`, empty auto values use inherited/provider fallback, and hidden controls still require resolved XML direction. The Expo gallery proves automatic LTR submission while focused Strict Mode coverage switches a live control from LTR to RTL.

**2026-07-23**:

- Changed: Promoted Frame GETs and successful promoted Frame forms now emit the shared notification-only `before-cache` lifecycle immediately before their outgoing snapshot is cloned.
- Why: Promoted history already cached the outgoing whole document, but omitted the lifecycle boundary used by every other controller-owned cache capture.
- Impact: Synchronous listener mutations are retained in the promoted snapshot; a direct Frame listener that replaces its document invalidates the plan before the request starts. Failed, empty, Stream, initially `no-cache`, and history-neutral Frame paths remain ineligible. This does not claim renderer-dependent Frame visual or browser next-task timing.

**2026-07-23**:

- Changed: Preserved the admitted Stream render scheduler through Frame-loader and form-submission controller option snapshots.
- Why: The direct dispatcher awaited the host scheduling point, but the two response-owning controllers silently dropped the same public option before embedded Frame or negotiated form Streams reached it.
- Impact: Direct, embedded Frame, and Stream-form actions now all await the host's pre-render scheduling point before mutation, notification, and later siblings. Focused tests hold both response paths at the scheduler boundary and prove no Stream mutation leaks through early; physical React/native paint remains host evidence.

**2026-07-23**:

- Changed: Expanded the source-only cross-language compatibility manifest with every built-in Stream envelope and compact exact metadata assertions.
- Why: The prior shared Stream fixture proved only append/remove parsing and Rails emission, leaving most Stream helper/runtime wire shapes outside the atomic TypeScript/Ruby contract.
- Impact: TypeScript and Ruby now consume the same Turbo 8.0.23-attributed append/prepend/replace/update/remove/before/after/refresh fragment and agree on target/selector, morph, request-ID, scroll, and ordered template-root metadata. The Rails TagBuilder independently emits the same complete shared assertion record.

**2026-07-23**:

- Changed: Added a standalone Rails response-scenario endpoint and a real-host native loader smoke for handled XML errors, empty responses, wrong MIME, matching/missing Frames, and bounded delay.
- Why: Deterministic core fixtures proved classification, but the public demo did not yet prove that a real Rails host emits those boundaries in the shape consumed by the package.
- Impact: The same public `DocumentRequestLoader` and `FrameRequestLoader` now consume the Rails response matrix in CI. Renderable `422`/`500` XML commits, `204` and wrong MIME preserve the active tree, matching/delayed Frames commit, and a missing Frame fails with the typed error while retaining the mounted Frame.

**2026-07-23**:

- Changed: Added a standalone-host protected-ticket generation, identified demo Cable connection, and local-only targeted revocation endpoint, plus a native proof control.
- Why: Subscription-time authorization alone does not remove an already admitted socket when its ticket or subject is revoked.
- Impact: The demo invalidates every prior ticket generation, sends its identified subject an Action Cable `disconnect` with `reconnect: false`, and permits recovery only through a newly issued ticket. Request coverage proves old-ticket rejection. A Maestro-only iPhone Simulator/Expo Go flow proves targeted removal, bounded fresh admission, protected re-confirmation, Frame reconciliation, and a later XML replacement. The gem still owns no connection identity or product policy; production distributed revocation remains host work.

**2026-07-23**:

- Changed: Added explicit host-triggered `LifecycleCableAdapter.rotateCredentials()` and a protected Expo demo control that uses it without rebuilding the document runtime.
- Why: Logout, impersonation, account switching, and credential rotation are host identity events; waiting for background, network loss, or socket failure can leave the prior authenticated transport alive too long.
- Impact: The current socket or pending credential result becomes stale immediately, logical subscriptions remain registered, and an active reachable host requests a fresh credential/transport snapshot. Suspended hosts defer recreation until they can connect. Deterministic package and protected-example tests prove stale-generation disposal and distinct ticket headers. A Maestro-only iPhone Simulator/Expo Go flow proves the explicit control closes the protected socket, obtains a fresh Rails ticket, re-confirms and reconciles its Frame, then receives another protected XML replacement. Server-driven revocation teardown and production identity mapping remain host work.

**2026-07-23**:

- Changed: Allowed `LifecycleCableAdapter` to await one host-owned credential/transport factory and moved the protected Expo demo ticket request into that per-generation factory.
- Why: Reusing the ticket captured at runtime creation could reconnect a fresh socket with an expired or rotated credential.
- Impact: Every protected foreground, reachability, or bounded-retry generation requests a fresh no-store ticket before constructing its socket. Pending credentials are coalesced across logical sources and safely discarded after suspension or disposal; credential request failures use the existing finite retry budget. Focused package and Expo tests prove rotation without exposing tickets in URLs or errors. A Maestro-only iPhone Simulator/Expo Go flow plus the live Rails request sequence proves a fresh ticket on foreground recovery, protected re-confirmation, Frame reconciliation, and a later protected XML replacement. Production identity and revocation teardown remain host work.

**2026-07-23**:

- Changed: Extended `LifecycleCableAdapter` with optional host-injected reachability and a finite exponential retry policy, then wired the standalone Expo host to `expo-network` with five attempts from one to sixteen seconds.
- Why: AppState recovery alone leaves a terminal socket offline after ordinary network loss, while retry inside the single-lifetime WebSocket transport would conflate credentials, platform policy, and protocol state.
- Impact: Logical subscriptions now pause without timer churn while offline/background, recreate a fresh transport immediately when reachable/active, retry terminal transport/factory failures within explicit bounds, reset after confirmation, reconcile affected Frames after re-confirmation, and fail terminally after exhaustion. Subscription rejection remains terminal. Focused package and Expo runtime tests, web/iOS/Android exports, and a Maestro-only iPhone Simulator flow through a real Rails-server stop/restart, socket re-confirmation, Frame reconciliation, and later Stream broadcast pass. Android interaction, production credentials, release builds, and physical-device proof remain pending.

**2026-07-23**:

- Changed: Added the public `LifecycleCableAdapter` and wired the standalone Expo host to keep one document runtime while it suspends and recreates credential-bearing Action Cable transports across inactive/background/active transitions.
- Why: Rebuilding the whole document runtime on background discarded native document state and made lifecycle recovery an example-only convention rather than a host-neutral package contract.
- Impact: Logical subscriptions survive suspension, stale transport callbacks are ignored, foreground confirmation reports a reconnect and drives existing Frame-aware canonical reconciliation. Focused package/example tests, full web/iOS/Android exports, and a Maestro-only iPhone Simulator flow through Settings background/foreground plus a post-reconnect Rails broadcast pass. Network reachability/backoff, production credentials, Android interaction, release builds, and physical-device evidence remain pending.

**2026-07-23**:

- Changed: Added an activation-facing settlement to retained document-preload leases while preserving the primary shared preload Promise identity.
- Why: A renderable `4xx`/`5xx` response is valid authoritative navigation input, but the marker-preload result rejects it; observing that same rejection during press-in emitted a false preload error before activation.
- Impact: The renderer stays silent when a raw response is safely captured for one-shot activation, then the ordinary document loader exclusively classifies and renders it. Transport and uncaptured-response failures still reach the press-in observer, and direct/automatic marker preloads keep their existing strict results.

**2026-07-23**:

- Changed: Replaced parsed-tree-only press-in handoff with an immutable raw response handoff that re-enters the ordinary document loader at activation.
- Why: A parsed tree discarded the original status, MIME type, request ID, redirect flag, and final URL, so reused navigation could not preserve normal error, history, refresh-suppression, or redirect lifecycle semantics.
- Impact: The one physical prefetch now becomes the authoritative activated response, including `201` redirects and renderable `4xx`/`5xx` XML. Focused core/example tests, web/iOS/Android static exports, and the distinct redirected demo fixture pass; a Maestro-only iPhone 17 Pro Simulator/Expo Go interaction confirms the redirected response renders without reaching its canonical fallback. Android interaction and physical-device proof remain pending.

**2026-07-23**:

- Changed: Added a one-entry, ten-second native press-in response handoff shared by document preloading and activation, plus a standalone fixture whose ordinary fallback response is visibly distinct.
- Why: The prior press-in path warmed a snapshot but still performed a second canonical GET on activation, unlike Turbo's short-lived interactive request reuse.
- Impact: Activation consumes the exact pending or settled parsed response once and commits it as authoritative truth without polluting marker-preload snapshots. Focused package/example tests and a Maestro-only iPhone 17 Pro Simulator/Expo Go interaction pass; Android interaction, release builds, and physical devices remain pending.

**2026-07-23**:

- Changed: Added opt-in provisional Frame preview rendering followed by an ownership-safe canonical revalidation request, and made the standalone Expo gallery expose distinct cached and canonical Frame states.
- Why: Consume-only preload removed the activation fetch but could not demonstrate Turbo-style preview truth or keep transient response side effects separate from canonical lifecycle truth.
- Impact: Preview content stays visibly busy and cannot dispatch embedded Streams, autofocus, autoscroll, morph reloads, history, or `frame-load`; canonical acknowledgement owns those effects and clears preview state. Focused package/example tests and a Maestro-only iPhone 17 Pro Simulator/Expo Go interaction pass. Android interaction, release builds, and physical devices remain pending.

**2026-07-23**:

- Changed: Routed exact same-document anchors through the existing Turbo Frame destination resolver when `data-turbo-frame` or an enclosing Frame default selects `_self`, `_parent`, `_top`, or a named Frame.
- Why: Same-current-Frame support still rejected target metadata even though ordinary links and forms already shared precise Frame fallback semantics.
- Impact: The anchor target must belong to the resolved document/Frame scope, Frame wrappers remain invalid, and no request/history write begins. Focused tests plus an iPhone Simulator document-to-named-Frame interaction pass; direct/cross-document Frame fragments, anchor actions/history, Android interaction, physical devices, and release builds remain pending.

**2026-07-23**:

- Changed: Admitted exact same-document fragment links whose source and ordinary application target share the same nearest mounted Frame, and made the Expo host measure root-owned targets against its registered document-content View.
- Why: Rejecting every Frame-contained fragment was stricter than Turbo, while local `onLayout.y` offsets are not document coordinates once the target is nested under Frame and card components.
- Impact: The package revalidates the same Frame scope and starts no Frame request or history write; an iPhone 17 Pro Simulator/Expo Go flow taps the Frame link and visibly reaches its target. Cross-Frame/targeted Frame anchors, direct Frame fragments, Android interaction, physical devices, and release builds remain pending.

**2026-07-23**:

- Changed: Extended the example host's later cross-document Expo Go anchor handoff to the explicitly declared nested target.
- Why: Once deferred nested anchors could reveal both owning viewports, the remaining later-event path only needed to preserve that target through the existing native-created route adoption.
- Impact: An iOS Simulator flow visibly starts on `/demo/linked`, receives a later raw `/demo#nested-native-anchor-target` URL, and reaches the nested target without another Router push or automation tap. Frame targets, other host codecs, Android interaction, physical devices, and release builds remain pending.

**2026-07-23**:

- Changed: Made deferred cold nested-anchor requests wait for both the gallery root and the explicitly declared nested `ScrollView`, then reveal the nested viewport before applying its inner target offset.
- Why: Inner target ownership alone could scroll content inside an offscreen nested viewport without bringing that viewport into the device window.
- Impact: A cold exact Expo Go fragment now visibly reaches the nested target without a tap, fetch, or history write. Focused tests and iOS Simulator evidence are green; cross-document nested targets, Frame targets, Android interaction, physical devices, and release builds remain pending.

**2026-07-23**:

- Changed: Added explicit nested `ScrollView` ownership to the Expo example's same-document anchor registry and placed a registered target inside the existing nested visibility region.
- Why: The package correctly delegates same-document anchors to the host, but the example previously mapped every admitted target to the gallery root even when native layout placed it inside a separately scrolling viewport.
- Impact: A descendant target now inherits its declared `DemoScrollRegion` container and scrolls only that inner viewport with no fetch or history write. Focused tests and iOS Simulator interaction are green; direct nested fragments, Frame targets, arbitrary container discovery, Android interaction, physical devices, and release builds remain pending.

**2026-07-23**:

- Changed: Extended the live Expo Go cross-document anchor handoff from the exact gallery route to the example's bounded nested, query-bearing catch-all route, and retained the deferred target through final root content sizing.
- Why: iOS delivers the raw URL to the outgoing screen while Expo Router creates the destination route; that screen could repair history but lose the fragment before the destination owner mounted, and an early native scroll could be clamped before the full document content size settled.
- Impact: The existing native route now receives the recovered anchor exactly once, preserves ordered repeated and empty query values, performs one canonical document GET, reaches the registered root target, and returns in one native Back step without another Router push. iOS Simulator evidence is green; Android interaction remains intentionally deferred to the final confirmation phase.

**2026-07-22**:

- Changed: The Expo example now adopts one later exact cross-document Expo Go root-anchor event through the route Expo Router has already created.
- Why: iOS can focus `/demo` from a live `/demo/linked` link before the host sees the raw fragment, leaving that transient native route without its managed history entry.
- Impact: The exact `/demo/linked` → `/demo#native-anchor-target` flow stamps the existing route once, restores canonical gallery XML, defers one root anchor, and retains one native Back step. iPhone Simulator evidence is green; Android interaction, arbitrary routes, Frame/nested anchors, physical devices, and release builds remain pending.

**2026-07-22**:

- Changed: Added a rooted anchor target to the standalone linked-document fixture and covered a cold raw Expo Go URL that reaches it after canonical document bootstrap.
- Why: The native bridge already validates a cold raw URL against the resolved catch-all route, so the example needed a real different-document target and regression/device evidence rather than claiming only same-document behavior.
- Impact: `exp://127.0.0.1:8081/--/demo/linked#linked-native-anchor-target` performs the normal guarded GET and history repair before one root-only anchor scroll. Later cross-document events, Frame/nested anchors, Android interaction, release builds, and physical devices remain outside this scope.

**2026-07-22**:

- Changed: Added the example-owned raw Expo Go URL-event bridge for an already running native gallery and preserved its current same-document Router metadata when Expo Router consumes that event.
- Why: Expo Router drops the fragment as expected, but on iOS it also replaces the active route's opaque history metadata before a later Expo Go event reaches the host.
- Impact: Only an exact `exp:`/`exps:` `/--/` URL for the active document's path and ordered query can scroll its registered root target. The bridge restores the existing entry in place, starts no document request or history transition, and has iPhone 17 Pro Simulator/Expo Go evidence. Cross-document/Frame/nested links, Android interaction, physical-device, and release-build confirmation remain pending.

**2026-07-22**:

- Changed: Added the standalone example's bounded native cold-link fragment bridge. A direct unmanaged Expo Go `exp:`/`exps:` URL whose `/--/` route and ordered query exactly match the focused route may supply one decoded root target ID through `expo-linking`.
- Why: Expo Router consumes the initial hash before route state reaches the bridge, while the raw Expo Go URL retains it.
- Impact: The example queues one root-anchor request until native layout is ready, with no document fetch, Router/history write, or managed-history adoption. An iPhone 17 Pro Simulator/Expo Go run visibly reaches `#native-anchor-target`; Frame/nested direct fragments, later live-link events, Android interaction, physical-device, and release-build evidence remain pending.

**2026-07-22**:

- Changed: Added bounded page-refresh semantics for a fragment-free, same-path explicit `replace` visit. The active XML root selects exact `data-turbo-refresh-method="morph"` and `data-turbo-refresh-scroll="preserve"`; absent or invalid settings fall back to replacement and root reset.
- Why: A native page-refresh-shaped visit needs an explicit, observable contract without claiming browser-wide visit/morph/scroll parity.
- Impact: Matching-path replaces bypass cached previews, use the canonical GET, retain compatible stable nodes only on successful morphs, and reset the registered root only after React acknowledgement unless the active root preserves scroll. Focused core/example tests and an iPhone 17 Pro Simulator/Expo Go flow are green; Android interaction remains the final parity confirmation.

**2026-07-22**:

- Changed: iOS Expo Go validation established that an incoming direct-link hash is not exposed in the Expo Router route state handed to the example bridge.
- Why: A cold `/demo#native-anchor-target` link reached the bridge as `/demo`, so a parser-only fragment implementation would falsely claim native support.
- Impact: Direct-query routing remains supported; direct-deep-link fragments remain pending a host URL handoff that exposes the hash. Android interaction stays a final parity confirmation.

**2026-07-22**:

- Changed: Added the optional post-focus `AutofocusScrollAdapter` and a root-only Expo gallery implementation with a dedicated offscreen document fixture.
- Why: Native autofocus alone can leave a valid field outside the visible root viewport, while generic container discovery or browser scroll semantics would overstate what React Native can guarantee.
- Impact: Focused package/example tests prove one focus-first adapter request, redacted failure behavior, delayed explicit measurement handling, and the exact root scroll calculation. An iPhone 17 Pro Simulator/Expo Go flow reveals the focused `Root autofocus scroll target`; Android interaction, nested/virtualized containers, release builds, and physical-device evidence remain pending.

**2026-07-22**:

- Changed: The optional Action Cable heartbeat monitor now records every valid server frame and replaces one socket after more than six seconds of silence.
- Why: A native socket can become stale without emitting a terminal close/error, so the standalone host needs a bounded recovery path that preserves the adapter's exact re-confirmation contract.
- Impact: The Expo/Rails demo opts in with its injected clock. A replacement that remains silent does not retry in a loop; generic lifecycle/network/backoff policy and Android interaction evidence remain separate work.

**2026-07-22**:

- Changed: The Action Cable adapter now keeps one injected 500 ms confirmation-guarantee timer for all active unconfirmed opaque identifiers.
- Why: Action Cable subscriptions must be re-sent until the server confirms them, without a later subscription delaying an earlier one.
- Impact: The timer releases on confirmation, rejection, final unsubscribe, socket detachment, or disposal. This does not add automatic socket reconnect, heartbeat, lifecycle, Android-interaction, release-build, or physical-device behavior.

**2026-07-22**:

- Changed: The manual release workflow now has a default candidate mode and a reviewer-gated stable-publication mode that consumes a successful frozen candidate run.
- Why: A stable release must publish the exact independently checked archives rather than silently rebuilding from source.
- Impact: Stable publication verifies the candidate's current-main commit, manifests, checksums, empty registry versions, downloaded registry bytes, and unused source tag before creating the GitHub release. npm and RubyGems trusted-publisher setup remains required; the first unscoped npm publication is still a human 2FA bootstrap.

**2026-07-22**:

- Changed: Added a bounded standalone Rails protected Action Cable panel and a public `expo_turbo_request` bridge for host credential callbacks.
- Why: Action Cable keeps its request private, while a host needs a supported way to read a native handshake credential without a query-string fallback.
- Impact: The example issues one no-store short-lived ticket, sends it only in an iOS native WebSocket header, admits the exact grant/token subscription, and visibly applies its Rails XML replacement in Expo Go. This is not production identity, revocation/rotation, lifecycle, Android-interaction, or physical-device evidence.

**2026-07-22**:

- Changed: Added a native-only standalone-Rails current-document Refresh morph panel. Its ordinary XML document has no protocol descendants; one Rails-authored Stream link returns a request-id-free `refresh method="morph"` before the injected controller performs the canonical document GET, while a second replaces a visible status node and echoes its initiating request ID in the Refresh Stream.
- Why: The core already supported bounded current-document refresh morph and recent-request suppression, but the public Rails demo had not exercised either exact server-to-native path.
- Impact: Controlled renderer and real-Rails smoke tests verify the canonical request/refresh sequence and the echoed-ID no-duplicate-GET sequence. An iPhone Simulator/Expo Go run showed the changed Rails timestamp while the compatible local counter remained `1`, then the echoed-ID suppression status. Android interaction, release-build, and physical-device confirmation remain later gates.

**2026-07-22**:

- Changed: Added an explicit Rails `422` Turbo Stream `replace method="morph"` action for the standalone native Frame form.
- Why: Ordinary matching XML validation correctly replaces the Frame, but a bounded Stream morph is the interoperable way to retain compatible component-owned native draft state while applying an authoritative Rails error.
- Impact: The Rails fixture deliberately renders an empty input value while the compatible mounted native input retains `invalid local draft` and displays `This demo name is unavailable`. Controlled, real-Rails, and iPhone Simulator/Expo Go checks prove that path; Android interaction, release-build, and physical-device confirmation remain later gates.

**2026-07-22**:

- Changed: Added a formatted standalone-Rails `replace method="morph"` Stream proof with a stateful native target.
- Why: Rails partial rendering naturally leaves indentation-only template text, which must not prevent a valid exact-root morph.
- Impact: The tree dispatcher now ignores that formatting text when admitting an outer Stream morph. Core, real-Rails, and iPhone Simulator/Expo Go tests prove the server-authored message updates while the mounted local counter remains at `1`; Android interaction remains a later gate.

**2026-07-22**:

- Changed: Added an example-owned two-plan native radio presentation through the core `select` descriptor to the standalone Rails Frame form.
- Why: The core's select successful-entry semantics needed a real native/Rails consumer rather than package-only snapshots.
- Impact: With neither radio selected, the form sends no `profile[plan]` and Rails returns matching `422` XML. Selecting `pro` sends exactly that one entry and follows the canonical `303` Frame GET. Focused renderer/Rails tests plus an iPhone Simulator/Expo Go flow cover omission → error → selected plan → reset. This remains a fixed app-owned radio presentation, not generic XML/DOM select discovery, a native picker, or complete device/accessibility evidence.

**2026-07-22**:

- Changed: Added a native consent `Switch` to the standalone Rails Frame form through the existing `checkable` descriptor.
- Why: The first consumer should demonstrate a real native successful-control omission/value contract, rather than proving it only in package fixtures.
- Impact: An unchecked switch sends no `profile[terms]` entry; a checked switch sends only `accepted`. Rails returns an authoritative matching `422` XML error for omission or an unexpected value, and a valid value follows the canonical `303` Frame GET. Focused renderer/Rails tests and an iPhone Simulator/Expo Go flow cover that exact sequence. This remains one example-owned native control, not XML/DOM discovery, a full browser control matrix, or complete device/accessibility evidence.

**2026-07-22**:

- Changed: Kept the Expo cache-backed `File` as the multipart Blob entry and added an explicit standalone-demo `upload-retry` submitter that renders a matching Rails `422` for a valid selected attachment.
- Why: Expo Go intentionally rejects the ArrayBuffer-backed Blob construction path, and a real selected-file retry needs a Rails-owned failure rather than a fake invalid picker result.
- Impact: An iPhone Simulator/Expo Go run downloads a 39-byte `text/plain` fixture through Safari into Files, selects it with the native picker, sends it as multipart, sees the matching `422` with the selected attachment still visible, then follows the normal `303` canonical GET. Rails receives `text/plain` with a safe cache-generated `.txt` filename; original provider display-name preservation is not claimed. Android interaction, release-build, physical-device, and broader host-upload evidence remain pending.

**2026-07-22**:

- Changed: Made rejected bounded multipart uploads render a matching Rails `422` Frame with an accessible file-control error, while the Expo example keeps the selected bounded attachment in document state for retry across that replacement.
- Why: A native user should not have to reopen Files after a server-side MIME, encoding, filename, or size rejection.
- Impact: Rails and controlled native renderer tests prove the authoritative error and retry use the same selected Blob. This is not generic morph-aware field preservation or iOS selected-file device proof; those remain explicit pending gates.

**2026-07-22**:

- Changed: Added an Expo example-owned system Files picker for the bounded multipart form control. It picks exactly one cached `text/plain` file, validates a safe filename and 1–64 KiB size, copies its bytes into the public host-owned Blob/file entry, and preserves the fallback Blob on cancellation.
- Why: The public multipart contract is deliberately platform-neutral, but the first consumer needs a concrete iOS-first picker boundary rather than a fixed fixture alone.
- Impact: The Rails demo now accepts and immediately discards bounded UTF-8 `text/plain` files with safe names instead of admitting one fixed byte sequence. Focused adapter/renderer/Rails tests pass, iOS export passes, and iPhone Simulator/Expo Go opens and cancels the native Files sheet. A full selected-file iOS round trip, Android interaction, release builds, physical devices, and broader host upload policies remain pending.

**2026-07-22**:

- Changed: Added bounded host-provided Blob/file entries to native form planning and a semantic `multipart/form-data` body that the Expo fetch adapter converts to `FormData` at the transport boundary.
- Why: The package needs a native upload contract without pretending React Native has browser `File`, DOM control discovery, or a generic picker.
- Impact: The standalone Rails form starts with a fixed `expo-turbo-upload.txt` text Blob, and an iPhone Simulator/Expo Go run proves the multipart POST, `303`, and canonical Frame GET. Android interaction, release-build, physical-device, full selected-file picker/URI proof, and broader host-upload-profile evidence remain later gates.

**2026-07-22**:

- Changed: Exercised the lazy gallery `preview-frame` and its registered Frame-boundary autoscroll in iPhone Simulator/Expo Go.
- Why: The adapter and renderer tests already covered cached geometry, alignment, and ordering, but the standalone native gallery had no direct iOS proof for that lazy Frame path.
- Impact: Bringing `preview-frame` into view produced its recursive source at the configured `start` alignment. This proves the simulator request path only; Android interaction, physical-scroll completion, release-build, and physical-device evidence remain later gates.

**2026-07-22**:

- Changed: Recorded an iPhone Simulator/Expo Go proof for the gallery's Frame-scoped native link and one-shot replacement autofocus.
- Why: Focused renderer/runtime tests already covered the shared Frame controller and exact focus order, but the standalone native Pressable needed an actual interaction check.
- Impact: The iOS flow activates the link, renders `Frame link loaded`, then accepts `Ada` in the returned `Autofocused Frame field` without a second tap. Android interaction, release-build, and physical-device evidence remain later gates.

**2026-07-22**:

- Changed: Added the standalone Rails form's deliberately constrained `text/plain` submitter and exercised it end to end in iPhone Simulator/Expo Go.
- Why: The public planner already has an exact string-only `text/plain` codec, but Rails must not pretend the browser format can be decoded generically or losslessly.
- Impact: The demo admits only UTF-8 `profile[first_name]` then `commit=save` with exact CRLF framing; `invalid` renders the matching `422` XML and `Ada` follows the canonical `303` Frame GET on iOS. Android interaction, release-build, physical-device, and broader host-profile evidence remain later gates.

**2026-07-22**:

- Changed: Exercised the standalone Expo Cable panel through a real iPhone Simulator/Expo Go Home-to-active transition, then delivered a Rails `replace` broadcast through the rebuilt source.
- Why: The example deliberately disposes live Cable state on iOS background and starts a fresh document/Frame/socket/subscription path on active; the focused renderer path needed simulator evidence.
- Impact: The support checklist now has iOS Simulator proof for that AppState boundary. Android interaction, release-build, authenticated-host, and physical-device evidence remain later gates.

**2026-07-22**:

- Changed: Added a buffered native WebSocket boundary for the standalone Expo Action Cable panel and verified the direct local Rails path in iPhone Simulator/Expo Go.
- Why: A native socket can emit `open`/`welcome` before the generic adapter has installed all listeners; the host boundary now retains that early event order until the adapter is ready.
- Impact: The checked Cable item has iOS evidence for the direct `welcome`/confirmation handshake and Rails `replace` broadcast. Android interaction, release-build, authenticated-host, and physical-device evidence remain later gates.

**2026-07-22**:

- Changed: Exercised the standalone Rails Frame-form panel end to end in iPhone Simulator/Expo Go: server-owned `422` validation, in-place `204 No Content`, and canonical `303 See Other` recovery.
- Why: The controlled renderer and desktop real-Rails smokes already covered the response matrix, but the native example had only demonstrated the `204` branch on iOS.
- Impact: The public support checklist now has native evidence that `204` deliberately keeps the mounted Frame and locally edited `Ada` value, while its no-tree response leaves the prior `422` error in place. The `303` GET is the replacement that clears the error and resets the input. Android interaction, release-build, and physical-device evidence remain later gates.

**2026-07-22**:

- Changed: Verified the standalone gallery's nested lazy-Frame scenario with a real inner `ScrollView` gesture in iPhone Simulator/Expo Go.
- Why: The visibility registry already had focused nested-clip tests, but the example had not demonstrated that its native `measureInWindow` clipping chain admitted the Frame after an actual nested scroll.
- Impact: Focused coverage keeps the Frame idle until nested visibility and limits it to one automatic load; the iPhone Simulator flow then visibly rendered `Nested lazy Frame loaded`. Android interaction, physical-device, and release-build confirmation remain later gates.

**2026-07-22**:

- Changed: Added a visible cached-history root-scroll checkpoint to the Expo gallery and deferred focused Router traversal reconciliation until the mounted document boundary is ready.
- Why: Native Back can notify Router state before the destination document renderer and root `ScrollView` exist, which would commit a cached restoration without applying its host-owned scroll position.
- Impact: Focused bridge/runtime tests cover the deferred handoff, cached tree restoration, zero extra document GETs, and the remounted root container. An iPhone Simulator Expo Go/Maestro flow now scrolls to the checkpoint, opens the linked document, taps native Back, and visibly returns to that position. Android interaction, physical-device, and release-build confirmation remain pending.

**2026-07-22**:

- Changed: Added an example-owned root-only adapter for the gallery's exact same-document `#native-anchor-target` link, plus a direct target component and focused adapter/runtime coverage.
- Why: The public anchor contract had strong core tests, but the standalone Expo consumer did not yet show how a host registers a direct target and maps the document root, document content, and target layout offsets into a native root `ScrollView`.
- Impact: The focused tests prove the request scrolls only the registered target and begins neither a fetch nor a Router-history write; an iOS Simulator/Expo Go flow visibly reaches that target. Android interaction, physical-device, and release-build confirmation remain later gates.

**2026-07-22**:

- Changed: Added a dedicated gallery fixture at `/demo/linked?refresh=scroll` with a visible one-shot Refresh Stream action, distinct canonical replacement XML, and a root-scroll reset after renderer acknowledgement. The delayed cached-preview fixture now also clears its timer when its request is aborted.
- Why: The package already covered refresh acknowledgement and request cancellation, but the standalone gallery did not give iOS a visible default-refresh/root-scroll scenario or prove that an aborted preview stopped its delayed fixture work.
- Impact: Focused gallery-runtime coverage proves the exact registered root `ScrollView` reset and a new iOS Simulator/Expo Go run shows the ready scenario, a manual scroll to the control, then the committed replacement at the top. Android interaction, physical-device, and release-build confirmation remain later gates.

**2026-07-22**:

- Changed: Added a gallery link at `/demo/linked?preview=automatic` that warms one XML snapshot through `data-turbo-preload`, renders it as a provisional native document, and then replaces it with an authoritative fixture response.
- Why: Cached-preview semantics already had core coverage, but the standalone native gallery offered no observable iOS path through automatic preload, provisional rendering, and canonical revalidation.
- Impact: The focused runtime test and iOS Simulator/Expo Go flow prove the bounded document-preview contract. Android interaction, physical-device, and release-build confirmation remain later gates.

**2026-07-22**:

- Changed: Added a visible query-bearing ordinary document link to the standalone Expo gallery. It advances to `/demo/linked?source=gallery&tag=a&tag=b&empty=` and the linked fixture identifies the repeated/empty query scenario.
- Why: The Router bridge already preserved managed query URLs in focused tests, but the native gallery had no direct iOS scenario for it.
- Impact: iOS Simulator/Expo Go can now confirm that the exact canonical URL reaches both the document session and opaque Router history metadata. Generic host route-query mapping, Android interaction, physical-device, and release-build evidence remain separate gates.

**2026-07-22**:

- Changed: Direct outer Frame reload-morph now retains eligible nested Frame wrappers/current children, ignores stale nested response payloads, and cascades each child reload only after the outer renderer acknowledgement and final identity/ancestor recheck.
- Why: Turbo's nested Frame refresh-morph behavior must preserve the mounted nested boundary instead of applying an outer response's stale child content, while still letting that child reconcile from its own canonical source.
- Impact: Core tests cover lifecycle-configured and lifecycle-free cascades; the standalone Rails `morph/outer` → `morph/inner` panel has an iOS Simulator/Expo Go proof. Android, physical-device, and release-build confirmation remain later gates.

**2026-07-21**:

- Changed: The standalone Rails document now carries one server-authored native `data-turbo-stream` link. Its GET response renders two ordered sibling Stream actions: a confined XML-partial update and an append.
- Why: The core already handled generated GET Stream links, but the public native harness had only fixture, Form, and Cable paths rather than an actual Rails HTTP Stream interaction.
- Impact: Controlled native rendering, Rails structural contracts, and the optional real-host smoke now prove the exact header/response path; host teardown stops an in-flight link before a late Stream can mutate the retired session. It does not add document navigation, unsafe links, auth, retries, or physical-device release evidence.

**2026-07-21**:

- Changed: The native standalone-Rails Cable panel now owns a bounded AppState rebootstrap: only `background` disposes its runtime, while a later `active` starts a fresh canonical document, eager Frame, and Action Cable subscription sequence.
- Why: A live native connection must not keep a socket or retry work alive while backgrounded, and the next foreground needs canonical Rails truth without changing the generic one-socket adapter's terminal/reconnect contract.
- Impact: The controlled Expo renderer test covers `inactive`, exactly one background teardown/restart, repeated active events, and late stale-runtime disposal. Credentials, heartbeat/network monitoring, automatic retry, Android-device confirmation, and physical-device release evidence remain separate work.

**2026-07-21**:

- Changed: The standalone Rails Cable demo now accepts one fixed local `?kind=refresh` control and injects its existing `DocumentRefreshController` into Cable Stream dispatch.
- Why: The package already implemented bounded ordinary Stream refresh, but the public native Cable demo still rejected that action instead of proving the canonical document-refresh contract.
- Impact: A delivered refresh debounces one whole-document GET without altering the distinct Frame-only reconnect path. Focused runtime, Rails Redis, and real WebSocket smoke checks cover the boundary; credentials, lifecycle policy, and physical-device evidence remain separate work.

**2026-07-21**:

- Changed: Added validated inherited XML `dir` context to the React renderer, including document and Frame boundaries, and exposed it as `useExpoTurboDirection()`.
- Why: Components and host boundaries needed one explicit native direction signal rather than inferring browser DOM or global device behavior.
- Impact: Expo's gallery now maps `ltr`/`rtl` to native layout direction and `auto` to host-native text direction. Hosts remain responsible for their component mapping; browser bidi/CSS parity and global `I18nManager` mutation are not claimed.

**2026-07-21**:

- Changed: Added an opt-in Rails protected Cable boundary with a gem-specific signed token, custom Channel, lazy host subject resolution, grant authorization callback, and isolated immediate/queued broadcast topics.
- Why: A standard Turbo signed stream name could be replayed through `Turbo::StreamsChannel`, bypassing a custom protected-channel authorizer. The isolated verifier and token topic keep public and protected subscriptions distinct.
- Impact: Hosts must configure credential extraction, subject resolution, authorization, and redacted callback-error reporting before rendering protected sources, then terminate affected subscriptions or connections after grant expiry/revocation. Native credential transport, grant policy, and device evidence remain release gates.

**2026-07-21**:

- Changed: Added an explicit standalone-Rails Frame-form submitter that returns `204 No Content` after validation without replacing the active Frame.
- Why: Empty successful form responses need real host proof that they clear pending state while preserving the mounted Frame and native input value.
- Impact: The controlled renderer and both real-Rails desktop form smokes now distinguish `422`, `204`, and `303`; a focused iOS Simulator run also confirms the real `204` preserves the mounted Frame/input. Release-build and physical-device evidence remain separate gates.

- Changed: Wired the standalone Rails Cable panel to `FrameReconnectReconciler`; its sole public Cable source now belongs to eager `demo-frame`, and the desktop smoke mounts that Frame before subscribing.
- Why: A Frame-scoped source must reconcile through its own canonical Frame GET after a confirmed server-directed reconnect, without silently broadening recovery to the document.
- Impact: The panel proves Frame-only recovery and keeps document-level recovery as a separate explicit case. Physical-device Frame/Cable evidence, authentication, and lifecycle policy remain release gates.

**2026-07-21**:

- Changed: Added source-aware `FrameReconnectReconciler` recovery for confirmed Action Cable reconnects, plus the exact mounted Frame lookup needed to reuse existing Frame request ownership.
- Why: A reconnecting Cable source inside a Frame needs canonical Frame truth, not a root-document request or a new controller inferred from an ID.
- Impact: The registry now passes frozen logical source keys with each recovery handoff. A host may opt into exact source/frame reconciliation; document-level sources still use document refresh, while active outermost mounted Frames reload serially only after document/Frame work settles. The public Rails Cable panel now adopts this Frame path; the physical-device matrix remains pending.

**2026-07-21**:

- Changed: Added `DocumentReconnectReconciler`, an opt-in wrapper for `DocumentRefreshController` used as `CableStreamSourceRegistry.reconnectRefresh`.
- Why: A server-directed Cable reconnect can complete while an owning document visit is still in flight; dropping that one recovery request unnecessarily leaves the newly settled current document without canonical reconciliation.
- Impact: The wrapper retains only the latest recovery request until the visit sequence next becomes idle, then lets `DocumentRefreshController` perform its existing URL-ownership and debounce checks. A changed document, disposal, stale ownership, or a still-active visit starts no extra GET. Ordinary Stream `refresh`, replay/retry policy, and Frame-local reconciliation remain separate.

- Changed: Added opt-in current-document reconciliation for a confirmed server-directed Action Cable reconnect. `CableStreamSourceRegistry` now sends one frozen scroll-preserving request to its host-injected `reconnectRefresh` requester after the first active `connected(true)` callback in a reconnect cycle.
- Why: A subscriber can miss XML broadcasts while a server-directed socket rotation is in progress; a bounded canonical GET is the safe way to converge an idle current document without inventing replay or retry semantics.
- Impact: Initial connections, terminal disconnects, stale/disposed transports, and ordinary Turbo Stream `refresh` stayed separate. This initial idle-only handoff was later extended by `DocumentReconnectReconciler`; Frame-aware reconciliation remains open work.

- Changed: The injected Action Cable adapter now honors a valid server `disconnect` with `reconnect: true` by rotating its socket and resubscribing its exact active identifiers after the replacement `welcome`.
- Why: Action Cable can explicitly direct clients to reconnect during a server restart; treating that frame like an ordinary terminal close broke the protocol's re-confirmation path.
- Impact: Confirmed records enter `reconnecting` and receive `connected(true)` only after the replacement confirmation; unconfirmed records receive `connected(false)`. Close/error, malformed frames, unsupported protocols, `reconnect: false`, command failures, credentials, timers, lifecycle integration, and automatic retry remain out of scope.

- Changed: Added a separate opt-in native-only Rails Frame-form panel to the Expo gallery. It loads the canonical form, renders authoritative validation XML, and follows the successful redirect back to the canonical Frame URL.
- Why: The prior Rails form evidence was desktop-only and intentionally kept out of the static Action Cable proof.
- Impact: The app now has a bounded interactive Rails form path alongside the static anonymous Cable proof. Renderer and desktop checks cover it, but CORS, device networking, release builds, and physical-device evidence remain open.

- Changed: Added a provider-rendered desktop smoke for the real standalone-Rails Frame form. It mounts `ExpoTurboProvider`/`ExpoTurboRoot`, drives the rendered `TextInput` and submitter through the canonical Rails request, renders the server-owned `422` error, then follows the `303` response and verifies the replacement input resets.
- Why: The existing core smoke proved the request/response controller but not React registration, rendered control activation, or Frame replacement.
- Impact: CI now covers both public form layers against Rails without placing the form in the deliberately static live-Cable panel. It remains desktop-only and is not native-device evidence.

- Changed: Added a desktop CI smoke for a real standalone-Rails Frame form: canonical GET, server-rendered matching `422` XML, and an adapter-followed `303` back to the canonical Frame URL.
- Why: Fixture and core tests did not prove the public form controller against an actual Rails request/response boundary.
- Impact: CI now verifies the URL-encoded request contract and server-authoritative validation/recovery on Bun. This does not wire the static live-Cable gallery to the form or provide native-device evidence.

- Changed: Added a desktop CI smoke from the public Cable registry and WebSocket adapter through the standalone Rails `/cable` endpoint and Redis-backed XML Stream broadcast.
- Why: The previous controlled adapter and Rails Redis checks did not prove their real endpoint-to-client chain together.
- Impact: CI now verifies `welcome`, confirmation, one XML replacement, an exact outbound unsubscribe command, and socket closure on Bun. This is not native-device, authentication, reconnect, or protected-channel evidence.

- Changed: The standalone Rails demo now uses Redis-backed Action Cable in development and tests its public Expo XML Stream broadcast through a real subscriber.
- Why: The existing in-memory adapter could not verify broker delivery or namespace isolation.
- Impact: Rails demo checks now require Redis; CI supplies a local Redis service. This remains anonymous and excludes auth, protected channels, reconnect, and device evidence.

## License

MIT. See [LICENSE](./LICENSE) and [NOTICE.md](./NOTICE.md).

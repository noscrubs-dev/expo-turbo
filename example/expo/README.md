# Expo example

This private Expo SDK 57 application is the standalone native consumer and future compatibility gallery for `expo-turbo`. It owns its manifest and Bun lockfile, is not part of a package-manager workspace, and resolves the public package root through `file:../..`.

The Rails-backed nested Frame morph panel exposes automatic direct `reload()` morphing, a normal Frame visit whose stable `before-frame-render` listener selects the package-owned bounded morph renderer, and an explicitly paused ordinary visit. The paused path keeps the prior outer/nested content visible until the host resumes that exact admitted response, after which it commits and runs the acknowledged nested canonical reload cascade.

`.maestro/live-frame-morph.yaml` drives that ordinary visit through Expo Go on
iOS and asserts both visible Rails responses. It deliberately reacquires the
control after dismissing the gallery's initial form autofocus so long-scroll
layout changes cannot turn a stale coordinate into a false interaction.

The app-lifetime Expo Turbo runtime/provider lives above the Expo Router Stack. `/` replace-redirects to `/demo` without attaching history, and only the focused `[...expoTurboPath]` route attaches the bridge and renders the document root. Its example-owned codec accepts canonical `https://example.test` paths rooted at `/demo` as one through eight safe catch-all segments (each at most 96 characters), rather than a fixed route list. It rejects wrong origins/roots, credentials, fragments, malformed or noncanonical encoding, trailing/empty/dot/control segments, and literal or encoded separators. Static export intentionally includes `/demo`, `/demo/linked`, and the linked generic-route proof `/demo/routes/ios-proof/details`. A complete bridge-managed history entry may supply an exact canonical fragment-free query URL: its full document URL is authoritative in opaque, versioned metadata while the catch-all validates the path, preserving query order, duplicates, empty values, `+`, and percent encoding through managed Router history. The `expoTurboPath` catch-all and three `__expo_turbo_*` fields are reserved; non-reserved external Expo Router query parameters remain host-owned and do not map to document queries. The bridge records the catch-all path plus three namespaced restoration params, rejects malformed, unencoded, noncanonical, or path-mismatched managed metadata, pushes canonical-path advances, replaces focused params in place, rolls back failed writes to an exactly verified captured Router state, and translates pop/restored-state notifications into package traversal restoration. A same-document managed cold start adopts without fetching or writing. A different-document managed cold start gates rendering behind one root-visitable, fragment-free exact GET and commits the exact `2xx`/`4xx`/`5xx` XML tree with managed history without a Router write. An unmanaged direct nested route performs the same guarded GET and initializes history plus same-key route metadata inside the tree-commit boundary. Redirects, empty/wrong-MIME/failed responses, stale route state, or focus loss cannot adopt stale tree/history and remain retryable. The provider also supplies a `DocumentPreloader` backed by the controller's exact snapshot cache, so a rendered semantic link carrying `data-turbo-preload` can feed the next ordinary preview visit without parallel cache state. The gallery now provides one dedicated cached-preview scenario at `/demo/linked?preview=automatic`: it renders the warmed XML snapshot before an intentionally delayed canonical fixture response replaces it. Its `/demo/linked?refresh=scroll` scenario begins with a long ready document; one explicit default Refresh Stream consumes a distinct canonical fixture response and, after React acknowledges it, resets only the owning gallery `ScrollView`. Its exact same-document `#native-anchor-target` fixture registers one direct target, combines the document root, document content, and target layout offsets, and asks only that root `ScrollView` to scroll without a fetch or Router-history write. Its cold `/demo/linked#linked-native-anchor-target` fixture performs the normal guarded document bootstrap before that target is deferred to the same root-only adapter. Its `/demo/linked?history=scroll` fixture captures the gallery root's finite position before the link, then defers native Back reconciliation until the destination document boundary has mounted the renderer and root container; the cached return restores that root position without another document GET. This proves generic nested path mapping, managed query history, reload/back/forward restoration, cached-preview transition, one iOS Simulator root-scroll reset, same-document initial/later Expo Go anchor interaction, a cold linked-document Expo Go anchor, and cached-history root-scroll restoration in iOS Simulator/Expo Go. The direct-query path and bounded native Expo Go root-fragment handoff are now covered; Frame/nested direct fragments, later cross-document events, Android interaction, physical-device, and release-build evidence remain later work.

The gallery's `/demo/linked?replace=morph` fixture proves the deliberately narrow page-refresh path. Its active root carries `data-turbo-refresh-method="morph"`; a lower explicit `data-turbo-action="replace"` link changes only the query to `revision=next`, so the source and destination keep the `/demo/linked` pathname and no fragment. The controller skips a cached preview, performs the canonical GET, morphs only a successful compatible response, replaces the focused native history entry, and resets the registered root `ScrollView` only after React acknowledges the committed tree. Exact root `data-turbo-refresh-scroll="preserve"` would suppress that reset; absent, invalid, differently cased, whitespace-padded, or nested metadata defaults to replacement/reset. An iPhone 17 Pro Simulator/Expo Go flow scrolls to the lower control, commits the morph, and visibly returns to `Same-path replace morph committed` at the root. This does not claim fragments, different-path replaces, `advance`/`restore`, Frames, nested scroll containers, Android interaction, physical-device, or release-build behavior.

The direct-query portion of that remaining-work note is now closed for the example route: a rooted, fragment-free direct `route.path` must exactly match the catch-all path, contributes ordered query names/duplicates, and must agree with its non-reserved parsed Router params before the bridge admits a Router-canonical query URL for the guarded cold-start GET and metadata repair. The iPhone 17 Pro Simulator/Expo Go proof opens `/demo/routes/ios-proof/details?source=direct&tag=a&tag=b&empty=&plus=+&encoded=%20`; repeated keys and empty values retain their Router-visible semantics, while the router may normalize the original query encoding. Expo Router omits a cold-link hash from route state, so the native screen reads `expo-linking`'s raw initial URL instead. On cold launch, an unmanaged exact `exp:` or `exps:` Expo Go URL with a `/--/` path and ordered query matching that route may contribute one nonblank, control-free decoded target ID; a different document follows the existing guarded canonical bootstrap before the root-only anchor waits for layout. After launch, the native host restores exact same-document metadata before accepting an anchor and supports the bounded cross-document catch-all handoff described below. An iPhone 17 Pro Simulator/Expo Go run opens `exp://127.0.0.1:8081/--/demo#native-anchor-target` both cold and while the gallery is already running, and cold-opens `exp://127.0.0.1:8081/--/demo/linked#linked-native-anchor-target`, visibly reaching each target. Frame/nested direct fragments, other host route codecs, Android interaction, physical-device, and release-build evidence remain later work.

That later cross-document limitation is now narrowed to the example host's bounded catch-all codec. When an already-running `/demo/linked` receives raw `exp://127.0.0.1:8081/--/demo#native-anchor-target`, `exp://127.0.0.1:8081/--/demo#nested-native-anchor-target`, or `exp://127.0.0.1:8081/--/demo/routes/ios-proof/details?source=gallery&tag=a&tag=b&empty=#generic-native-anchor-target`, Expo Router may create a destination route without the bridge metadata. The bridge validates the exact raw path, ordered query, and nonblank fragment, assigns a fresh managed history entry to the existing route, and transfers the recovered anchor once to the destination owner. Normal traversal loads canonical XML before the adapter retries after final layout; no additional Router route is created. iPhone 17 Pro Simulator/Expo Go runs reach all three targets, with one Back step also proven for the generic route. Other host codecs, Frame anchors, Android interaction, physical-device, and release-build evidence remain later work.

Nested scrolling is separately bounded and explicit. `DemoScrollRegion` registers its own native `ScrollView` with the example anchor registry and provides that container identity to descendant `DemoAnchorTarget` components. The visible link at the top of `nested-scroll-region` targets `#nested-native-anchor-target`; activation still uses the package's ordinary synchronous same-document anchor request, starts no fetch or history write, and scrolls only the inner viewport. A cold or later cross-document exact request waits for both root and inner owners, measures the nested viewport into the root's window, reveals it, and then applies the inner target offset. Focused registry/runtime tests plus no-tap cold and `/demo/linked` → nested-target iPhone 17 Pro Simulator/Expo Go interactions pass. This is not Frame target ownership, arbitrary container discovery, Android interaction, physical-device, or release-build evidence.

The mounted `link-frame` also contains an exact `#frame-native-anchor-target` link and ordinary application target in that same nearest Frame. A second top-level link selects that target through `data-turbo-frame="link-frame"`. Activation stays in the package's semantic-link path, resolves `_self`/`_parent`/`_top`/named/default Frame targeting through the shared destination policy, revalidates both nodes against the resolved scope, starts no request or history write, and asks the example root adapter to reveal the target. Because a nested target's local `onLayout.y` is not a document coordinate, `DemoAnchorTarget` uses native `measureLayout` against the registered document-content View before scrolling. Focused renderer/runtime tests and iPhone 17 Pro Simulator/Expo Go taps visibly reach `Frame native anchor target` from both links. Frame-wrapper or mismatched targets still fail closed; direct/cross-document Frame fragments, anchor actions/history, Android interaction, physical devices, and release builds remain pending.

The gallery also exposes `/demo/linked?autofocus=scroll` as a bounded root-autofocus proof. It puts one `DemoFormInput` carrying raw `autofocus` below explicit ordinary spacer cards; after the renderer focuses that input, the example-owned `AutofocusScrollAdapter` compares only the registered input and root `ScrollView` window rectangles, uses nearest-edge nonanimated scrolling, and may retain that one host request until those measurements arrive. It does not discover nested containers, route virtualizer rows, or report physical-scroll completion. An iPhone 17 Pro Simulator/Expo Go flow opens the gallery link and shows the focused `Root autofocus scroll target` with its caret visible. Android interaction, release-build, and physical-device evidence remain later work.

The focused route waits for the root navigation container's ready signal before attaching the synchronous bridge, including an already-ready microtask handoff. That explicit hydration boundary keeps a production static export from treating the initial same-key `setParams` as a no-op. When native Back changes focus, the outgoing owner ignores that state event; the replacement owner attaches deferred and lets its rendered document boundary arm reconciliation only after the renderer and gallery root `ScrollView` are live. The gallery's ordinary advance link intentionally targets `/demo/linked?source=gallery&tag=a&tag=b&empty=` so iOS can visibly exercise repeated and empty values through the opaque managed-history URL; its separate replace link keeps the real Expo Router `push` and `setParams` pathname writes device-testable.

The current screen renders a small registered native component tree from XML and includes an editable native form with app-owned Zod constraint snapshots, exact `TextInput` focus handles with user-driven focus/blur tracking, a host-owned Alert confirmation, registered terminal recovery boundary, host-owned static document-visit and terminal-form announcements, interactive document links, a Frame-scoped link, a Stream update, and programmatic Frame visits. Its direction card demonstrates inherited XML `dir`: an outer right-to-left card, nested left-to-right override, and a host-native `auto` reset. The example maps `ltr`/`rtl` to `View.direction` and text/input values to `writingDirection`; it does not change global `I18nManager`, `flexDirection`, or `textAlign`. The app owns one form submission lifecycle shared by rendered forms, retries, and generated-form links; a focused rendered-form test proves its exact start/end pairing around the fixture transport. It also owns one Stream lifecycle shared by direct gallery actions, refresh, Frames, and form responses; the focused runtime test proves direct `before-stream-render` → `stream-action` ordering. The initial whole-document generation focuses the stable-ID first-name input once after its native handle mounts. Clear that required value and submit to see the ordinary invalid report block confirmation, pending state, and fetch while focusing the first invalid logical control; restore a value to continue. The valid form confirms its immutable proposal before busy/submitter state and the fixture request begin. Its first safe GET intentionally returns the wrong MIME type; the form boundary exposes the redacted terminal failure and an explicit retry that uses a fresh request ID, the exact submitter, and current edited values before the fixture succeeds with `204`. The host maps only a document visit's redacted status and the form's structured redacted terminal result to static copy. On iOS, ordinary transitions use queued low-priority `AccessibilityInfo` delivery and failures use default-priority delivery; web keeps polite and assertive live regions mounted before changing their text. Android has the basic native path but no interaction evidence yet. Plain same-origin links use the fixture document controller; the Frame-scoped link reuses the exact mounted fixture Frame registry and returns a stable-ID `autofocus` field, demonstrating the separate one-shot Frame replacement focus contract after that native input mounts; and root-external or safe cross-origin links demonstrate the app-owned navigation adapter shared by the provider and Frame registry. Focused runtime tests prove those two claims occur in order—initial document, then replacement Frame—without Strict Mode duplication. An iPhone Simulator/Expo Go flow now also activates that link, renders `Frame link loaded`, and types `Ada` in the returned `Autofocused Frame field` without a second tap. The registry also receives the shared document controller so `_top` and promoted Frame visits use the same root policy. Its fixture-backed lazy Frames use non-collapsible native window measurement against the root and registered nested `ScrollView` clipping regions, plus one horizontal `FlatList` whose explicit Frame-ID rows require both positive clipping geometry and a current native full viewability snapshot. Focused tests cover root/nested/FlatList buffered-row suppression, one-time appearance loading, stale keys/indexes, replacement, and cleanup; iPhone Simulator/Expo Go flows now scroll the nested region to visibly load its Frame and bring `preview-frame` into view to render its recursive source at the registered `start` alignment. The latter proves the simulator request path only, not physical-scroll completion. Android interaction, arbitrary virtualizers, and physical-device/release-build focus/accessibility evidence remain later gates. The renderer also exercises stable node subscriptions plus error containment. Inspector controls remain later work. Optional native-only standalone-Rails Cable and Frame-form proofs are described below; authenticated, lifecycle-managed, and device-validated real-server modes remain later work.

Web uses the browser's settling confirmation dialog because react-native-web's `Alert` is a no-op. On iOS and Android, aborting a pending native Alert prevents any late answer from sending or reviving a request, but the platform Alert API cannot dismiss an already-visible dialog programmatically. A host that requires visual dismissal on navigation or supersession should inject a cancellable Modal-based adapter; that physical presentation/accessibility evidence remains unchecked.

The anonymous Cable panel's AppState behavior is intentionally one bounded demo-host policy rather than a generic lifecycle manager. It opts into the adapter's one-replacement stale monitor with the same injected clock. An iPhone Simulator/Expo Go run stays connected beyond the six-second monitor window and applies a live Rails replacement, confirming normal Action Cable pings do not falsely trigger recovery; deterministic tests cover the simulated stale branch. It has no auth, network monitoring, generic retry/backoff, release-build, or physical-device proof. Broader lifecycle-managed real-server behavior remains later work.

## Run

```sh
cd ../..
bun install --frozen-lockfile
bun run build
cd example/expo
bun install --frozen-lockfile
bun run check
bun run start
```

`bun run check` runs lint, types, fixture tests, and static web, iOS, and Android exports. The individual `export:web`, `export:ios`, and `export:android` scripts are available when one platform needs a focused bundle-admission check. Those native exports produce Hermes bytecode and catch Metro/runtime dependency regressions; they are not release-build, simulator, or physical-device evidence.

## Optional native Rails proofs

With the standalone Rails host running, set the origin before starting Expo:

```sh
EXPO_PUBLIC_EXPO_TURBO_DEMO_ORIGIN=http://<host>:3001 bun run start
```

The opt-in Rails panels are not rendered on web because the standalone Rails host intentionally has no CORS/origin policy. On native, the anonymous Action Cable panel uses `GET /api/expo_turbo/demo/document`, whose eager `turbo-frame#demo-frame` loads `GET /api/expo_turbo/demo/frame`; that Frame owns the panel's only public Cable source. The Rails-authored document contains one `data-turbo-stream` link that negotiates `GET /api/expo_turbo/demo/stream` and applies ordered update/append XML siblings, plus `?mode=morph`, which returns a normally newline-formatted Rails partial as `replace method="morph"`. That latter path preserves the exact `DemoStreamMorphProbe` root and its local counter while replacing the server message. Focused core/renderer tests, a real-Rails smoke, and an iPhone Simulator/Expo Go run cover it; that simulator run stays connected beyond the six-second monitor window and receives a live Rails replacement, proving ordinary server pings do not falsely trigger recovery. Android interaction, release-build, and physical-device confirmation remain later work. The same-origin `/cable` endpoint and local-only `POST /api/expo_turbo/demo/broadcast` deliver a fixed Expo XML `replace` or `?kind=refresh` control. Ordinary `refresh` Streams use the panel's injected `DocumentRefreshController`, debounce, and issue one canonical whole-document GET; they are intentionally distinct from bounded socket replacement reconciliation. The injected adapter honors an explicit Action Cable `disconnect` with `reconnect: true` or an opt-in stale-socket replacement by re-confirming the active Frame-scoped source, then uses `FrameReconnectReconciler` to issue one scroll-preserving canonical GET for the active outermost Frame—not a document GET. This example host treats only AppState `background` as destructive: it stops any in-flight generated Stream link before disposing the panel runtime, leaves iOS `inactive` alone, and creates a fresh document/Frame/socket/subscription path on later `active`; it has no user document navigation, server-owned Frame form, generic Frame navigation, auth, network monitoring, or generic client retry/backoff. The fixture deliberately has no document-level source; a document-level source would instead take document recovery. This remains bounded Stream/reconnect proof, not a general navigation surface or physical-device evidence.

The separate **Header-ticket Action Cable proof** fetches `GET /api/expo_turbo/demo/protected_ticket` with `Cache-Control: no-store`, opens the same-origin Cable endpoint with that short-lived value only in `X-Expo-Turbo-Demo-Ticket`, and loads a protected document/Frame containing the exact protected grant and opaque stream token. It never puts the ticket in the Cable URL or broadcast request. Rails resolves the header-derived demo subject, admits only that source's grant/token pair, and the local protected broadcast replaces the visible XML node. Focused Expo/Rails tests plus an iPhone Simulator/Expo Go interaction cover this bounded flow. It uses the same bounded stale monitor as the anonymous panel, not a production identity, ticket rotation/revocation, network/AppState/retry policy, Android-interaction, release-build, or physical-device claim.

A separate native-only Rails current-document panel loads `GET /api/expo_turbo/demo/refresh_morph_document`, an ordinary application-root document with no Frame, Stream, or Cable-source descendants. Its first Rails-authored link negotiates `GET /api/expo_turbo/demo/stream?mode=refresh-morph`; the standard request-id-free `refresh method="morph"` response debounces one canonical document GET. Its second link negotiates `?mode=refresh-morph-originating`: Rails first replaces a visible status node, then echoes the link's originating request ID in the Refresh Stream, so the controller suppresses a duplicate canonical GET. The Rails timestamp changes while the compatible `DemoStreamMorphProbe` counter remains mounted. Controlled renderer, real-Rails smoke, and an iPhone Simulator/Expo Go flow cover both bounded document-root paths. Android interaction, release-build, and physical-device evidence remain later work.

A separate native-only Frame-form panel uses the configured Rails origin to load `GET /api/expo_turbo/demo/form`, submit URL-encoded values to its Frame, render Rails-owned matching `422` XML, retain the mounted Frame and edited input after a successful `204`, and follow the `303` back to the canonical Frame URL. Its explicit `Save name and preserve local draft` action instead receives a standard `422` Stream `replace method="morph"` for the compatible form root: the Rails fixture deliberately carries a blank input `value`, while the mounted native input retains the user's local draft and renders the Rails validation error. A focused renderer test, real-Rails smoke, and iPhone Simulator/Expo Go flow cover that bounded path; ordinary matching `422` XML still replaces the Frame and owns its rendered values. It also exposes one deliberately constrained `text/plain` submitter: only UTF-8 `profile[first_name]` followed by `commit=save` with exact CRLF framing is admitted by the standalone Rails host, not a generic Rails parameter decoder or body-based method override. Its native consent `Switch` uses the existing `checkable` descriptor: unchecked sends no `profile[terms]`, checked sends `profile[terms]=accepted`, and Rails returns matching `422` XML for omission or an unexpected value before a valid submission follows the canonical `303` Frame GET. Its adjacent fixed two-choice plan control uses the core `select` descriptor behind native radio controls: no choice contributes no `profile[plan]`; `Pro` contributes only `profile[plan]=pro`; Rails returns matching `422` XML for omission or an unsupported value. A focused iPhone Simulator/Expo Go flow covers each real omission → matching `422` → accepted selection → canonical reset sequence. This is app-owned radio presentation, not generic XML/DOM select discovery or a native platform picker. The Files control keeps one selected bounded attachment in document state. Its explicit demo `Validate selected attachment (return 422)` submitter sends a valid selected file to the Rails-owned retry response; the matching `422` XML leaves that same attachment ready for the normal upload retry. Its controlled renderer test and separate Bun desktop provider/root smoke cover those request/response paths and replacement-input reset. An iPhone Simulator/Expo Go flow downloads a provider-managed 39-byte `text/plain` fixture through Safari into Files, selects it with the native picker, sends the cache-backed Expo `File` as multipart, sees the matching retry `422` while the displayed selection remains, then follows the normal `303` canonical GET. Rails receives an `UploadedFile` with `text/plain` and a safe cache-generated `.txt` filename; it does not claim to preserve the provider display name in multipart. CORS/origin-policy, device-network, auth/ticket, generic reconnect/AppState/network policy, Android interaction, release-build, and physical-device evidence remain later work.

A separate native-only nested-morph panel reloads `GET /api/expo_turbo/demo/morph/outer`; the outer response deliberately carries stale nested content, which the mounted `morph-inner` Frame ignores before its own `GET /api/expo_turbo/demo/morph/inner` reload. The focused renderer test and iOS Simulator/Expo Go proof cover that renderer-acknowledged cascade. Android, physical-device, and release-build evidence remain later work.

The example install links its React into the local `file:../..` source checkout so the package and native app resolve one peer instance. Published package consumers use normal peer-dependency resolution and do not need this source-development link.

Start with Expo Go where supported. Release builds and physical iOS/Android evidence remain required before compatibility can be claimed.

# Rails example

This directory will contain the standalone Rails and Action Cable demo host after the public source bridge lands. It intentionally has no Gemfile, routes, or runtime implementation in the scaffold commit.

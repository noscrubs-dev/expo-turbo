# Changelog

All notable public package, gem, and protocol changes will be recorded here.

## Unreleased

- Add native Frame-boundary autoscroll through an optional synchronous `FrameAutoscrollAdapter`. Controller-owned matching Frame GETs and matching Frame-target form XML now freeze one mounted-Frame render effect when either wrapper has raw `autoscroll` presence; mounted `data-autoscroll-block` (`start`/`center`/`end`/`nearest`, default `end`) and `data-autoscroll-behavior` (`smooth` or `auto`) control it. React invokes the exact registered boundary after mounted children/embedded Streams and before Frame autofocus/terminal lifecycle work; absent or unavailable anchors consume a no-op, while invalid adapter contracts are redacted without rolling back committed Frame children. The standalone Expo gallery supplies a cached-geometry root-`ScrollView`/non-collapsible-Frame implementation. Arbitrary-node discovery, standalone Stream/morph scrolling, and physical-device evidence remain pending.
- Align renderer-acknowledged native Frame completion and optional `FrameLifecycle` notifications with the Turbo ordering boundary. Every mounted controller-owned matching Frame GET now waits for React children/embedded Streams and the autofocus attempt before the controller publishes `busy: false` / `complete: true`, then dispatches frozen `frame-render`, rechecks the exact ticket and controller epoch, and dispatches frozen `frame-load`. A listener-triggered reload, replacement, disposal, supersession, or failed acknowledgement suppresses stale old notifications; headless and nonreplacement terminal paths remain immediate. Applied Frame-target form XML keeps its renderer-acknowledged public render/load order without extending Frame-controller busy/complete ownership to forms. Browser DOM attributes and physical-paint/repaint timing, `before-frame-render`, and morph remain pending.
- Add renderer-acknowledged native owning-document `render` and `load` notifications to `DocumentVisitLifecycle`. A mounted `ExpoTurboRoot` acknowledges the exact committed document generation only at the current revision after its response-owned revision is sealed, dispatches frozen `render` after the React tree or root error fallback commits and before document autofocus, and lets the originating visit or form settle only after acknowledgement. Cached previews render before canonical fetch but do not load; authoritative cache/network/refresh/traversal documents render, publish completed/failed state, then load, including committed GET `4xx`/`5xx` documents. Document-targeting forms include embedded Stream mutations in the seal, render success/error documents, and load only successful documents after activity and terminal cleanup. Strict Mode replay, observer-induced revision changes, superseded generations, final renderer disposal, and headless sessions cannot duplicate or deadlock. Explicit post-commit controller cancellation releases its visual wait without relabeling installed completed/failed truth or consuming response autofocus. Document reload, Frame morph, browser repaint/timing detail, and device evidence remain pending.
- Add Turbo 8.0.23-compatible automatic Frame response promotion when the exact sole XML root declares `data-turbo-visit-control="reload"`. Direct and bounded-recurse Frame GETs plus Frame-targeted forms detect the marker before matching-Frame extraction or embedded Stream application, release the exact request lease, and pass the exact buffered `2xx`/`4xx`/`5xx` response to the shared trusted visitor as an owning-document `advance` with reason `visit-control-reload`. Promotion mutates no Frame children, applies no autofocus or Frame history, emits no `frame-missing`, waits for visitor completion before terminal settlement, and fails closed on absent or rejected visitors. Failed cross-target forms transfer to the origin Frame first; primary direct and form responses retain canonical `src` publication, while intermediary recurse responses do not retarget it. Exact ownership checks prevent stale or superseded work from promoting.
- Add an optional shared native `FrameLifecycle` for Turbo 8.0.23 `frame-missing` semantics across direct/recurse Frame GETs and Frame-targeted XML form responses. Eligible missing matches emit one frozen cancellable event with final primary-response metadata and no public body/headers; default handling preserves `FrameMissingError`, while prevention returns explicit non-applied `prevented` truth. Failed cross-target forms transfer to the exact origin Frame before dispatch, and the package path performs no tree mutation before the optional visitor. The first controlled `visit()` intent runs through an optional trusted buffered-response visitor only after exact ownership is rechecked and the Frame/form lease is released, enabling host promotion without a response refetch. Matching Frames, primary `204`, negotiated Turbo Stream responses, MIME/parse failures, loop/depth errors, and work stale before dispatch emit nothing; listener-side supersession cancels continuation and visitor execution. Exhausted recurse, including recurse `204`, emits once for the primary response. DOM bubbling, browser missing-content markup, raw `Response` exposure, and renderer-dependent complete/render/load timing remain pending or native-inapplicable.
- Add a native `before-cache` equivalent to `DocumentVisitLifecycle` for ordinary cacheable outgoing-document capture during network visits, cached previews, explicit cached restores, cached or network host traversal, and successful safe-GET document forms. The frozen notification runs outside the destination commit guard so synchronous listener mutations are included, then ownership/history/tree truth is rechecked before guarded snapshot/history commitment and replacement-tree application. Initially `no-cache` documents, refresh, preload/manual capture, unsafe/failed/empty/Stream form responses, and promoted-Frame snapshots emit nothing. Browser next-task clone timing and renderer-dependent promoted-Frame ordering remain pending.
- Add Turbo 8.0.23-compatible semantic-link `click` to the existing `DocumentVisitLifecycle`. Exact-live enabled supported root-visitable plain links emit one frozen cancellable `{ nodeKey, url }` event before document or Frame ownership; prevention returns frozen native no-work evidence without allocating request/history/navigation state. Generated-form links, forms, opt-outs, delegated root-external/cross-origin/extension destinations, external schemes, disabled or stale links, and unsupported target/download metadata emit no click. Interactive root-unvisitable links now delegate directly through host navigation and emit no link-driven `before-visit`, while explicit programmatic controller visits retain their existing lifecycle. React Native deliberately exposes no fabricated DOM event/bubbling path or browser-default fallback.
- Add an optional synchronous `DocumentVisitLifecycle` to `DocumentVisitController`. Cancellable `before-visit` gates canonical initial and refresh proposals before history allocation, cache/request ownership, or host delegation and returns frozen no-request cancellation evidence. Notification-only `visit` runs once after exact ownership and `started` publication for network, cached-preview, explicit cached-restore, refresh, and cached/network host-traversal starts; traversal deliberately skips `before-visit`, preview revalidation does not duplicate the event, and request-local ordering places `visit` after the `before-fetch-request` listener phase but before pause/cancellation settlement, navigation bookkeeping, recent-request-ID publication, or physical fetch while retaining the logical proposal URL. Stable listener snapshots, exact synchronous-return admission, redacted observer failures with asynchronous surfacing when unhandled, thenable consumption, and proposal/ownership reentrancy guards keep newer work authoritative. Visual document/Frame render/load and morph lifecycle families remain pending.
- Add an optional typed request lifecycle to document loads/preloads, primary and recurse Frame GETs, and form execution/submission. Request-local `before-fetch-request` listeners can pause, cancel, or mutate cloned URL/method/header/body state, with every result re-admitted against transport-specific same-origin, method, body/negotiation, cache-key, and protected-header constraints before adapter fetch. Document preload retains its reserved cache key, Frame recurse context remains explicit, and forms report the admitted wire transport method separately so redirect safety, MIME admission, cache invalidation, and retry safety follow actual transport. `before-fetch-response` may prevent body reading and application with an explicit result. Non-abort rejection emits redacted, cancellable `fetch-request-error`; prevention suppresses default visit/Frame/form recovery and observer reporting plus refresh, traversal, and automatic-preload reporting while transport still rejects and cleanup still runs. Aborts and supersession remain cancellation-only, late settlement is consumed, listener failures are redacted, and one paused request cannot block another. An adapter-invoked safe GET retry is constrained to GET after live replanning and lifecycle mutation, while pre-fetch failures retain their existing retry behavior. Higher-level visual document/Frame render/load and morph lifecycle families remain pending.
- Add render-time automatic `data-turbo-preload` for semantic native document links. Hosts inject a shared `DocumentPreloadRequester` through `ExpoTurboProvider.documentPreloader`; each rendered component using `useExpoTurboDocumentLink` treats marker presence, including `"false"`, as intent and schedules only an enabled, navigable, fragment-free, root-visitable document destination. Initial, whole-document, cached-preview, matching-Frame, and Stream-inserted rendered links participate without scanning arbitrary XML tags, while inert protocol subtrees, method/Stream links, opt-outs, unsupported native target/download metadata, and Frame destinations remain excluded. Exact `_top` promotion may preload; Strict Mode and duplicate links share the underlying canonical reservation; cleanup does not cancel host-owned work; and only a still-current typed or redacted failure reaches the optional renderer error observer. Fragment-bearing markers, Frame response preload, hover/press-in prefetch, TTL/request reuse, redirects, and device evidence remain pending.
- Add Turbo 8.0.23-style cached previews to fragment-free ordinary top-level `advance` and history-backed `replace` visits. A preview-eligible cache hit now starts one visit, captures outgoing truth, commits the requested history proposal when configured, installs a provisional clone without document autofocus, and immediately performs one canonical GET under the same lifecycle/progress epoch; the authoritative replacement owns a fresh autofocus opportunity. Frozen `DocumentVisitSnapshot.previewVisible` plus `DocumentSession.treeState` expose whether the active whole-document tree remains provisional, including after empty, failed, or canceled revalidation; exact subscriber provenance cannot be hidden by an earlier synchronous state read. Canonical redirects perform Turbo's fresh same-index follow-up history `replace` before the final tree, while `no-preview`, fragments, and misses keep the prior network-only path. Exact tree-generation plus session-wide document-claim checkpoints prevent cache, request-ID, and finalization reentrancy from reclaiming newer work; commit-phase cancellation remains blocked, post-release cancellation is honored, and reported committed-preview finalization or host-clock timer failures cannot suppress canonical revalidation. Frame preview/preload, hover/press-in response reuse, and device evidence remain pending.
- Add a host-invoked `DocumentPreloader` for safe full-document GETs into a shared `DocumentSnapshotCache`. Fragment-free root-visitable sources send the normal Expo Turbo headers plus `X-Sec-Purpose: prefetch` without claiming document ownership or changing history, visit lifecycle, recent request IDs, or the active tree. Canonical URLs are reserved before cache/request-ID/transport callbacks; cache hits allocate no request ID and send no request; equivalent in-flight URLs share one request; successful exact/all cancellation blocks late caching until a protected final write; successful nonredirected nonempty Expo XML is parsed under snapshotted positive-integer limits and cache policy; `no-cache` returns a distinct non-retained result; and for a cacheable response an entry created while the miss is pending wins with a distinct `superseded` result. Host metadata is read once and request-ID/transport/metadata/body/parser failures are redacted. Frame preload, hover/press-in prefetch, TTL/request reuse, redirects, and device evidence remain pending.
- Harden staged form-request admission by reading every top-level planner field once, immediately freezing form, submitter, protocol, and entry scalar snapshots before reading later fields, and validating the request URL before touching entries. Property-read, revoked-Proxy, entry-array-trap, and entry-field failures now become redacted `RequestError`s; later getters cannot rewrite already admitted metadata. Active request-plan, proposal, submit, and retry entry points also stage and redact their outer option and protocol reads before composing or dispatching a request.
- Add bounded string-only HTML `text/plain` form serialization for raw-body endpoints. The planner now admits at most 1,024 entries, normalizes scalar values and CR/LF once, preserves ordered duplicate/empty names and values without escaping, emits exact `name=value\r\n` records with a final CRLF and `text/plain` content type, and rejects final UTF-8 bodies above 1 MiB. GET remains URL-encoded/bodyless; direct unsafe transport leaves `_method` literal, Rails effective POST may be consumed explicitly as raw text, and Rails body-based method overrides remain rejected because Rack cannot parse the standard's intentionally ambiguous format. Multipart/File/Blob transport remains pending.
- Add bounded, string-only multi-name native form entries through `FormControlDescriptor`'s `kind: "entries"`. One exact registered control with no outer `name` contributes up to `MAX_FORM_CONTROL_ENTRIES_PER_CONTROL` (256) cloned and frozen `{ name, value }` pairs at its whole-document slot, preserving empty names/values, duplicates, list order, and literal `_charset_` semantics through collection; form request planning consumes the resulting string entries. Exact form ownership/external association, direct and fieldset disabledness, datalist exclusion, validity, live replacement, and exact-node cleanup remain shared with other controls. The host supplies the list; XML/DOM discovery, automatic or inherited `dirname`, File/Blob values, browser `ElementInternals` lifecycle, and multipart/upload transport remain pending.
- Add a session-bound, host-injected `CableStreamSourceRegistry` for rendered `<turbo-cable-stream-source>` nodes. Within one registry it strictly admits channels, standard signed names, and deterministic snake-cased `data-*` parameters; shares one transport per complete canonical Action Cable identifier; serializes string messages through the existing Stream dispatcher; binds leases to exact active node identity; and cleans attribute changes, removals, replacement, unmount, and stale callbacks without connecting inert template/Stream/source descendants. Subscription, cleanup, non-string-message, and dispatch failures are redacted. React Strict Mode and multiple providers share the active transport through deferred final-lease cleanup. Socket selection, credentials, connection/rejection state, heartbeat, lifecycle/reconnect, authorization, canonical reconnect refresh, Rails/Redis integration, and device proof remain pending.
- Add one-shot native autofocus for every authoritative whole-document tree generation, including the initial session tree, network/full-document replacements, refreshes, cached restorations, and an explicit reinstall of the same tree object. Each pending claim records the exact session, generation, tree, document, ordered stable-ID application-element candidates, and candidate node identities before the generation becomes observable. A parent renderer layout effect consumes only the exact rendered generation after descendant handles mount, preventing Strict Mode, multiple providers, stale renders, same-ID replacements, and in-place Frame or standalone Stream mutations from duplicating or stealing focus. Nested Frames participate in document order, while protocol wrappers are not candidates and `template`/Stream/source subtrees are excluded. Cached preview generations suppress autofocus and their authoritative replacement receives a fresh opportunity. The shared synchronous `AutofocusAdapter` skips unavailable handles and focuses the first available candidate; a missing adapter permanently consumes the generation, and invalid returns, Promises, thenables, or throws are redacted without rolling back the committed logical tree. Standalone Stream/morph autofocus, autofocus scrolling, browser focusability parity, and physical-device evidence remain pending.
- Add one-shot native autofocus for controller-owned matching Frame GET and matching Frame-target form XML responses configured with the exact shared mounted Frame registry. After Frame replacement and embedded Streams settle, raw `autofocus` presence contributes only stable authored-ID application-element descendants from the exact final mounted Frame in document order. Nested Frames are traversed while Frame/protocol wrappers and `template`/Stream/source subtrees are excluded. Internal intents stay bound to their exact session, wrapper, ordered candidates, and candidate node identities through React consumption, allowing unrelated Frame mutations while rejecting changed candidates and same-ID replacements. The optional synchronous `AutofocusAdapter` runs after descendant native handles mount, skips unavailable candidates, and focuses the first available handle once under React Strict Mode; a missing adapter consumes the intent as a no-op and missing handles are skipped. Adapter throws, invalid return values, Promises, and thenables are redacted through the Frame error surface without rolling back committed children. Direct headless Frame application, standalone Stream/morph mutations, autofocus scrolling, browser focusability parity, and physical-device evidence remain pending.
- Add host-owned synchronous native constraint validation to registered value, checkable, multiple, and select controls. Exact frozen validity snapshots produce a complete whole-document invalid report, including unnamed and externally associated controls, while disabled/fieldset-barred, datalist-barred, hidden, submitter, and unconstrained controls are skipped. Interactive submit/retry honors form `novalidate` and activated-submitter `formnovalidate` presence, resolves invalid input as an ordinary `status: "invalid"` attempt, focuses only the first invalid logical node through the injected `FocusAdapter`, and starts no proposal, confirmation, activity/terminal, ownership, history, or fetch work. Focus must settle synchronously with `undefined`; failures, returned values, Promises, and thenables are rejected without retaining host errors. Entry collection, request planning, and proposal creation remain validation-free; server `422` XML remains authoritative. The standalone Expo example adds an app-owned Zod required-field rule, identity-safe `TextInput` focus registry, and user-driven focus tracking; physical-device and screen-reader evidence remains pending.
- Add a concrete Expo SDK 57 / Expo Router managed-history bridge to the standalone example. The app-lifetime Expo Turbo runtime lives above the Stack; `/` replace-redirects without attaching history; and the focused `[...expoTurboPath]` route maps only the fixed fixture URLs `https://example.test/demo` and `https://example.test/demo/linked` to `/demo` and `/demo/linked`, with both paths statically exported. The catch-all array plus three namespaced string params carry canonical path and restoration state, and path/managed-metadata mismatches fail closed. Same-document managed cold starts adopt without fetching or writing. Different-document managed cold starts perform one root-visitable fragment-free exact GET before adopting history in the guarded tree commit without a Router write; unmanaged direct-path cold starts perform the same exact GET and initialize history plus same-key metadata inside that commit. Exact authoritative `2xx`/`4xx`/`5xx` XML restores; redirects, empty/wrong-MIME/failed responses, stale route state, and focus loss leave both history and tree uncommitted and can retry after refocus. Advances synchronously `push("[...expoTurboPath]", params)`, replacements use `setParams`, and every failed/no-op/partial/thrown write resets and verifies the captured Router state before retry; failed rollback is terminal and visible. Pop/restored-state events feed the shared traversal controller with own-write/rollback echo suppression. Generic host path/query/fragment mapping, a dedicated cached-preview gallery/device scenario, and scroll parity remain pending.
- Gate the standalone route owner on Expo Router navigation-container readiness before the first history repair. This prevents production static-export hydration from presenting a pre-commit `setParams` no-op as host failure, and the gallery now includes a permanent path-changing `replace` link alongside its `advance` link.
- Replace the standalone Expo gallery's always-visible Frame stub with a concrete example-owned `VisibilityAdapter` for ordinary layout inside its root `ScrollView`. Non-collapsible Frame anchors register native window measurement, the scroll view supplies the measured viewport, layout/scroll/content-size/window-dimension changes trigger remeasurement, and unknown or unmeasured Frames remain hidden. The registry suppresses duplicate transitions plus stale Frame/viewport callbacks and cleans same-ID replacements identity-safely. Focused fixtures cover offscreen request suppression and one-time appearance loading; physical-device visibility evidence, nested scroll containers, virtualized `FlatList` cells, autoscroll, and concrete scroll adapters remain pending.
- Add Turbo 8.0.23 Frame-target form `advance`/`replace` coordination through the destination's exact mounted `FrameControllerRegistry` record and existing `FrameHistoryCoordinator`. Registered forms and generated-form links freeze explicit or inherited destination actions without creating a controller or issuing a Frame GET; exact `restore`, unmounted/historyless destinations, fragments, root-unvisitable URLs, and misaligned history fail before request ownership. Successful non-`204` Expo XML optionally captures the latest outgoing whole-document snapshot when a shared cache is configured and admits the final `src` before body buffering except for a potentially empty `201`, then commits optional snapshot storage → Frame-scoped history → preflighted Frame children/final `src`/logical document URL. Direct Frame visits and form promotions share the mounted controller's restoration identifier; `advance` always pushes and `replace` retains the current index. Authoritative error XML and Streams write no history entry or logical-document promotion but still apply their normal Frame or Stream mutation; empty/canceled/superseded/failed work, drift, and host failures write no entry or child/logical-document mutation, while an already-admitted final `src` may remain visible after later parse or matching failure. The standalone Expo gallery now wires one shared document history/cache into document, Frame, and generated-form controllers and exposes a promoted Frame form scenario.
- Add Turbo 8.0.23 history-backed explicit top-level `restore` visits. Root-visitable, fragment-free registered links and programmatic document visits require aligned initialized history, use a fresh restoration identifier, push for a different requested URL, and replace at the current URL. An unconditional cached hit, including `no-preview`, performs no GET or request-ID allocation, attempts the outgoing snapshot under normal cache policy (`no-cache` skips storage), commits history → restored tree, and returns frozen `restored`/`canceled` snapshot evidence; a miss performs one guarded GET, retargets the original proposal to an admitted successful final redirect URL, and keeps authoritative redirected or unredirected `4xx`/`5xx` XML commit-capable before the lifecycle reports failure. Fragment-bearing requested/final URLs, initially unvisitable targets, successful redirects whose final location is unvisitable, historyless, misaligned, canceled, superseded, empty, transport/MIME/parse/root-policy-failed, snapshot-failed, and host-write-failed work publishes no proposed entry or replacement tree. Host traversal remains the distinct already-moved-entry path; Frame-local and generated-form restore remain fail-closed.
- Add optional Turbo 8.0.23 top-level form history/action coordination through aligned `DocumentHistory`. Registered forms freeze first-present submitter/form action metadata, generated-form links copy exact document actions, and successful document XML proposes from the validated final response URL. Default/`advance` pushes except at the current URL, `replace` retains the index, safe GET application orders snapshot → history → tree, and unsafe success clears the shared cache before history → tree. Authoritative errors, Streams, empty/canceled/superseded/failed work, drift, and host failures write no entry. Historyless default/`advance` remains supported; document-form `replace`/`restore` fail closed until their required coordinator exists, while Frame-destination coordination is recorded above.
- Add Turbo 8.0.23 current-document refresh history synchronization. With aligned optional `DocumentHistory`, `refreshCurrent` now prepares a snapshot-free `replace` proposal before request ownership, keeps the current restoration index with a fresh identifier, retargets admitted same-origin redirects, and commits history before the replacement tree for valid `2xx`/`4xx`/`5xx` XML. The native history boundary intentionally normalizes Turbo's redirected follow-up visit into one atomic final-URL replace write. Historyless hosts retain ordinary refresh replacement, while stale/active suppression, empty/canceled/superseded work, transport/MIME/parse failure, discarded redirects, concurrent history movement, and host-write failure publish neither the proposed entry nor a new tree. Refresh morph and scroll policies remain pending.
- Add Turbo 8.0.23 lazy-appearance Frame history promotion. On first actual visibility, the controller re-reads live exact action metadata and prepares `advance`/`replace` through the existing `FrameHistoryCoordinator` before request-ID allocation or canonical `src` exposure. Immediate, synchronous-callback, and delayed visibility share one promoted load and the mounted controller's restoration scope. Exact `restore`, missing coordination, unvisitable sources, and history drift fail once before request ownership; invalid action values remain ordinary history-neutral lazy loads. The root-`ScrollView` Expo visibility implementation is recorded above; nested/virtualized visibility, autofocus scrolling, autoscroll, scroll adapters, and physical-device autofocus evidence remain pending.
- Add optional Turbo 8.0.23 form-response invalidation for a host-shared `DocumentSnapshotCache`. Unsafe successful document XML clears before strict parse/application, while authoritative document `4xx`/`5xx` clears after replacement commits, including committed finalization failure. Any unsafe successful or failed Frame response clears after exact response admission and before MIME/body/Frame parsing, including unsafe `204`/blank-`201`, wrong MIME, malformed XML, and missing Frames. Safe successful XML, document empty responses, direct Streams, denial, cancellation, supersession, and network failure preserve the cache. Frame-target form history now shares this cache contract; morph-aware `422` state preservation remains pending.
- Add optional promoted Frame `advance`/`replace` coordination through `FrameHistoryCoordinator`. One exact mounted Frame controller reuses a reserved restoration identifier; `advance` always pushes, including the same URL, while `replace` keeps the current index. Root-visitable direct-GET admission optionally snapshots the outgoing whole document before the requested absolute `src` becomes observable; primary changed-final/success response timing may retarget that source before parse. Matching `2xx`/`4xx`/`5xx` XML commits against the then-current history entry, so independent Frames publish in response-completion order, while a newer admitted whole-document visit or document-targeting form submission cancels an older promotion. The guarded path blocks package session/tree mutation APIs, protects active protocol relationships through frozen public nodes/index snapshots, and applies optional snapshot storage → history → Frame children/final `src`/logical document URL. Empty and failed work changes neither history, logical document URL, nor Frame children but retains the already-exposed requested/final `src`. Host traversal uses whole-document snapshots when available and otherwise one canonical GET, never a Frame GET. Frame-target form history is recorded above; Frame-local `restore`, scroll/anchor restoration, and previews remain pending.
- Add a preflighted final-ownership Frame commit transaction. Matching Frame GET responses now parse once, reject active-document ID collisions before the frozen callback, block reentrant same-session destination claims and exact cancellation during that callback, preserve wrapper/unrelated identities, and commit children plus the final canonical response `src` through a single-use prepared mutation that can also retarget the logical document URL without replacing the tree. Finalization-started newer work remains authoritative, stale embedded Streams stop with `interrupted: true`, and `FrameCommitError` distinguishes post-commit finalization failure. Promoted Frame history coordination is recorded above.
- Add `subscribeDocumentHistoryTraversal` as the host-neutral managed-entry subscription boundary for `DocumentVisitController.restoreTraversal`. Every newer host event invalidates older traversal work before its own admission, valid entries adopt synchronously, and only the newest event reports a result or typed failure. Unsubscribe invalidates late events and settlements before exact synchronous cleanup returning `undefined` without canceling controller work; lifecycle failures are synchronous and redacted. Promoted Frame and explicit top-level restore history coordination are recorded above; the standalone Expo Router mapping is now recorded above, while fragments and scroll application remain pending.
- Add host-driven back/forward restoration through `DocumentVisitController.restoreTraversal`. It admits only same-origin fragment-free host entries, adopts the validated entry without another history write, returns frozen direction/entry/restoration evidence, restores retained `no-preview` snapshots without a GET, retargets the fresh clone to the exact traversed URL, captures outgoing truth under shared final ownership, falls back to one history-neutral guarded GET on a miss, and lets a newer traversal supersede older work. Redirects and concurrent history drift fail closed while later traversal can recover; cached restoration remains cancellable before replacement; empty and failed restoration leaves the already-moved host/ledger entry adopted instead of pretending rollback. Promoted Frame, explicit top-level restore, and ordinary cached-preview coordination are recorded above; fragment/anchor restoration and scroll application remain pending.
- Admit history-backed explicit top-level `replace` visits through `DocumentVisitController`. A visitable replacement requires aligned initialized history, captures the latest outgoing snapshot when a cache is configured, writes a fresh restoration identifier at the current index through `replace`, and then commits the parsed tree under the existing final ownership guard; admitted redirects retain that method/index/identifier while adopting the final URL. Authoritative `4xx`/`5xx` XML commits before the lifecycle fails, while empty, canceled, superseded, transport/MIME/parse-failed, discarded, snapshot-failed, and history-host-failed work cannot publish a replacement entry or tree. Initial unvisitable replacements still delegate to the host, explicit restore is recorded above, and same-path replace morph/scroll behavior is not claimed.
- Wire optional aligned `DocumentHistory` coordination into parsed-tree top-level `advance` visits. Observable lifecycle starts only after exact document ownership is installed; admitted redirects retain the requested-location push/replace decision; the guarded commit order is outgoing snapshot, acknowledged history write, then tree replacement. Empty/canceled/failed/discarded work writes no history, snapshot or host failure preserves the old tree/history, and cancellation cannot relabel a released committed visit or clobber abort-listener work. Current-document refresh coordination is recorded above.
- Add a dedicated synchronous document-history host adapter and atomic host/ledger boundary. Managed initialization adopts existing state without a write; unmanaged initialization publishes only after an acknowledged index-0 replacement; one exclusive guard covers each public mutation from injected identifier generation through any exact push/replace and internal publication. Thrown failures, returned values, Promises, thenables, and hostile Promise assimilation are rejected and redacted before publication, leaving initialization or the exact proposal retryable. A host that mutates before violating the contract still requires authoritative external-state reconciliation.
- Add an optional final-ownership synchronous document tree-commit callback. It receives the exact frozen parsed candidate after ordinary admission, holds a session-shared guard against reentrant destination claims or exact-owner cancellation, rejects returned values and async callbacks, releases cleanly on failure, never runs for non-tree outcomes, and clears before tree finalization so newer listener-started work remains authoritative.
- Add frozen history visit proposals. Ordinary advance proposals choose push versus same-location replacement from the canonical requested URL, explicit replacement retains the current index, redirect retargeting preserves the original method/identifier/index, and exact current-base commitment rejects forged, stale, settled, reused-identifier, invalid, and overflowing proposals without mutating current history.
- Add explicit cached-preview eligibility without rendering or consuming a visit. `DocumentSnapshotCache.getPreview()` returns fresh default-policy clones, suppresses retained `no-preview` snapshots only after refreshing their LRU recency, preserves fragment-free keys, leaves suppressed entries available through `get()` for restoration, and does not admit `no-cache` writes.
- Add a native `DocumentHistory` identity/restoration ledger. It validates and freezes managed/unmanaged host state, generates an explicit index-0 replacement entry for unmanaged state, preserves managed entries for native remount continuity, derives Turbo's forward/back traversal direction, admits both replacement identifiers sharing an index and promoted Frame entries reusing one identifier across URLs/indexes, and retains finite scroll positions per admitted identifier. Host traversal, top-level replacement, explicit restore, and ordinary cached-preview consumption are recorded above; scroll capture/application remains pending.
- Capture the exact outgoing document snapshot immediately before an admitted parsed-tree `advance` or history-backed explicit `replace` visit's optional history write and tree commit when a `DocumentVisitController` receives a cache. In-flight mutations are included; valid `2xx`/`4xx`/`5xx` XML commits capture, including commit-before-finalization errors. Refreshes, empty responses, cancellation, transport/parse failures, delegated/discarded redirects, `no-cache` documents, and historyless `replace` do not use this parsed-response path; explicit restore and host traversal capture are recorded above.
- Add root-owned native cache policy metadata through exact lowercase `data-turbo-cache-control="no-cache"` and `"no-preview"`. `no-cache` skips snapshot writes without evicting an older admitted entry; `no-preview` remains cached for host traversal and explicit document restore while suppressing the ordinary cached-preview path recorded above.
- Add explicit document-session snapshot interop without admitting history actions. `DocumentSession.captureSnapshot()` records the exact active URL/tree pair, cache writes now reject missing or mismatched document URLs, and `restoreSnapshot()` returns a frozen hit/miss result while reusing the established tree-replacement, disposal, revision, identity, and stale-response safety contracts. Host traversal, explicit document restore, and ordinary cached-preview coordination are recorded above; scroll restoration remains pending.
- Add the host-neutral history foundation: production `DocumentTree.clone()` preserves exact keys/indexable structure without sharing mutable nodes or reusing mutation keys, while `DocumentSnapshotCache` retains ten independent snapshots under normalized fragment-free HTTP(S) URL keys with query identity, get/put LRU recency, clear/size inspection, and cached-copy-only `data-turbo-temporary` subtree pruning. Host traversal, explicit restore, and ordinary cached-preview consumption are recorded above; scroll restoration remains pending.
- Add Turbo 8.0.23 generated-form document links behind a host-configured `FormLinkSubmissionController`. Method/Stream marker presence now reuses the canonical form submission controller with exact link ownership, ordered query-to-hidden entries, `_charset_` rewriting, direct unsafe HTTP verbs, Stream negotiation, nonempty confirmation, and source/default/named/parent/top Frame destination resolution without a synthetic source-Frame origin. Browser-bypassed scheme/root/opt-out paths keep their existing delegation; exact link/inherited Frame actions and globally disabled native form interception fail closed before request ownership.
- Admit Turbo 8.0.23 destination-Frame action inheritance through the shared programmatic/registered-link visit path. An omitted action reads only exact lowercase `advance`, `replace`, or `restore` from the resolved destination Frame; invalid Frame values remain ordinary loads, present link non-actions explicitly mask inheritance, and promoted/external visits do not inherit. Local `advance`/`replace` coordination is recorded above; local `restore` remains whole-document-only and fails closed.
- Match Turbo 8.0.23 link-action recognition without overclaiming history support. Only exact `advance`, `replace`, and `restore` are actions; every other value retains the ordinary document, Frame, or delegated path. Browser-bypassed scheme, root-delegated, and opted-out paths ignore action metadata. History-backed top-level `advance`/`replace`/`restore`, owning-document promotion, and local Frame `advance`/`replace` support are recorded above; historyless document `replace`/`restore` and local Frame `restore` fail closed before request ownership.
- Match Turbo 8.0.23's browser link click predicate: absent, empty, and exact `_self` targets retain the current native document/Frame/delegation path, while every other target and any present `download` attribute fail closed before URL handling or request ownership. Disabled presence remains the earlier inert short-circuit.
- Extend explicit host-resolved `dirname` snapshots to hidden controls, preserving the direction companion immediately after the admitted hidden value or `_charset_` replacement through collection and GET/URL-encoded planning. Pin Turbo 8.0.23's manual submitter append as name/value-only without browser image coordinates or submitter direction sidecars, and record registered native controls as the string-valued equivalent of browser form-associated custom elements while File/Blob and arbitrary multi-name values remain unsupported.
- Match Turbo 8.0.23's exact Stream `method` selection. Structural `replace`/`update` actions now fail closed before mutation only for lowercase `method="morph"`, while every other value keeps plain semantics; document refresh likewise reserves only exact morph and treats absent, blank, arbitrary, or differently cased methods as ordinary replacement. Native morph and explicit refresh-scroll policies remain unsupported.
- Treat confirm-only native document-link activation like plain-link activation, matching Turbo 8.0.23: `data-turbo-confirm` alone is inert during activation across document, Frame, delegated, disabled, and request-supersession paths. This slice did not yet include method/Stream-generated form links, functional link confirmation, or confirm-based prefetch suppression.
- Add HTML-compatible select-option value fallback for explicit native snapshots. An authored string value, including `""`, wins; otherwise host-supplied option text is stripped and collapsed using only ASCII whitespace before successful-entry collection and request encoding. Hosts still own live selectedness, option order, group disabledness, and the raw text snapshot.
- Add explicit native hidden-control successful entries. Hidden values default to `""`; only ASCII-case-insensitive `_charset_` hidden names are replaced with `UTF-8`, while ordinary value descriptors remain unchanged and existing disabled/name/fieldset/datalist admission plus XML ordering still apply.
- Add strict native document-link delegation for normalized `mailto:` and `tel:` URLs through the existing host navigation adapter. Scheme links delegate before document/Frame capture from top-level, nested Frame, named-Frame, or opted-out contexts; disabled and unsupported-metadata admission still wins, fragments/raw C0 or DEL controls/credentials and every other scheme remain rejected, and no fetch ownership changes.
- Add capability-visible native datalist component roles. Explicitly registered controls under semantic datalist ancestry are omitted from successful entries regardless of external form ownership, while an explicitly selected submitter still follows Turbo 8.0.23's manual append-last behavior. React disabled/accessibility state remains unchanged because datalist ancestry bars ordinary entry construction rather than disabling the control. Automatic XML/DOM discovery, suggestion behavior, and child hiding remain unsupported and host-owned.
- Add capability-visible native fieldset/legend component roles plus a structural form-semantics adapter. Explicitly registered controls now inherit live disabled-fieldset ancestry with the first direct legend exception, independent nested scopes, external-owner physical ancestry, inherited-disabled control omission, inherited-disabled submitter rejection, and React effective-disabled accessibility updates. Automatic XML/DOM control discovery remains unsupported.
- Add Turbo 8.0.23-compatible native form interception modes: `on` by default, global `off`, and form-ancestor `optin`, with independent live `data-turbo` ancestry for internal or external submitters. Core and React expose a synchronous host interaction decision while explicit planning, proposal, submission, and retry APIs remain intentional bypasses.
- Add explicit native select snapshots: a disabled or unnamed select emits nothing, while selected options emit in host-supplied option order only when neither directly disabled nor inside a disabled `optgroup`, preserving repeated and empty values. Hosts retain selectedness/value/order/group-state ownership; XML/DOM option discovery, browser default-selection/value derivation, and the remaining successful-control matrix stay unsupported.
- Add bounded exact-ID external native form association: `form` overrides nearest ancestry, registered controls collect in whole-document order, external bindings/submitters share the declared exact form, owner removal/insertion/same-ID replacement rebinds through React, and stale submitter ownership invalidates proposal admission. Blank, missing, undeclared, and foreign owners fail closed; association does not move React/state-scope ancestry, and the remaining browser successful-control matrix stays unsupported.
- Add plain current-document Refresh Streams with target/template independence, Turbo's 150 ms trailing debounce, exact active-URL and active-visit guards, session-shared latest-20 request-ID suppression across document/Frame/recurse/canonical-form transport, and complete replacement through the existing document GET lifecycle. Morph and explicit scroll refresh policies remain unsupported.
- Add host-admitted `ltr`/`rtl` value direction companions in deterministic control order; browser control discovery, direction inference, complete eligible-control coverage, and the remaining successful-control matrix stay unsupported.
- Add exact-live-node disabled document links with XML presence semantics, frozen ignored activation results, incumbent request isolation, runtime disable/re-enable checks, and Expo `Pressable` accessibility projection. Export `presenceCodec` so host registries admit empty and valued presence attributes without confusing them with boolean-value attributes.
- Add host-injected exact-form terminal live announcements with one at-most-once exact-node adapter attempt per revision after cleanup, redacted structured input, host-owned localized copy, Strict Mode and multiple-provider deduplication, stale/disposal isolation, and synchronous or asynchronous delivery-error reporting that does not alter form settlement. The standalone Expo example maps the contract to React Native `AccessibilityInfo` plus persistent polite/assertive web live regions; physical screen-reader evidence remains pending.
- Add exact-form terminal submission reporting and host-rendered recovery: session-shared frozen snapshots distinguish applied, empty, canceled, uncommitted failure, and committed-with-error outcomes after pending presentation clears; exact-attempt settlement blocks stale overwrites; React hosts can register a stable form boundary, dismiss results, and explicitly retry only a safe uncommitted failure with a fresh request ID and current-value replanning. `retryFailure` refuses unsafe or already-committed outcomes.
- Add host-injected async native form confirmation with submitter-first presence precedence, immutable proposal capture, strict boolean admission, cancellation-safe hidden exact-form ownership, no busy/destination/fetch state before acceptance, and stale/duplicate/supersede protection. The standalone Expo example supplies a native Alert adapter with a settling browser-confirm fallback.
- Cancel exact active form requests after the last mounted React form-scope/provider owner unmounts without disposing the host-owned collection. Ownership lives on the session-shared exact form, so Strict Mode replay and multiple providers cannot cancel an incumbent prematurely; headless callers retain explicit cancellation ownership.
- Add session-shared exact-form submission activity and React form/submitter presentation: observable busy state tied to the immutable planner-issued request, atomic duplicate prevention by default, an explicit same-form `supersede` override, exact activated-submitter pending/effective-disabled/nonempty `data-turbo-submits-with` presentation, and deterministic cleanup on success, failure, abort, or removal before response application. The standalone Expo example exercises the path; validation/focus, lifecycle events and request-mutation hooks, physical accessibility evidence, and morph-aware `422` preservation remain unsupported.
- Make `FormSubmissionController` the canonical ownership-safe transport-and-application path: retain and recheck the exact destination lease across staged application, stop undispatched actions after reentrant supersession, dispatch negotiated Streams across HTTP classifications, apply successful destination-Frame/document XML plus origin-Frame `4xx`/`5xx`, surface post-commit finalization failures, treat ordinary `204`/blank-`201` as no-ops while updating redirected empty Frame sources, enforce unsafe nonredirect document `200`, consume adapter-followed bodies once without refetching, and prevent invalid or stale responses from beginning mutation. `FormRequestExecutor` remains candidate-only, and morph/local-field preservation remains unsupported.
- Add destination-aware form transport coordination with session-scoped document/exact-Frame lanes shared across GET and form requests, pre-admission non-cancellation, exact-form destination-change supersession, reentrant/stale-owner safety, and frozen destination-bearing unapplied candidates.
- Change active-form `requestPlan` metadata to reject caller-supplied `frameId`; Frame-targeted callers must use `submissionProposal` so destination and `Turbo-Frame` metadata are derived together.
- Add atomic active-form submission proposals with exact Turbo Frame target precedence, internally derived `Turbo-Frame` metadata, browser-target rejection, React exposure, and hidden form/submitter/destination/tree-generation admission identity without fetching or changing request ownership.
- Add caller-scoped form request execution with exact-plan forwarding, lane-local cancellation/supersession, same-origin final-URL and MIME admission, single-read buffering, and classified immutable response candidates without tree mutation.
- Make the standalone Expo example install reproducibly with Bun's isolated linker, disabled peer auto-install, and a refreshed local-package dependency graph.
- Add registration-bound submitter selection and immutable active-form request planning to the core and React APIs, composing current form/submitter attributes and live XML-ordered values into a caller-owned request plan without fetching.
- Unify registry and React text handling around inherited `xml:space`, explicit default reset, CDATA preservation, XML-whitespace collapse, and deterministic diagnostic serialization.
- Add a shared programmatic Frame visit API that resolves current, named, parent, and top-level targets through the existing controller loader and navigation adapter.
- Add bounded recursive Frame extraction through same-origin intermediary sources with token matching, independent request IDs, redirected-base resolution, and loop/depth protection.
- Add injected lazy-Frame visibility observation with first-visible loading, observer cleanup, and renderer error propagation for automatic requests.
- Wire opt-in Frame controllers into the React renderer so mounted Frames connect, eager sources load, lifecycle snapshots update, and removed subtrees cancel and dispose their requests.
- Add Turbo-compatible Frame target precedence plus named, `_self`, `_parent`, and `_top` resolution, and an observable Frame controller for eager/manual loading, reload, lifecycle state, and cancellation.
- Add the same-origin Frame request loader with protocol headers, MIME validation, empty-response handling, cancellation, and request-epoch protection.
- Add exact Frame response extraction, wrapper-preserving child commits, redirected source tracking, embedded Stream processing, and missing-frame failures.
- Add ordered structural Turbo Stream dispatch with target/selector resolution, payload cloning, ID collision handling, no-op semantics, and per-action failure isolation.
- Add the static React protocol renderer, revision-cached document session, isolated node subscriptions, renderer error surfaces, and a real XML-rendered Expo gallery scenario.
- Add matching TypeScript/Ruby version baselines plus the TypeScript package subpaths, host-neutral adapter contracts, redacted protocol error taxonomy, and development inspector foundation.
- Establish the governed TypeScript/Ruby repository scaffold without a runtime compatibility claim.
